# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Paper HTML build cache (tikz standalone compiles)
paper/.build/tikz-html/

# Build products (canonical PDF lives in paper/build/, which git already skips as untracked)
paper/build/
paper/paper.pdf
paper/tools/fig4-artifact/dist.html

# The zoo's per-run renders and diffs; the blessed goldens are tracked
tests/zoo/out/
