% Moire Fields. Build: tectonic -X compile paper.tex --outdir build
%
% SUBMISSION MODE (the line below). TOG's author guidelines require exactly
% these options: `acmtog` for the two-column journal template, `anonymous` to
% hide the byline, and `review` for the line numbers referees cite. TOG is
% author-anonymous to referees, so the byline below stays in the source and the
% class suppresses it -- safer than deleting it and forgetting to restore it.
% See SUBMISSION.md for the full checklist.
\documentclass[acmtog, anonymous, review]{acmart}
%
% Do not hand-edit that line to make a named copy. `node tools/arxiv.mjs` builds
% one -- byline showing, line numbers gone, ACM copyright block off -- from this
% same file, so the public PDF cannot fall behind the submission PDF.

\usepackage{amsmath}
% acmart's text font already defines a blackboard k; amssymb wants to as well.
\let\Bbbk\relax
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\usepackage{algorithm}
\usepackage{algpseudocode}

\pgfplotsset{compat=1.18}
\usetikzlibrary{calc, arrows.meta, decorations.pathreplacing, patterns, positioning, fit}

\definecolor{ink}{HTML}{15181C}
\definecolor{accent}{HTML}{C81E5A}
\definecolor{cool}{HTML}{1B6CA8}
\definecolor{warm}{HTML}{D4761A}
\definecolor{soft}{HTML}{8A94A0}
\definecolor{paperbg}{HTML}{F7F7F5}

\pgfplotsset{
  paperaxis/.style={
    width=\linewidth, height=4.4cm,
    tick align=outside, tick pos=left,
    axis line style={gray!60},
    grid=major, grid style={gray!18, very thin},
    label style={font=\small}, tick label style={font=\footnotesize},
    legend style={font=\footnotesize, draw=gray!40, fill=white, fill opacity=0.92,
                  text opacity=1, inner sep=2pt, row sep=-1pt},
  },
  smallaxis/.style={paperaxis, height=3.6cm},
}

% Figure captions read as standalone paragraphs, so they get a lead-in.
\newcommand{\figlead}[1]{\textbf{#1}\ }

% A panel and its magnification as one column: the window drawn on the wide view,
% the detail hanging under it, and two rules joining the window's lower corners to
% the detail's upper ones, so the reader is told what is a close-up of what
% without having to be told in the caption. The four coordinates are fractions of
% the wide image, origin bottom left, and come from the experiment that chose the
% window rather than from the eye.
\newcommand{\detailpanel}[6]{%
  \begin{tikzpicture}[inner sep=0, outer sep=0]
    \node[anchor=south west] (zo) at (0,0) {\includegraphics[width=\linewidth]{#2}};
    \node[anchor=south west] (im) at ($(zo.north west)+(0,0.34cm)$)
      {\includegraphics[width=\linewidth]{#1}};
    \begin{scope}[shift={(im.south west)},
      x={($(im.south east)-(im.south west)$)},
      y={($(im.north west)-(im.south west)$)}]
      \coordinate (dpl) at (#3,#4);
      \coordinate (dpr) at (#5,#4);
      \draw[cool, line width=0.8pt] (#3,#4) rectangle (#5,#6);
    \end{scope}
    \draw[cool, line width=0.4pt] (dpl) -- (zo.north west);
    \draw[cool, line width=0.4pt] (dpr) -- (zo.north east);
    \draw[cool, line width=0.8pt] (zo.south west) rectangle (zo.north east);
  \end{tikzpicture}%
}

% Two narrow columns full of monospaced identifiers and inline mathematics leave
% TeX few break points; a little emergency stretch is cheaper than a slug of
% overfull lines.
\setlength{\emergencystretch}{1.2em}
% A slash inside a monospaced path, with a break opportunity after it.
\newcommand{\pslash}{\discretionary{/}{}{/}}

% Reference entries carry unbreakable DOIs, so a few lines cannot be justified at
% all. Be sloppy there rather than in the body, where the stretch would show.
\AtBeginEnvironment{thebibliography}{\sloppy}

% Labels centered under the panels of a strip figure. The strips are assembled as
% single images by tools/lib/render.mjs, so the geometry has to be restated here:
% #1 is the panel width and #2 the gutter, both as fractions of the image width,
% and #3 is a comma list of labels.
% The row is one unbreakable box: the fractions sum to the full width by
% construction, so any line breaking at all would scatter the labels.
\newcommand{\panelrow}[3]{%
  \par\nointerlineskip\vskip 2.5pt%
  \noindent\makebox[\linewidth][l]{\footnotesize
    \foreach \panellabel [count=\panelno] in {#3}{%
      \ifnum\panelno>1\hspace{#2\linewidth}\fi
      \makebox[#1\linewidth][c]{\panellabel}%
    }\hss}%
  \par
}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Rot}[1]{\mathbf{R}_{#1}}
\newcommand{\rad}{\rho}
\newcommand{\idx}{\phi}
\newcommand{\ph}{\psi}
\newcommand{\het}{\eta}
\newcommand{\Moire}{\textsc{Moir\'e}}
\newcommand{\ours}{\textsc{Window}}
\newcommand{\sweep}{\textsc{Sweep}}
\newcommand{\loose}{\textsc{Loose}}

\input{numbers}

% TOG asks that the byline be replaced by the ID Manuscript Central issues when
% the submission is created. Fill this in and rebuild before uploading the PDF.
\acmSubmissionID{}

\acmJournal{TOG}

% TOG requires author-year citations. acmart's default for `acmtog` is numeric,
% so leaving this out silently typesets the wrong style.
\citestyle{acmauthoryear}

\begin{document}

\title{Moir\'e Fields}

\author{Neo Mohsenvand}
% TODO(orcid): register free at https://orcid.org/register and paste the iD
% here. TOG requires one from the submitting author AT SUBMISSION, and from
% every author before an accepted paper reaches production. The `anonymous`
% class option hides it during review. See SUBMISSION.md section 1.1.
\orcid{0000-0000-0000-0000}
% obeypunctuation stops acmart appending a comma after the institution; the class
% insists on a city and a country, and there is no true value for either, so both
% get a space rather than an invented place.
\affiliation[obeypunctuation=true]{\institution{Independent}\city{\ }\country{\ }}
% The class footnotes an address block built from the affiliation, which for a
% single unaffiliated author is just the byline again.
\authorsaddresses{}

\begin{abstract}
Superpose two dense families of curves and a third pattern appears that belongs
to neither. Graphics treats moir\'e as an aliasing artifact to filter away, or
as a printed pair of gratings solved offline and slid by hand; neither supports
interactive authoring of the pattern itself. We represent a layer instead
as two scalar fields: a \emph{counting map} naming the nearest member of a
curve family, fractionally, and a distance field to it, so that superposition
composites fields rather than pixels and no raster exists at any stage. The
counting map exists in exactly three ways, and the taxonomy organizes the
subject: as a global formula, in which case the fringes of a superposition are
the unit level sets of the difference of two counts, with a saturating tent
profile that is exact under stated local hypotheses, without periodicity, and
a per-pixel validity criterion; up to an integer winding, in which case
fringes \emph{end}, at authored defects whose signed endings count the winding
exactly; or only as a search, when each member carries its own rigid motion
and the family folds over itself. Which of the competing beats is visible is a
best-approximation question: the beat hierarchy of two pitches is the
continued fraction of their ratio, and per-pixel lattice reduction finds the
winner at any order, where the order-two cap it replaces missed a fifth of
visible fringes. For the folded case, the support function of the generating
shape supplies a provably sufficient search interval, a Lipschitz skip rule,
and an exact closed form for translated polygons:
$\gpuSpeedLo$--$\gpuSpeedHi\times$ faster than the fixed enumeration it
replaces across rotated settings, in exact agreement with brute force wherever
the interval fits its budget. We present \Moire{}, a browser tool built on the
model, and show that choosing two layers so that their count difference is an
authored field turns the moir\'e into a live contour plot of that field, whose
fringes localize to the true level sets within a mean of
$\contourWorstErr$\,px on the hardest field tested.
\end{abstract}

\begin{CCSXML}
<ccs2012>
<concept><concept_id>10010147.10010371.10010372</concept_id><concept_desc>Computing methodologies~Rendering</concept_desc><concept_significance>500</concept_significance></concept>
<concept><concept_id>10010147.10010371.10010396</concept_id><concept_desc>Computing methodologies~Shape modeling</concept_desc><concept_significance>300</concept_significance></concept>
<concept><concept_id>10003120.10003121.10003128</concept_id><concept_desc>Human-centered computing~Interaction techniques</concept_desc><concept_significance>100</concept_significance></concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Computing methodologies~Rendering}
\ccsdesc[300]{Computing methodologies~Shape modeling}
\ccsdesc[100]{Human-centered computing~Interaction techniques}

\keywords{moir\'e, interference, distance fields, support functions, procedural
patterns, interactive authoring, GPU rendering}

\begin{teaserfigure}
  \includegraphics[width=\linewidth]{figures/teaser-row1.png}
  \panelrow{0.24615}{0.00513}{%
    (1) two ring families,
    (2) a terrain field,
    (3) counter-spirals,
    (4) a hexagonal twist}
  \vspace{2.5pt}%
  \includegraphics[width=\linewidth]{figures/teaser-row2.png}
  \panelrow{0.24615}{0.00513}{%
    (5) a four-vortex flow,
    (6) one walking family,
    (7) three cocentric ring families,
    (8) two walking families}
  \Description{Two rows of four square panels, each split down the middle by a
  dashed vertical rule. In every panel the left half is a dense black hairline
  drawing and the right half is a smooth grayscale field showing the same broad
  bands with the fine lines gone. Top row: two overlapping ring families making
  hyperbolic and elliptical channels; wobbling slanted lines making a contour
  map of rolling hills; two crossing spirals making a fan of rays; two
  hexagonal lattices at a small twist whose averaged half is a lattice of
  bright spots. Bottom row: slanted lines carrying four swirling vortices; a
  pinwheel of nested hexagons; a bullseye of fine concentric rings whose
  bold beat rings swell and fade on a slower radial wavelength;
  and two families of drifting circles whose paired rosettes fan out into
  swirling arms.}
  \caption{\figlead{Eight superpositions rendered directly from fields, with no
  intermediate raster.} Each panel is cut at the dashed rule: to its left the
  pattern as drawn, to its right the same parameters under the envelope view of
  \S\ref{sec:envelope}: the stack averaged over one period of its own phase,
  which removes the carrier exactly and leaves the fringe field it was
  carrying. The same bands cross the rule textured on one side and smooth on
  the other because the right half is not a filter applied to the left: both
  halves are per-pixel evaluations of the same two scalar fields per family, a
  fractional index and a distance, so the pattern is identical at every zoom
  and every parameter is live.
  \textbf{(1)}~Rings about two centers: hyperbolae where the radii differ by a
  whole spacing (Theorem~\ref{thm:fringe} in its simplest instance) and
  ellipses where they agree, the difference and sum moir\'es at once.
  \textbf{(2)}~Two identical line families, one displaced by a \emph{chosen}
  field: the fringes are that field's contours (\S\ref{sec:beyond}), here a
  band-limited terrain. \textbf{(3)}~Two spirals of opposite chirality: the
  radial parts of the two indices cancel, their difference depends on the polar
  angle alone, and the fringes are a fan of rays. \textbf{(4)}~Two hexagonal
  lattices at a $5^\circ$ twist: the averaged half resolves the moir\'e
  superlattice of coincidence spots, the pattern that reorganizes twisted
  bilayer graphene~\cite{Cao2018Magic}; a lattice indexes members by a pair of
  integers, so its envelope is the rank-two case of \S\ref{sec:envelope}.
  \textbf{(5)}~The construction of (2), carrying the stream
  function of four point vortices: the fringes are the flow's streamlines, and
  no trajectory was integrated.   \textbf{(6)}~A \emph{single} walking family
  (member $n$ turned by $n\theta$, pushed by $n\delta$) with no global index
  field and no partner layer: it interferes with itself, and finding the
  nearest member is the search problem of \S\ref{sec:inverse}.
  \textbf{(7)}~\emph{Three} ring families sharing a center, spacings tuned
  so their reciprocals are nearly arithmetic: each pair beats at its own
  ring system ($37$, $61$ and $97$ world units), and the three-layer
  zero-sum character $(1,-2,1)$ — a beat \emph{between} the beats — sweeps
  the slow envelope that swells and fades the rings at a period ($\sim$$160$)
  no pair of layers possesses. The one panel whose fringe field only the
  joint index torus of \S\ref{sec:envelope} can name.
  \textbf{(8)}~Two walking circle families drifting against each other, the
  tool's default preset.
  Settings in the supplemental material (\S S4).}
  \label{fig:teaser}
\end{teaserfigure}

\maketitle

\input{sec-intro}
\input{sec-related}
\input{sec-field}
\input{sec-super}
\input{sec-defects}
\input{sec-walking}
\input{sec-inverse}
\input{sec-system}
\input{sec-results}
\input{sec-beyond}
\input{sec-discussion}

\bibliographystyle{ACM-Reference-Format}
\bibliography{refs}

\appendix
\input{app-proofs}

\end{document}
