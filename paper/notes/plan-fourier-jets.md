# Plan: pictures on tori as the semantics of an anti-aliasing compiler

The aim is to move anti-aliasing of procedural shading from approximation to computation, with a theory that is short enough to state on a page, a compiler that follows from the theory with one rule, and demonstrations that a graphics audience cannot argue with: a public benchmark at its noise floor, automatically, and the same shaders running exactly filtered in real time in a browser. This note is the plan, the design, and the ledger of what each stage must show.

## The thesis in one paragraph

A shader is a program from pixel position to colour. Trace it and every intermediate is one of two things: a smooth function of position, or a non-smooth primitive (fract, mod, floor, sin, cos, step, sign, abs, max, min, comparisons) applied to something. The theory of the third pattern says what the second kind is: the smooth argument of a non-smooth primitive is a *count*, and everything computed from it is a *picture on the torus* of the counts so far. So any shader is F(c₁(z), …, c_K(z); p₁(z), …, p_M(z)), periodic in K counts and smooth in M parameters, and the anti-aliased pixel is E[F] under the pushforward of the pixel's Gaussian through the counts and parameters: the pixel theorem of `antialiasing.md`. Nothing in that sentence is an approximation rule. The current state of the art (Yang and Barnes 2018) propagates a mean and a variance through every operation with a rule per operation and a genetic search to choose among them; this replaces the rules by one fact about what the intermediates are, and the search by an arithmetic that says in advance which recipes are slow.

## The theory to write (paper 4, "Fourier jets")

1. **Fourier jets.** A *jet* is a value with its gradient and Hessian in pixel coordinates (second-order automatic differentiation). A *Fourier jet* is a finite sum Σ_k a_k(z) e^{2πi k·c(z)} with recipes k ∈ ℤ^K, coefficient jets a_k and count jets c_j. The Fourier jets over a set of counts form an algebra: closed under sums, products (recipes add, coefficients multiply), smooth functions of the count-free part (jet composition), and periodic functions of counts (by expansion). Non-smooth primitives extend the count set: fract(s + P) with s smooth and P periodic makes s a new count and the result a picture on the enlarged torus.
2. **The pixel functional.** For a Gaussian pixel of covariance Σ, E[a(z) e^{iφ(z)}] with a quadratic jet a and a quadratic phase φ is closed form: the quadratic-phase Gaussian integral I₀ = det(I − iΣQ)^{−1/2} exp(−½ bᵀ(Σ⁻¹ − iQ)⁻¹b) and its first two moments I₁ = iM⁻¹b I₀, I₂ = (M⁻¹ − M⁻¹bbᵀM⁻¹) I₀. So the pixel functional on Fourier jets is explicit, exact to third order in the counts' curvature, and its cost is the number of recipes with non-negligible coefficient times multiplier: the slow recipes, enumerable before any term is computed (the oracle).
3. **Modes per axis.** Each count has a pixel-sigma σ_c = σ|∇c|. Spectral mode (σ_c above a threshold): the axis is expanded and multiplied. Local mode (below): the count is nearly constant across the pixel, the picture is piecewise smooth on the reach, and the axis is integrated by quadrature with its jumps located; products with spectral axes are conditioned. Edges (steps of smooth arguments) are always local. This is the trichotomy of pixels made operational, and it is why the near field and the far field are free.
4. **Shaded pictures and tables.** The observer theorem says shading composes into the picture: the object to filter is N∘I, never N(E I). A picture on a torus of one or two counts, shaded, has a *toroidal mipmap*: its Gaussian blurs at a ladder of widths on the wrapped torus. The pixel value is that table read at the pushforward footprint, anisotropy by taps along the footprint's major axis, exactly as hardware texture filtering does, on the right torus with the right picture. That is the speed path and it reuses the industry's hardware.
5. **Time.** A shutter is a third pixel dimension. Counts have time derivatives (optical flow of the pattern); the multiplier is at the spatio-temporal rate; the exposure theorem of the paper says which recipes an accumulation keeps. Exact motion blur of procedural content falls out, and temporal anti-aliasing's ghosting is the fold rung in (x, y, t): a count whose jet does not exist across a disocclusion.
6. **Sampling.** Where no formula applies (folds, high-dimensional tori beyond the tables), the sampling half of the theory places samples: stations of the sampler against the content are the aliases, the desert is the placement that has none.

## The compiler (Stage 1: CPU, exact)

A small expression language in JavaScript, the same shape as theirs (their shaders port line for line): constants, pixel coordinates, arithmetic, smooth functions, and the non-smooth primitives. Tracing a shader builds elements:

- A **term** is a coefficient jet times a product of single-axis periodic closures, one per count it touches, and at most one multi-axis closure where a non-smooth primitive was applied to a smooth part plus an existing picture (fract(q + 0.2 sin ψ) is one).
- An **element** is a sum of terms. Sums concatenate. Products distribute, closures on the same axis multiply. A smooth unary on a single-term single-axis element composes the closure; a polynomial (integer power) distributes; a non-polynomial smooth function of a multi-term element collapses it to one multi-axis closure. A non-smooth primitive on an element with a smooth part makes that part a new axis.
- The **evaluator**, per pixel: count jets and parameter jets by automatic differentiation; a mode per axis from σ_c; spectral axes expanded by a numerical transform of their closures (cached when the closure has no pixel-dependent parameters, which is most of them), the recipe lattice enumerated in the ellipse of slow recipes, each term's pixel functional in closed form with curvature; local axes integrated by jump-aware quadrature with the spectral part conditioned; the output assembled.

What Stage 1 must show: the five hand-derived cases of `benchmark-yb.md` reproduced automatically to the same noise-floor numbers, then the remaining shaders of the benchmark (zigzag, fire, colour circles, bricks with their Gabor noise) and the parallax variants, all automatic, all at the floor. That is the whole 21-shader benchmark from one rule.

## Speed (Stage 2)

The compiler emits three things per shader: the oracle (term count per pixel from the jets and the pictures' spectra), tables for the pictures of one and two counts, and the fallback. The CPU frontier is measured as in `benchmark-yb.md`; then a WebGPU emitter puts the same shaders in the tool, exact, at frame rate, with a comparison slider against point sampling and supersampling. The claim to make: exact filtering of procedural materials at the cost of texture lookups.

**Which table (measured, `paper/tools/exp/tables.mjs`, `paper/data/tables.json`).** The first plan said toroidal mipmaps: the picture blurred isotropically at a ladder of widths, read at the pixel's count covariance with taps along the major axis, as hardware anisotropic filtering does. Measured on the benchmark's plane for the checkerboard and the circles, over the 26122 pixels whose minor width is at least 0.02 periods (the rest are local and no table's business): taps spaced 1.5 minor widths apart, so that the minor blur hides the comb of taps, reproduce the exact spectral sum to 6.6e-4 RMS (5e-3 worst), but need 108 lookups a pixel on average, four times the anisotropy ratio, which reaches 50 at the horizon. Capped at 16 taps by raising the minor width, as hardware does, the error is 5.7e-2 RMS on the checkerboard and 4.0e-2 on the circles, worst 0.4: hardware anisotropic filtering cannot reach this benchmark's floor on these pixels, whatever the picture. The exact sum over the same pixels needs 175 recipes a pixel on average, 939 at most, no more than the taps, and it is exact. So the table to put on the GPU is the spectrum: a texture of the picture's Fourier coefficients, read at the lattice points inside the multiplier's ellipse, each read an exp and a cosine. The spatial mipmap has its place where the anisotropy is mild (the mid field, fewer than 16 taps) and none where it is not; the spectral table has no such limit and the oracle is its term count. The near field stays quadrature, which the compiler already does in a few closed forms per pixel.

## Time (Stage 3)

Their shaders animate. The compiler's jets gain a time component, the pixel a shutter, and the benchmark a motion-blurred truth. The demonstration is the exposure theorem on a moving pattern: the accumulated frame is what the theory says a shutter keeps, and a rippled surface under motion blur is exact where temporal anti-aliasing smears.

## What would make it a breakthrough, honestly

Not the theorems, which are elementary. The combination: one rule replacing a rulebook, a benchmark at its floor automatically, hardware-speed filtering by reusing mipmaps on the right torus, and motion blur for free from the same jets. If Stage 1 and Stage 2 both land, the method is better than the state of the art on both axes it is measured on, and general. If only Stage 1 lands, it is a strong paper. The risks, in order: shaders whose count set is large (fire) making per-pixel transforms expensive, which the product-of-single-axis representation and caching are designed to prevent; smooth functions of multi-axis pictures forcing large transforms, which should be rare; and the tables' accuracy under strong anisotropy, which is the known EWA trade and can be measured.

## Ledger of Stage 1 (what the compiler had to learn)

Each of these was a wrong number at a probe pixel, and each is the theory, not a patch.

- A count's harmonic range is set by the slowest rate its multiples can reach with the other counts' help, not by its own rate: the quadratic sine at (240,20) has the sawtooth's fifth harmonic in register with the sine field's first through the perspective, a station, and a range of two harmonics missed it by 9e-3.
- The family of that station has a stationary point inside the pixel, a fold, so its multiples decay algebraically; the pruning uses the second-order magnitude of the quadratic-phase integral, which is exact, and a curvature budget where the recipe is not yet complete.
- Two local axes are evaluated at the point they fix, never through a truncated series; a second local axis has its jumps located with the first fixed; the first axis's nodes are placed on the integrated function along the second, with its own jumps located and panels refined where halving changes the sum, because an edge whose position moves with a field along the second axis makes that function steep but continuous.
- An axis slow along the conditioning line is integrated pointwise along it. Freezing it to its value at the line's centre cost 5e-2 at one pixel of the quadratic sine.
- One axis per count: sine and cosine of one argument, relu and step of one argument; a count that is an integer multiple of an existing one is the same torus, and the two axes of the palette lookup (argument modulo 8, its fractional part) otherwise make an exact station and a million recipes.
- An edge whose argument carries a field is periodised over the field's amplitude, and is a local axis in raw units when the smooth part's reach is small against that amplitude: the specular highlight on a rippled surface.
- A count's width under the pixel includes its curvature: the standard deviation of g.z + z^T H z / 2, sqrt(sigma^2 |g|^2 + sigma^4 |H|_F^2 / 2). A reviewer's example showed the old rule wrong: a count at its stationary point (rate zero, curvature not) had been frozen at its value, and cos(2 pi t^2) under a pixel of sigma 1/2 returned 1 where the exact pixel is Re (1 - i pi)^(-1/2) = 0.4446. Now an axis is frozen only when its width is under 1e-9, or when it is narrow (under the local threshold) and mostly curvature, and then at the count's mean over the pixel u0 + sigma^2 tr(H) / 2 rather than its centre value (a stationary count suppresses none of its harmonics, so neither the line quadrature nor a series in its coordinate is available; the fire's sin(0.005 x^2) count on the axis x = 240 is one, and freezing at the mean took (240,120) from 2.8e-4 to 1.3e-4, while treating it spectrally cost 3e-3 through the palette field's truncated series); a narrow axis whose rate dominates is local, sized by its first-order width; everything else is spectral, where the quadratic phase is closed form (gate fjet-stationary.mjs: exact to 1e-10 at three curvatures and five offsets; the hard-edged version, sign of the cosine, to 2e-5 at the stationary pixel and 3e-3 half a pixel off, where the far harmonics decay only as k^(-3/2) under the cut). A sum of pictures on one axis with constant coefficients is one picture; a composition whose coefficients vary across the pixel is a jet-valued closure.

- A step of a sum: a non-smooth primitive of a constant plus pictures and closures on some counts X plus pictures with constant coefficients on further counts Phi that carry fields over X, g(c + A(x) + sum_j B_j(phi_j + G_j(x))). Colour circles is one: sign(r - rVar) with r the disc's radius on the two cell counts and rVar a picture on the two counts (s + t)/(2 pi d), (s - t)/(2 pi d) of the floors' smooth parts, each shifted by a sawtooth field over the cell counts. As a function of phi it is a picture on the Phi torus whose coefficients T_m(a) depend on a = c + A(x) alone, so its transform over all four axes is the table T_m(a), tabulated once over the range of a, times the transform over X of T_m(c + A(x)) e^{2 pi i m.G(x)}, which does not depend on the pixel and is remembered across it. The table is exact: g is linear on each side of zero (step, sign, relu, abs), so between the roots of a + B(phi) = 0 and the jumps of B the integrand is linear in B and Gauss-Legendre integrates it, the roots and jumps located; for two Phi axes the inner transform is exact at each value of the outer variable and the outer integral is cut where a + B_2 meets a critical value of B_1 (a square-root singularity) and at the jumps of B_2, each panel mapped by a cosine so that its ends are regular. Checked against brute force to 1e-6, the brute force's own limit. The evaluator handles any split: a local Phi axis is a number and its picture joins a, local X axes are fixed and the grid runs over the residual ones (rebuilt per point), and the X harmonics that can pass the cut with a Phi recipe are enumerated from the second-order bound as an ellipse of rates around the station, not scanned. The rule fires only where the plain closure would reach three axes or more; fire's sign of a sine with a field over one count stays on the residual paths that were measured. Measured on colour circles against a million-sample brute force (noise 3e-4): (400,60) 1.2e-5, (120,34) 6.4e-6, (30,5) 8.1e-5, (300,12) 5.6e-4, (240,5) 1.0e-3.
- A step of a closure over two axes has its outer transform cut where a jump curve is tangent to the rows (the number of jumps on a row changes: a square-root singularity of the row's coefficients) and where the closure jumps along the outer axis, and integrated on cosine-mapped Gauss-Legendre panels between; the uniform outer rule it replaced had 4K + 8 rows, twelve at the horizon where K is one, and the circles' horizon carried 2.8e-3 from it. Now (240,20) 2.6e-4, (240,34) 1.6e-4, (30,5) 4.4e-4 against a million samples.
- A term may hold several steps of sums, pictures with fields and other closures on the same counts: the checkerboard with bumps is two steps (its two sawtooths, each with a parallax field over the bump's two sines) times the shading closure over those sines. The product is one transform over X with the tables' product, the field pictures' phase and the closures' values on the grid, and the Phi recipes run over the union of the steps' residual Phi axes. A step of a sum is never merged into a product closure (the evaluator multiplies the factors itself): merged, its structure was lost and the zigzag with ripples fell to a three-axis closure.
- A closure takes the bare coordinates of its axes and adds their fields itself; point evaluation had been handing it coordinates with the fields already added, harmless while no closure's axis carried a field and a doubled field once a step's X axis did (the zigzag with ripples at (240,120), 5.5e-3).
- A field is a jet across the pixel, not a number: its coefficients vary (the parallax term 2 a s delta of the rippled quadratic sine has coefficient zero on the axis x = 240 and a gradient there), so point evaluation carries the pixel displacement (the coordinate map's key -1) and evaluates fields at it, and the spectral paths take the fields' phase as a jet whose derivatives the term expectation integrates. The on-axis pixels of both rippled shaders were this: (240,120) 4.6e-3 to 1.4e-3 for the quadratic sine, 5.5e-3 to 3.2e-4 for the zigzag, against a million samples.
- A root of a + B(phi) that sits on a sample of B (a sign of a sine with no offset) is a boundary of the pieces itself, and the wrap sample is the first sample exactly; a sign test between samples missed it and the transform returned the wrong constant.
- The parallax theorem. A bump-mapped surface with parallax shifts every count by the height times the view: the closure is F(x + c_X H(s), phi + c_phi H(s)) for one function H of the bump counts s, with a scalar coefficient per field. Its transform over (X, Phi, S) is F's transform at (k, m), computed with the shift's terms dropped (the coordinate map carries the function to skip), times the transform over S of O(s) e^{i theta H(s)} at theta = 2 pi (k . c_X + m . c_phi + k_f . c_f), a one-parameter family tabulated once by FFT on a theta grid with its first two theta derivatives, which carry the view's variation across the pixel as the coefficient's jet. Recognised at trace time (the shift axes are the counts that enter only through fields, every such field term being the same function H) and at evaluation for plain pictures with fields too; the X harmonics are enumerated by their distance to the shift lattice, since a shift harmonic can complete a rate the X harmonic alone leaves fast. A step of a two-valued step (fire's select of its sign) is affine in it, and two steps (0/1) of one sum are nested sets whose product is the one with the smaller offset. Checkerboard with bumps and colour circles with bumps run through it: (400,60) 5.4e-4, (300,12) 4.1e-4 for the checkerboard, (300,12) 1.7e-3, (120,34) 1.3e-3, (400,60) 4.1e-4, (100,120) 4.0e-5 for the colour circles, against 100k to 200k samples. Fire with bumps does not: its pattern's period is the bump's own (the sign's count is the bump's u count, a self-referential shift) and its palette's field is a second function of the bump counts; it wants the general grid over three counts or a two-parameter family, both still to build.
- Closures take bare coordinates everywhere, the local quadrature's jump scan included, which had been handing them doubled fields: colour circles with bumps at (400,60) carried 7.2e-3 from it.
- The two-axis jet transform runs on typed arrays (four times faster) and probes a modest harmonic range before committing to the cancellation-aware one, which at the horizon reaches its cap of 64 per axis for a shading closure whose coefficients died long before.
- The sawtooth's phase factor is what makes the transform over X discontinuous (sin(floor(s) + floor(t)) jumps at the cell seams), and a midpoint grid with the half-sample shift applied to its coefficients is accurate enough for it: the seam's contribution to a coefficient at k carries a relative error of order (k/N)^2 on a part that is itself of order 1/k, an absolute 1e-6 at N = 256.

- Four corrections from a review of the snapshot, all accepted. The priced merit mu = eta |ab| is a search heuristic and not a visibility law (the contribution is the coefficient times the multiplier, and the compiler uses only that); the continued-fraction proposition holds in one direction only: every record of mu is a convergent (452 of 452 over 207 ratios) but most convergents are not records (452 of 1246; sqrt 2 has mu(1,1) = 0.3431 and mu(3,2) = 0.3532), and the gate that reported 207 of 207 had tested the plain slowness |h - kx|, which is Lagrange. Two-valued pictures make the filtered nonlinearity affine in the filtered coverage, not commuting with the filter. Only the whole-number slide is a projection; the Gaussian window leaks by its multiplier, the conditional-expectation reading is a limit, and the remainder hypothesis excludes hard profiles unless weighted by the multiplier; the compiler's guarantee is a per-pixel budget (second-order model, recipe cut, quadrature tolerance), not exactness, and a frame at the noise floor of a 1000-sample reference shows only that the reference resolves no more. And the curvature rule above.
- The Gauss-Hermite mixture is not the remedy for the third-order rows. Twenty-five sub-pixels of sigma 0.25, each exact to 3e-4 to 6e-4 on its own (the compiler at sigma 0.25 against a million samples: circles (240,5) 6.4e-4, (300,12) 8.6e-5; colour circles (240,5) 3.2e-4, (300,12) 4.1e-4), mix to 5.1e-3 at the circles' (240,5) and 5.8e-3 at (300,12): the sub-pixel value varies with the sub-pixel's centre on the pattern's own scale, and five Hermite nodes cannot integrate it. Nor is the curvature growth cap of the harmonic ranges the cause: the horizon pixels are unchanged with the cap at 2, 4 or 8. What remains is a third-order pushforward or quadrature along the third derivative's direction. FJET_SPLIT stays in the harness as the record of the experiment; FJET_CURV=0 restores the first-order widths (the ablation); FJET_FOLDGROWTH and FJET_MAXK expose the caps.

- Two more repairs from the review failed and are replaced. The multiplier-weighted remainder is false (E cos(4Z + Z^2/2) is -0.01376 where the affine prediction is e^-8 and the weighted "bound" 1.7e-4: the quadratic phase detunes the Gaussian's suppression), retracted; the compiler is unaffected since it integrates quadratic phases exactly. Mean-freezing fails at hard edges: xi = eps (Z^2 - 1/2), S = 1{xi > 0} has width sqrt(2) eps and mean eps/2, so every freeze returns 1 where P(Z^2 > 1/2) = erfc(1/2) = 0.4795; and the compiler in fact returned 0, because the tracer folded a zero-gradient count to a number before the evaluator saw it. Now the tracer folds only flat counts (gradient and Hessian both zero), edge axes keep a curvature-aware period, and a hard picture of a count that is mostly curvature is integrated as the coverage of the quadratic form (coverageExpect: eigenframe, condition on one coordinate, intervals in the other, cuts at the discriminant's zeros and at the inert-direction jumps, the picture's jumps and kinks located by bisection, adaptive Simpson for the distribution's square-root singularities). Two bugs the rank-two gates caught: a located jump treated as a segment of length 1e-16 gave a chord of slope 1e16 and P(xi > 0.2) = -0.38; a jump detector against the median step, zero when most of the excursion is flat, called every sloped segment a jump and never located a relu's kink (5e-5 on the ridge). fjet-coverage.mjs gates the reviewer's example at three sizes (step, sign, relu) and rotated saddle, bowl and ridge counts against a 2-D quadrature reference. The hard factor is replaced by its expectation: the term's other factors are treated as independent of the edge's region, stated.
- Exact conditioning on the perspective's Y removes the horizon residue (a reviewer's proposal, measured standalone on circles against a 2-D reference converged to 1e-7): (240,5) compiler -1.6e-3, conditioned 6e-8; (300,12) -2.7e-4 and 2e-7; (240,20) -9.9e-5 and 1e-9; (30,5) 3e-8 both. The whole residue is the perspective's Taylor remainder. In (X, v = 1/d) the state map is bilinear and second-order jets are exact; only the measure is non-Gaussian in v. Compiler implementation next: trace in (X, v), closed form in X, one Fourier integral per recipe over v against the explicit density.

- A collaborator's review (paper/reviews/2026-09-05-integral-compiler/) with nine probes, all reproduced. Three fixed. Expression identity was lossy: elementSig rounded a coefficient's centre value to seven digits and dropped its derivatives, so two fields X sin B and 2X sin B on the axis X = 0 shared an axis and their difference traced to zero; identity is now lossless (jetSig: six components exact, real and imaginary, closures recursively, the local path's closure key exact), sixty-one benchmark pixels unchanged, and fire with bumps at (100,120) runs as a side effect (7.8e-5 of a million samples; (300,12) and (400,60) still stop at four residual axes). The coverage integral carries the term's coefficient jet (Gaussian interval moments to order two in the eigenframe): E[Z^2 1{Z^2 >= 1/2}] = 0.9189 where the product of expectations gave 0.4795; other factors of the term still see the constant E[c p], stated. A step folds to a constant only when the count's excursion over the trace reach (6 sigma, set by the harness) cannot reach zero: exact for the model and invariant under positive scaling, where an absolute flatness tolerance had turned 1{1e-14 (Z^2 - 1/2) >= 0} into 0. The cubic step (source 1/2, model 1) is the source-versus-model contract and stays: a validity probe against the source at a few displacements is the proposed selector.
- The analytic model as a control variate loses: at sinQuadratic (300,12) Var(S - q) is 0.042 against Var S 0.048 (1.2x at 2-3x the cost) although E q is within 1.2e-4 of a 500k reference; the centre-expanded fract count is off by 0.11 periods at 0.5 px along y and 2.7 at 1.5 px (the perspective's cubic term), so the model's point values correlate with the source at 0.57 while its mean is right because the misplaced recipes are the fast ones the multiplier kills. Means from the centre model where a validity probe passes; controls from per-sample exact derivatives (the collaborator's Stein construction, whose 7.4x at four harmonics reproduces exactly, and whose 1214x smooth-feature case is an oscillation whose mean the multiplier already gives as zero). The decisive comparison is on fire with bumps and the horizon rows, where the analytic route is absent or biased; at (300,12) the compiler's 3.7 ms mean is two to three orders of magnitude cheaper than either sampled route at equal error.

- The collaborator's follow-up (author-reply/): the Bessel claim holds against the actual shift tables (1.45e-8 on the nodes at |theta| <= 16), with corrections taken: the argument is J_n(theta h) with the compiler's theta, the 64-point torus grid aliases past theta about 32 (2.7e-3 at 40, 0.22 at 64; 256 points restore it), the rippled quadratic's count carries a sin^2 term and a nested sine whose generalised Bessel coefficient they derived, and a Bessel provider must enumerate at the mixed frequency k grad u + n grad u' (a sideband can return a fast carrier to DC: coefficient 0.1427 at k = 4 with a bare multiplier of 5e-35). Their oracle: within polynomial amplitudes times quadratic phases the control projection is exact and pilot-free (21.8x on their example); the normalised-gradient controls are outside that family; the centre model cannot price a source-exact control (0.019 predicted, 0.588 actual). Their coverage primitive gaussian-chirp.mjs (interval moments under a quadratic phase, 38 fixtures to 2.4e-14) is the inner integral of the joint coverage lowering. Instrumented in return (FJET_BUMPSCALE, shift statistics in the probe line): at the published amplitude every firing pixel sits at the sideband window cap of 16 with theta h up to 16.4, and at twice the amplitude theta h reaches 33; and the adapter author-probes/correlated-coverage-adapter.mjs integrates mask times amplitude times cosine jointly to 1e-6 where the factorised value is off by 0.1 to 0.17.

- The analytic shift provider (shiftAnalytic). For a shift function that is a product of one sine or cosine per S axis, the family Q(theta; l) is a Jacobi-Anger series: one axis J_l(theta) (a cosine carrying i^l); two axes, H = T1(a) T2(b) = (e1 U1(a - b) + e2 U2(a + b)) / 2, the coefficient of e^{i(n a + m b)} is u1_p(e1 theta / 2) u2_q(e2 theta / 2) with p = (n - m) / 2, q = (n + m) / 2, products of two Bessel functions at half the argument; the other closures O on the S axes (the shading relu) are sampled once on a 64-point grid and transformed, their spectrum convolving the Bessel coefficients; Q' and Q'' from adjacent orders. Bessel functions by Miller's backward recurrence in double precision. The sideband window is sized from the argument (ceil(theta) + 12 + 3 cbrt(theta)) instead of the fixed 16, and the mixed-frequency enumeration (harmonicsThrough) is unchanged. Gate fjet-bessel.mjs: against the 64-point table where it is sound, 1.5e-8 (Float32 storage) on Q, Q', Q'' for one and two axes, sines and cosines, with and without a smooth O; against a 1024-point table at theta 40 and 64, 5e-9. FJET_SHIFT=table restores the theta grid. Measured on the benchmark: on every ripple pixel (one S axis, O the shading with about 20 spectrum terms) the provider reproduces the table's value to five decimals at the same cost, at the published amplitude and at twice it (Bessel argument 33, where the 64-point table's aliasing is still only 2e-6), and keeps sidebands to order 18 to 21 where the table's window stopped at 16. On the bumps (two S axes) the shading's spectrum has 125 terms at the 1e-5 cut and the per-request convolution (96k to 179k requests a pixel, 4k to 8k distinct thetas) costs 1.4 to 2.4 s against the table's 0.4 to 0.9 s, so two-axis shifts with more than 64 spectrum terms go to the tables, whose torus grid now grows with the argument (64 up to theta about 34, then 128), and the analytic route serves one axis and smooth O. Truncating O's spectrum at 1e-5 relative changes a pixel by 5e-9; at 1e-4, 3e-8. The GPU form of the two-axis case is a per-shader table of O's spectrum (it does not depend on the view) times Bessel products per recipe; the request count is what a real-time path must bound.

What the probes now leave: the rippled quadratic sine's on-axis pixel at 3.1e-3 (1.4e-3 before the parallax theorem's commit; its element and the evaluator agree to the noise, so what remains is between the traced element and the shader, a second-order model of a field somewhere), and the row y = 5 of every shader at 1e-3. Earlier text of this paragraph: the rippled quadratic sine's on-axis pixel at 1.4e-3 (its element and the evaluator agree to the noise, so what remains is between the traced element and the shader, a second-order model of a field somewhere), and the row y = 5 of every shader at 1e-3 (circles 1.5e-3, colour circles 1.0e-3, fire 1.0e-3 against a million samples), where halving sigma cuts it to the noise (6.4e-4 and 3.2e-4 at 3e-4 noise). There the perspective's rates change fourfold across the pixel's reach and the second-order counts are no longer the shader: the third-order phase, the theory's floor, at the one row of the frame where it shows. The remedy is not a cubic integral (there is no closed form) but the pixel as a mixture of narrower Gaussians where the second-order model fails, each with its own expansion, which the oracle of Stage 2 can decide from the count's third derivative against its rate; the desert regime of those rows makes it cheap. The Phi harmonics of the tables are cut at 24 per axis. That keeps the stations of sin(n) over the integers up to the convergent 44/7 of 2 pi (whose recipes pass the multiplier untouched at y = 5) and drops 333/53 and beyond, at the 1e-4 level. It is the one truncation in the compiler that the theory prices rather than removes: a step's two-dimensional transform decays like |m|^{-3/2}, so the tail past M costs of order M^{-1/2} times the number of live pairs.

## Order of work

1. Jets and the expression language; trace their checkerboard.
2. Elements and the pixel functional; reproduce the checkerboard number.
3. Circles, quadratic sine; then the ripples variants through the same code.
4. The remaining shaders; the automatic benchmark table.
5. Tables and the oracle on the CPU; the frontier.
6. WebGPU emitter in the tool; the real-time demo.
7. Time.
8. The paper.
