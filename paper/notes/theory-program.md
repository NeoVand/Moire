# Theory program: what the product needs that the theory does not yet give

Drafted 2026-09-05 late at the author's request, to be planned with the collaborator through the feedback loops we already run. Companion to [product-plan.md](product-plan.md), which holds the product criteria. This file is the ledger: every claim below carries a status, and a claim moves only through the loops in the last section.

Status vocabulary: conjectured (stated, no derivation), derived (a note with the mathematics and its assumptions), prototyped (a CPU implementation against brute force in the harness), gated (the collaborator's numerical and compiler gates pass on an immutable commit), promoted (in the frozen kernel and the compiler).

## Why new mathematics

The theory we have, the count-map pushforward with second-order jets, the multiplier theorem for quadratic phases, exact coverage of half-planes and conics, and recipes with per-term pruning, produces error at the reference's noise floor on every scene tried. It does not produce bounded cost across scales or general material graphs, and the numbers say why: each node lives in two regimes, explicit edges when the pattern is coarser than the pixel and a global series when it is finer, and both blow up at the pixel's own scale, where 100 edge pairs or 700 recipes buy a value that a bounded local description should give with a few dozen terms. Engineering the constants moves that cost by a third; the representation moves it by an order of magnitude or not at all. Generality has the same shape: we have rules for a handful of node types and no algebra that composes two filtered descriptions into a third with an error that is known.

## Track A. A local multi-scale representation of pictures

A1 (derived for affine pullbacks; approximate with a stated remainder otherwise; corrected after the collaborator's review, bridge #75). A Gaussian atom with a linear phase in count space, pulled back through an affine map, is a Gaussian with a linear phase in pixel space, and its expectation under the pixel window is closed form: a complex symmetric 2x2 Gaussian integral (atom-expectation.md). Through a curved map the pullback is not Gaussian: a quadratic count map gives a quadratic phase but a quartic log-envelope (one dimension, u = x^2, atom exp(-u^2/2), pullback exp(-x^4/2)), so the quadratic form in the derivation is an approximation of the log-envelope. It stands only with (a) a positivity witness, the real part of the truncated precision matrix positive definite over the footprint, and (b) a remainder bound for the dropped cubic and quartic terms of the log-envelope over the ball, both computable per pixel from the jet and the atom's width, and the exact alternative where they fail is the one-dimensional quadrature along the direction of the curvature, as the depth conditioning does for phases. Cost per atom in the affine case: one complex exponential and a 2x2 solve.

A2 (conjectured). Each node's picture decomposes into explicit edges (the coverage branch keeps them) plus a sum of atoms that carries the content below the edge scale, with a residual bounded by the decomposition's own tail (a frame bound for the atom system, a Sobolev-class decay for the smooth part). For periodic pictures the atoms are the harmonics windowed to a few periods; for thresholded fields they are the mid-scale content between the edge description and the far-field mean; for lattice noises they are the noise's own kernels, which are atoms already. The decomposition is computed once per material by the compiler, not per pixel.

A3 (conjectured, restated after #75). The earlier statement, atoms meeting a footprint bounded by the footprint's area over the atom's area, is false: the ratio is itself scale dependent, Gaussian atoms have infinite support, and a long thin footprint of fixed area meets arbitrarily many atoms of a fixed width. The claim to test is a hierarchy: atoms at the scale of the footprint's short axis; along the long axis the sum of the atoms met is a periodic or quasi-periodic structure whose Gaussian average has few terms (the mean and low harmonics, the existing spectral machinery), with the residual certified by Track C. The bound must be stated as a function of the tolerance, the footprint's two semi-axes in count space, the picture's complexity (the rank of its phase module and the count of its edge branches) and the tail mechanism. The first test is the noise mask's transition band, where today 450 to 760 evaluations a pixel leave 2.5e-3 to 3.4e-3: the target is 2e-3 at a cost that does not grow with the footprint's anisotropy.

Risk: the hierarchy may not have a clean seam between the atom scale and the periodic collapse, and the edge-plus-atoms split may leave a mid-scale residual that neither side carries. The prototype answers this within a week, after the collaborator's full adversarial response.

## Track B. A composition calculus with certified intervals

B1 (conjectured; corrected after the review). A filtered description is a tuple: a mean, a list of atoms, a list of explicit edges, and a remainder with a stated norm. The review's counterexample kills the mean-interval version: a balanced field of plus and minus one has mean zero, so replacing it by zero has zero mean error, and E[f^2] = 1; a component harmless to one filtered mean is the whole answer after a product. So the tuple must keep its atoms and edges in the same latent pixel coordinates (the joint measure), and its remainder must say whether it bounds the pointwise error, the L2 norm under the joint pixel measure, or only the final integral. The product contract that is valid is Cauchy-Schwarz on the residuals: with f = f~ + r_f and g = g~ + r_g, |E[fg] - E[f~ g~]| <= |r_f|_2 |g~|_2 + |r_g|_2 |f~|_2 + |r_f|_2 |r_g|_2. Rules are needed for sum, scalar function of a smooth field (the jets, which exist), product (atom times atom is a Gaussian times a Gaussian, closed form; edge times edge is the joint of two half-planes, the bivariate normal for two and general intersections beyond; edge times atom an integral of a Gaussian over a half-plane, closed form through the error function; and the cross terms the review names, which a representation change may not discard), threshold of a description (a uniform field error delta leaves the ambiguity P(|f~ - t| <= delta) plus the outside allowance, order one on a plateau however small delta is: only the shifted enclosure addresses it; and the zero set of an atom sum can have arbitrarily many components, so locating roots on lines needs a work cap, not a count bound), select and min and max (thresholds of differences), lookups of sampled data (the mipmap is the pushforward of the window through the texture; its error is the texture's own), warps (jet composition, which exists, and which changes the measure under which the remainder's norm must hold), and seams in the mapping (the window split by the seam as an explicit edge, each piece with its own jet).

B2 (prototyped for the mask's near regime, 2026-09-05 late). The interval: the coverage enclosure the collaborator proposed (two shifted conics around the model's error over a ball, plus the mass outside the ball, plus the integrator's own error) for thresholds; the quadrature's error for lines; the tail bound of Track C for series and atoms. The interval is what decides the representation per pixel and what raises the flag. First numbers (demo modes 11 and 12, twenty near-field probes): every interval contains the truth, and the widths are 0.011 to 0.18 where the true error is 1e-4, because the sup of the Taylor remainder over a 3-sigma ball and the 1.1 percent of the window outside it dominate; at 8-bit the conic regime is certified nowhere with this bound. The tighter rigorous form to derive next: the two indicators can differ only where |q - t| <= |F - q|, so the coverage error is bounded by the window's mass of the band {|q(X) - t| <= eps(|X|)} with eps growing as |X|^3 from the centre, which is small where the density is large; computable as a line integral along the conic or by shells, and it removes the ball's outside mass from the bound.

B3 (conjectured). Lowering: for each pixel the cheapest representation whose interval meets the budget, in a fixed order, with the unresolved interval and the flag carried out when none fits. This is the compiler's inner loop and the definition of "automatic lowering from a node set".

## Track C. Certified truncation at 8-bit precision

C1 (open; corrected after the review). The earlier statement, that the indicator's coefficient mass beyond order M is bounded by the boundary's length over M, is false as an absolute statement: the periodic half-interval has |c_n| = 1 / (pi |n|) at odd n, an absolute tail that diverges with finite perimeter, and a picture independent of the second coordinate carries that into two dimensions. A perimeter-over-M estimate bounds the squared L2 tail energy under finite-perimeter assumptions, whose norm error is of order the square root of P / M, not P / M. The quantity to certify is neither: it is the omitted part of the actual pushforward-weighted integral, the sum over the dropped recipes of c_k times the window's pushforward at -k, with the map's assumptions stated: a full-rank affine Gaussian map damps every direction, a rank-deficient map leaves resonant directions undamped, a nonlinear map adds stationary-phase and degeneracy cases, and a picture-space L2 bound does not control integration against a singular pushforward. The pruning in the kernels bounds each dropped term; the total allowance is the sum, and it is what the interval must carry. Smoothness-class decay and frame bounds for atoms likewise need explicit constants and a summed tail, not a class name.

C2 (prototyped as constants, not as a dial). The precision dial: the map from an error budget to the cuts, the reaches, the node counts and the handovers, validated per node by the harness. The first measurement on the flat scenes shows the constants move the cost by a third and the structure by 4x.

## Track D. Shading with contracts

D1 (promoted for one case). Lighting as a function of a phase coupled to the picture: the rippled checkerboard, where the lighting's spectrum over the phase enters the recipes. Generalisation: any lighting that is a function of the same structure as the picture composes through the same recipes.

D2 (conjectured, bounded not exact). Specular on structured normals: normalised normals are not Gaussian and a clamped BRDF has no closed-form expectation, so the deliverable is a closure with a stated BRDF, stated random variables, a validity witness and an error interval, preserving energy and the correlation with the picture. For procedural bumps the slope field's jet is known and the closure is a Gaussian moment; for sampled normal maps the engines' moment filters are the baseline and our contribution is the coupling with the picture.

## Track E. Mappings and phases beyond second order

E1 (prototyped as quadrature; corrected after the review). Depth conditioning: a rational-linear phase is linear along lines perpendicular to the depth gradient, so the expectation is a one-dimensional quadrature with the exact phase; in the kernel since 20bc272 with a cubic witness. The review's correction: the transverse integration leaves the exact integrand phi(y) exp(i (b y + c) / (d + e y) - a^2 S / (2 (d + e y)^2)) under the depth Gaussian, a rational integrand and not a Gaussian times a cubic phase, so an Airy formula addresses a cubic surrogate and cannot replace it; a third-order jet does not determine a map (x and x + K x^4 agree through third order and filter differently), a rational Taylor model needs a remainder tied to the pole's distance and none exists once the ball crosses the pole, and a generic two-dimensional cubic phase has mixed terms with no single curvature direction: rank-one or separable cubic structure is the restricted target if a closed form is ever wanted. Probe 6 (paper/tools/exp/theory-probes/probes-1-2-6.mjs) measures it: with the pole at 40, 12, 6 and 3 sigma the quadratic model errs by 1.8e-4, 6.7e-3, 2.0e-2 and 6.7e-2, the cubic surrogate by 1.1e-5, 2.5e-4, 3.1e-3 and 2.4e-2, and the kernel's 24-node depth quadrature by 4.5e-5, 7.3e-5, 4.1e-5 and 1.2e-4; at 1.5 sigma the ball meets the pole, polynomial bounds are refused and the source clips at D <= 0. Exact rational conditioning stays until a replacement proves both its scope and its cost advantage; the open items are its quadrature error bound and its cost where many recipes take it.

E2 (conjectured). Piecewise-smooth mappings (seams, triplanar blends, parallax): the seam is an explicit edge in the window and each side carries its own jet; the coverage branch already integrates edges, so the rule is a composition rule of Track B, not a new integral.

E3 (out of the product's scope, noted). Geometric edges belong to the engine's AA; the coverage branch could take a silhouette as an edge if an engine handed it over.

## Track F. Time

F1 (conjectured). Animated phases and moving cameras: the analytic value is deterministic, so temporal stability under the engine's TAA is a property to verify, not to build; the combined filtered-plus-TSR arm on the native side is the test. Motion blur as a space-time window is a possible extension of the same pushforward, not planned.

## The organizing principle, from the review

Bound the work needed to approximate the joint pixel integral instead of assuming uniformly sparse pictures. For a partition of the window into regions C, E[f] = sum_C P(C) E[f | C], conditional intervals combine with the same weights, and products need E[f g | C] = E[f | C] E[g | C] + Cov(f, g | C): the covariance is what a change of representation may not discard. Edges, atoms, spectral terms and exact depth conditioning are alternative evaluators of these conditional contributions; aggregation handles fine regions collectively, refinement targets unresolved boundaries and correlations. This is an organizing contract and not a complexity theorem: conditional means may themselves be expensive, and a theorem must bound the unresolved contributions for a stated function and mapping class, tolerance, preprocessing and storage. The work cap is separate from the error budget; when the error stays unresolved the interval is carried and the declared fallback used. The same contract gives the only temporal guarantee we have: two representations within epsilon of the same instantaneous integral disagree by at most 2 epsilon at a handover; determinism alone guarantees nothing under the engine's temporal filter.

Three evidence axes accompany every status (from the protocol companion): the mathematical contract (domain, norm, joint measure, assumptions, bound); the implementation evidence (source identity, floating-point behaviour, compiler acceptance, bounded execution); the product evidence (native pixels, temporal behaviour, measured cost). An implementation may pass finite probes while its bound stays conjectural; promotion is an engineering decision within a stated supported domain, never a theorem; a jittered reference's apparent noise floor is not an accuracy certificate.

## Feedback loops, as they exist

1. Claims are written here with their status; the collaborator reviews each adversarially on the bridge (variables, witnesses, contracts, what the claim does not cover) before any prototype, as they have done for the depth measure, the counter contract, the enclosure and the specular caution.
2. The CPU harness (`demo/tests/ripples-cpu.mjs`, the shader text itself run in a WGSL interpreter against a jittered brute force, with textual variants for bisecting) is the first numerical gate for every kernel change.
3. The demo's GPU profile by row band and its isolated benches are the second, and the arbiter for compile errors.
4. The collaborator's immutable-candidate gates (DXC compile of the emitted HLSL, float fixtures including adversarial families, the 120-probe comparison, the watchdog controls) on a named commit are the third; promotion into the frozen kernel needs them.
5. Native captures in Unreal, still and moving, next to the engine's AA at matched settings, with the combined filtered-plus-TSR arm, are the product-level test.
6. Formal write-ups go through the review folders (`paper/reviews/`), with probes the reviewer runs.
7. The product criteria in product-plan.md are the acceptance test for the whole.

8. A derivation ships with its adversarial fixtures (the protocol companion): a small versioned JSON contract with the claim and source revision, the canonical expression and parameters, the coordinate map and joint window, the threshold and visibility domain, the requested norm, budget and work cap, the expected witness or decline, the reference method with its convergence evidence, and the exact candidate export and entry; recorded independently: measured value, claimed interval, actual work, status, reference disagreement; failures and unknowns preserved. Pairing rules: every representation handover gets fixtures on both sides and on the boundary; every warp gets off-axis mixed-term probes and pole-distance and anisotropy sweeps; every product gets correlated, anticorrelated and near-resonant controls; every threshold gets a plateau and a saddle; every temporal test crosses handovers, changes parameters and cuts.
9. The error is allocated across the complete expression including the summed omitted tail, and the 8-bit target names its transfer function and range: a uniform linear error is not a uniform code-value error near black; for a monotone transfer, propagate the endpoint interval through it.

Cadence: a claim moves one status per loop; nothing skips a loop. The ledger is updated in the commit that moves a claim. The first six probes are the review's (paper/reviews/2026-09-05-integral-compiler/theory-program-review/REVIEW.md); 1, 2 and 6 ran on 2026-09-05 (paper/tools/exp/theory-probes/probes-1-2-6.mjs), 3, 4 and 5 are open.

## Ownership, accepted (protocol companion)

Claude: derivations and their notes, the compiler's node semantics, the kernels, the CPU prototypes and the harness, this ledger. The collaborator: the native integration and plugin, the independent reference and candidate gates, the comparison fixtures, the native cost protocol and the evidence folders. Either side proposes hypotheses; a numerical pass never turns an unsupported mathematical claim into a theorem; the hierarchy is a research direction, not a demonstrated novelty.

## The benchmark families and the cost host (protocol companion)

Held-out material families, frozen in expression and parameter distribution before any representation or cutoff is fitted: brick with narrow grout and a separate paint mask; noncommensurate periodic layers multiplied before filtering; thresholded cell noise; seams and triplanar blends; procedural bump slopes under a specified lighting model; sampled normal maps under a stated BRDF. Geometry and silhouettes stay with the engine. A texture baseline must represent the same field (a periodic tile is not a baseline for an aperiodic source); every approximation reports its own error; Gaussian reference scores are filter-specific diagnostics, not a ranking of AA quality against TSR's own filter. The named cost host is the Apple M4 with Metal and Unreal 5.8.2 at an actual 1920x1080, each method run separately (raw, TSR, analytic, analytic plus TSR), both the incremental shading cost and the completed whole-frame cost including rejected representation trials, enumeration, fallbacks and table construction and upload, with cache warmup, table memory and parameter invalidation stated, bracketed by raw controls, unstable batches rejected; no frame-rate claim follows from capture runs.

## Order

B2's honest threshold enclosure first (done as stated, sound and loose; the band-mass form next); the affine atom positive control and the curved-envelope counterexample (done, probes 1 and 2); then a hierarchy experiment with explicit unresolved-residual accounting on the mask's transition band, which is what A3 became; B1's product contract with the correlated, anticorrelated and near-resonant controls (probe 4) and the plateau (probe 5); C1's weighted tail; the measured bottleneck is optimised only after these contracts separate model error, integration error and missing correlation; D2 last, with its contract before any code.
