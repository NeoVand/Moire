# Beyond count maps: the latent pushforward, the moire spectrum, and the self-dual pairing

A research note, 2026-09-06, written after the author asked what the new theory is and warned against testing before thinking. This is a brainstorm with definitions and candidate theorems, not a ledger. Nothing here is proved; the point is to say what the theory should be, why it would be deeper than count maps, and which single theorem would make it consequential. Written to think with the collaborator, whose question it answers: what is the invariant object behind count maps.

## 0. Current formulation, read this first (2026-09-06, after bridge #98 to #105)

The sections below are a log, and some of them state claims that were later withdrawn. The withdrawn passages are marked in place with the word Withdrawn and a pointer; this section is what I currently hold.

- The object of section 1 stands and is Heitz et al. 2014's pairing. What is ours is narrower: the exact characteristic function of the footprint's distribution for quadratic and rational-linear maps, the exact coverage of coherent discontinuities, and the analysis of which harmonics survive (section 8).
- A latent state is admissible when it is closed under the diffusion the screen Laplacian induces (section 9, the collaborator's). Three closures are distinct: geometric, finite observable, and composition. The last is impossible for a finite state on a connected domain (the collaborator's algebra argument in CLOSURE-AND-SCALE.md), so the target is an infinite algebra closed under the node set and under filtering, with a certified truncation at the output.
- The count-map calculus is such an algebra: complex Gaussians on the screen, closed under products, with a closed-form filtered response (the multiplier theorem), in which the filtered product series of FILTERED-COMPOSITION.md is summed exactly (section 12). An identification of that response with a resummed Mori-Zwanzig memory of rank three was sent and withdrawn (#114): the first-order term is the drift, and the eliminated space is not finite.
- The certified core is the wrapped-Gaussian density lemma (section 10 and bridge #98). For the T2 class the far-field certificate depends on Q alone, the material side is a per-node coefficient table, and the two-sided enumeration count is at most R sqrt(c) / 2 + 2 sqrt(pi R sqrt c) kappa^{1/4} + 2 pi with c = c(eps, kappa), independent of the overall scale of Q and linear in L = kappa^{1/4} (section 12; my counts, the collaborator's example #113 and their repair #118). Proved on both sides; the material work is a separate term. The mixed expansion is the third branch of the theorem, consolidated with the other two, its certificates and its material work in paper/notes/t2-count-theorem.md, which the collaborator audited as a whole on 2026-09-06 as a conditional result (bridge #157; #134 to #157): at balanced scale the smaller of the spectral and mixed branches is O(1 + kappa^{1/8} sqrt E) with table coefficients, O(E) when the footprint is aligned with a lattice direction, and the purely spatial branch is needed as well.
- The shared-carrier family (one carrier, slow shifts, single-arc masks, any bounded graph) has a complete conditional theorem on the cell side (the collaborator's SHARED-PHASE-COST.md: arrangement evaluation, exact Gaussian integration between endpoint collisions, an event budget, the corrector certificate) and, on the spectral side, the ladder identity with assumptions (a) and (b), the collaborator's O(1) certificate #123 and their dyadic-block proof of (b) for every finite graph of masks with nonzero rates (#125). The joint statement is in paper/notes/shared-phase-family.md; its section 8 proposes the next derivation, the coefficient envelope along the material's ladder from the stationary-phase geometry of the kernel direction on the boundary surface, which is what would make the noise mask's far-field certificate a theorem.
- The incommensurate class has an effective law, the sum over the material's near-resonance ladder, after its exact phase relations are composed first: a material of one phase q . theta with K q = 0 is constant on the leaf however its absolute spectral sum behaves (the collaborator's #131), so the exact resonance module is quotiented before the ladder, with four qualifications (the collaborator's #145): the module read off the graph's periodic leaves is a conservative enclosure of the module of the nonzero Fourier support, since sums and products can cancel; the exact operation on the constant characters is substitution of their values into the source material, not an average over the rest, and the restricted material can keep the original difficulty; exact relations need exact rates (rational rates admit integer-kernel algorithms, every stored float is rational, so a float rate matrix of rank two always has an integer kernel with enormous coefficients, and 'no exact relation' is a statement about the ideal model, the computable object being resonance up to a coefficient size); and a zero screen rate certifies a constant phase only for an affine map or a proved source identity. Its spectral certificate is a conditional geometric theorem in progress (section 8 of paper/notes/shared-phase-family.md: a coefficient envelope along the ladder from uniform stationary-phase charts on the boundary, with the collaborator's lattice tail). A slow absolute tail measures the cost of coefficientwise certification in this representation, not the material's difficulty (section 12).
- Withdrawn: the slow set as a module (section 8), the Diophantine log count and the dimension limit (section 3), the coefficient law for arbitrary discontinuities (section 3), the sampler lower bound and its 1 / epsilon^2 separation (section 5), and the multiplier as a frozen generator (section 9). Section 10 records why.

## 1. The object

A material is a function g on a latent space L, and a shader is g composed with a map Phi from the pixel plane to L. The pixel's value under a window measure mu is

    <g, nu>,   nu = Phi_* mu,

the pairing of the material's latent function with the pushforward of the window onto the latent space. That pairing is the invariant object. Neither the map alone nor the function alone is enough; the pushforward carries the map's whole influence, and the pairing carries the function's.

What L is, for the materials people build:

- a torus T^d of phases, for anything periodic or quasi-periodic: tilings (d = 2), stripes (d = 1), the rippled checkerboard (T^2 x T^1 = T^3), a mask of three sines (T^3), lattice noises (T^2 per octave family, since the octaves of a Perlin or value noise share one lattice and are harmonics of one T^2), tileable cell noise (T^2);
- the slope plane R^2 (or the sphere of normals), for shading: the normal field is a map into slopes, the BRDF with its light and view vectors is a function on the slope plane, and the clamp is a threshold there;
- a data domain, for sampled inputs: a texture is a function on its own square, which is a T^2 under wrapping;
- products of these, for materials that combine them: the product latent space, with the joint map.

Count maps are this object in the toroidal case with a particular chart: a count is a phase coordinate divided by 2 pi, the count map is the map into the torus, the "picture" is g. So count maps are not wrong, they are the chart of the structured case. What they capture, and what they miss, is the subject of the next sections.

## 2. What count maps capture: the exactly solvable stationary phase

The pushforward nu is determined, to second order, by the jet of Phi: the map's value, gradient and Hessian at the pixel's centre. Its characteristic function on the torus,

    nu^(m) = E[ exp(i m . theta(X)) ],   m in Z^d,

is the multiplier of the count-map theory, closed form for a quadratic phase under a Gaussian window (the multiplier theorem), and the pairing is the series sum_m g^(m) nu^(m) over the material's Fourier coefficients. This is an oscillatory integral, and its magnitude,

    |nu^(m)| = exp(-(1/2) S |b_m|^2 / (1 + S^2 lambda_m^2)) (1 + S^2 lambda_m^2)^(-1/4),

with b_m the harmonic's rate and lambda_m its curvature, is exactly stationary phase for a quadratic phase: a harmonic whose phase has a stationary point inside the window (rate cancelled by curvature within a few sigma) survives with the 1 / sqrt(S lambda) prefactor of a nondegenerate critical point, and one without decays like a Gaussian in its rate. The depth conditioning of the kernel is the exact treatment of the one non-polynomial phase that perspective produces, rational-linear, which the collaborator showed is not a cubic surrogate; the cubic and higher polynomial phases are the fold and cusp caustics of catastrophe theory, with Airy and Pearcey functions where the count-map theory has the Gaussian.

So what count maps fundamentally capture is: the characteristic function of the pushforward for polynomial phases of degree two, exactly, and for the perspective phase, exactly along the depth line. What they do not capture is which harmonics matter and how many, which is the next section; the latents that are not tori; and the certification of what is dropped.

## 3. The moire spectrum: aliasing as near-resonance

Write the rates of the d phases at the pixel as the 2 x d matrix G (rows: the pixel's x and y). The harmonic m has the rate G^T m in the pixel plane, and its multiplier decays with |G^T m| sigma. The harmonics that survive the window are those with

    |G^T m| <= kappa,   kappa of order 1 / sigma,

the integer points of Z^d in a thin cylinder around the kernel of G^T, a subspace of dimension d - 2. Call the set of surviving m with |g^(m) nu^(m)| above the tolerance the moire spectrum of the pixel. Everything the pixel shows that a point sample would alias is in that set: the rippled checkerboard's moire on row 8 was the combination 3 k1 - k2 + k3 with a net rate of two radians a pixel, a near-resonance of three rates; the checkerboard's own harmonics near the horizon are the near-resonances of d = 2; the mask's far field was the sum over the cylinder for d = 3.

Withdrawn (section 10): the count and the coefficient law in this paragraph and the two bullets after it. What survives is the ladder of section 12. The paragraph as first written: this makes aliasing a statement in the geometry of numbers. The number of lattice points in the cylinder up to |m| = M grows like M^(d - 2) for generic rates, and the coefficients of a material with a discontinuity (a threshold, a tile edge) decay on average like |m|^(-(d + 1) / 2). The absolute sum over the moire spectrum converges only when (d + 1) / 2 > d - 2, that is d < 5, and the sum of the terms themselves converges absolutely for d <= 3 with a margin, for d = 4 on the edge. Two consequences worth their own proofs:

- Withdrawn. A dimension limit for certifiable exactness. A discontinuous material with five or more independent frequencies has a moire spectrum whose tail cannot be bounded absolutely; its pixel integral may still converge conditionally, but no truncation can be certified. This is not a limitation of the method, it is a property of such materials: they alias densely.
- Withdrawn. A Diophantine design rule. For rate vectors that are badly approximable (their combinations stay away from resonance as well as integers allow), the cylinder holds only about log M lattice points up to M instead of M^(d - 2), and the moire spectrum is sparse for any d. A procedural material whose frequencies are chosen with that property (ratios related to quadratic irrationals, the golden ratio being the classical case) has a small moire spectrum everywhere in the image, and the compiler can verify or choose this at compile time. The filterability of a material is a Diophantine property of its frequencies.

The count-map kernels enumerate the cylinder as a lattice under a reach, which is right for d = 2 and which the mask node extended to d = 3 as a shifted lattice; the moire spectrum is the object those enumerations approximate, and its size is the honest cost of a pixel in the far and transition fields.

## 4. The self-dual pairing: why the crossover is cheap

For a periodic material the pairing has two expansions. Summing the window over the cells of the torus gives

    <g, nu> = int_cell g(u) theta_nu(u) du,   theta_nu(u) = sum_n nu(u + n),

the material on one cell against the periodised window, a theta function. Its two representations, the sum over cells (the coverage branch: the cells the window reaches, each an exact integral of g against a Gaussian piece, the erf products and conic integrals of the kernel) and the sum over the dual lattice (the spectral branch: the harmonics of g against the multipliers), are the two sides of Poisson summation on the torus. This is what the two regimes of every kernel are, and it says why the crossover between them is cheap when it is handled right: a Gaussian is its own Fourier transform, and at the self-dual scale (window width about a quarter to a half of a period) both series of the theta function converge in a handful of terms. The slow decay of a discontinuous material's coefficients never enters, because on the spectral side it is multiplied by the Gaussian decay of the multipliers, and on the cell side the cells are integrated exactly.

The cost claim this supports, to be proved: for a material on T^d whose cell integrals against a Gaussian piece are closed form (polygonal and conic pictures) and whose harmonic coefficients are known, the pixel value to tolerance epsilon is a sum of at most N(epsilon, d, the moire spectrum) terms, independent of the scale of the pattern relative to the pixel, with the cell expansion below the self-dual scale and the harmonic expansion above it. Below the self-dual scale the cells the window reaches are few; above it the multipliers kill all but the moire spectrum. The number is bounded across the whole image because the Gaussian's self-duality bounds the crossover and the moire spectrum bounds the far field. The kernels' measured mid-band costs (78 edge pairs before the four-edge handover, 18 after; 450 to 760 in the mask's transition band) are what happens when the handover is not at the self-dual point and the cut is at 1e-6 instead of the tolerance: this section says where the handover belongs and what the count should be.

Anisotropy is where this needs care and where the collaborator's objection to atom counting applies: a perspective footprint is long along depth and short across it, so the self-dual scale differs by direction; the theta function on T^d with an anisotropic Gaussian has its own self-dual ellipse, and the expansion should be chosen direction by direction (cells across, harmonics along, or the reverse), which is a mixed expansion the kernels do not have yet and which the depth conditioning approaches from one side.

## 5. The consequential theorem: the intrinsic complexity of a pixel

Status: claim 2 below is withdrawn (section 10); it is the IID variance, not a lower bound. Claim 1 survives only as the ladder count of section 12, and the separation stated at the end is an observation about IID and temporal samplers on these scenes, not a theorem.

The theorem that would make this theory matter to the industry is not about our method; it is about every method. Define the moire spectrum M_epsilon of a pixel as above. Two claims:

1. The analytic value to tolerance epsilon costs about |M_epsilon| multiplier evaluations plus a bounded number of cell terms: work proportional to the size of the moire spectrum, with a logarithm of 1 / epsilon.
2. Any unbiased sampling estimator of the same pixel with N samples has variance at least the aliasing variance, sum over M_epsilon of |g^(m)|^2 (1 - |nu^(m)|^2) up to constants, and so needs N of order that variance over epsilon^2 samples to reach epsilon: the 1 / sqrt(N) law.

The separation is the factor 1 / epsilon^2: at 8-bit precision, about 2.5e5. This is the quantitative reason the measured table looks the way it does: supersampling at 16 samples and temporal AA at an effective 8 to 16 sit at 0.03 to 0.12 RMS on these scenes because the aliasing variance of a tiled picture is about 0.25 and sqrt(0.25 / 16) is 0.12, and the analytic value sits at the reference's floor because it pays for the terms, not for the variance. Stated and proved with the moire spectrum as the intrinsic quantity, it gives the product its claim: a computable, per-material, per-pixel quantity that says how much any anti-aliasing method must work and shows that analytic prefiltering is the one that works only that much.

## 6. Beyond the torus

Shading. The normal field is a map into the slope plane, its jet is known for procedural bumps, and the pushforward of the window onto the slope plane is a chirped Gaussian, the same object as on the torus. The BRDF for fixed light and view is a function on the slope plane: a Blinn-Phong lobe is nearly a Gaussian there, a GGX lobe is a sum of a few, the clamp n . l > 0 is a half-plane, the normalisation of the normal is inside the function and needs no Gaussian assumption. So the filtered specular is the pairing of a lobe with the slope pushforward, closed form for Gaussian lobes, with the clamp as a coverage term; and the coupling with the picture, the highlight riding on the tiles, is the joint pushforward onto T^d x R^2, the same as the ripple's coupling in one parameter. The collaborator's caution stands: this is a stated closure with a lobe approximation of the BRDF and an error from that approximation, not an exact BRDF integral; but it is the right object, and it says what to approximate.

Sampled inputs. In the affine case the pairing of a texture with the pushforward is exactly the elliptical weighted average, so the count-map theory contains EWA texture filtering as its data case. In the curved case the pushforward is not an ellipse but a bent footprint, and the exact filter for a texture near the horizon is the bent one. Computable from the texture's spectrum with the multipliers, or from a pyramid with a few lobes along the bend: anisotropic filtering with second-order footprints, a concrete extension the industry does not have.

Composition. A product of two quasi-periodic structures is a function on the product torus with the stacked rate matrix, and its multipliers are the same formula. Correlation between the factors is not something to carry: it is automatic, because both sets of phases are functions of the same X and the pushforward is joint by construction. This is the torus form of the collaborator's conditional-expectation hierarchy, and it moves the whole difficulty of composition into section 3: the moire spectrum of the product is the near-resonances of the stacked rates, which grow with the total dimension. Sums are sums of coefficients; thresholds of products are indicators on the product torus with coefficients from a DFT of the stacked phases, which is what the mask node did for d = 3 and which the dimension limit bounds.

Visibility. Parallax occlusion, displacement with self-occlusion, and silhouettes are maps that are not single valued: the latent point seen along a pixel's ray depends on a height intersection. The pushforward of the window through such a map is the pushforward through the visible branch, and the theory as stated does not have it; this is the honest boundary of the framework, and it stays with the engine's geometry AA for now.

Windows. The self-duality of section 4 is the Gaussian's. A box or tent pixel filter is a mixture of Gaussians to any tolerance, at the cost of the mixture's size, and the engine's temporal filter is a window in space-time whose pushforward through a moving map is the same object one dimension up: temporal stability is then a statement about the space-time moire spectrum, not about determinism.

## 7. What this predicts, and what would show it wrong

- The mask's transition-band cost should equal the size of its moire spectrum at the tolerance, computed from its rates alone, not the 450 to 760 evaluations of the current line and series paths. If a count of near-resonances from the rate matrix does not predict the measured cost, section 3 is wrong.
- The ripple's moire terms are the near-resonances of the three rates (gu, gv, grad psi); an enumeration of that cylinder should reproduce the terms the shifted lattice found and no others.
- Replacing the mask's three wavelengths (23, 17, 11) by three in badly approximable ratios should visibly shrink the far-field term counts at equal error. If it does not, the Diophantine rule is wrong or the rates are not the right quantity.
- The measured errors of supersampling and temporal AA on every scene should follow the square root of the aliasing variance over the effective sample count, with the variance computed from the moire spectrum; a scene where they do not would break section 5.
- The four-edge handover and the reach of the kernels should be derivable from the self-dual scale and the tolerance; if the derived handover costs more than the tuned one, section 4 is missing something, most likely anisotropy.

## 8. Credit, and the reformulation the collaborator's reading forced (2026-09-06, bridge #89 and #91)

The pairing of section 1 is not new. Heitz, Nowrouzezahrai, Poulin and Neyret (TVCG 2014, section 4.1) write shader filtering as the inner product of the shading function with the footprint's distribution of inputs, <C0, D_f>, and expand both in finite bases to get sums of pairings of basis elements; Yang and Barnes (2018) propagate pairwise correlations between inputs through the shader with several approximations; elliptical weighted averaging (Heckbert 1989) is the affine data case; LEAN (Olano and Baker 2010) and Toksvig's filter are moment closures for normals; classical homogenization (Bensoussan, Lions and Papanicolaou) and its quasi-periodic extension (Kozlov) integrate out fast periodic variables to produce effective laws; and the near-resonance problem of section 3 is the small-divisor problem of the geometry of numbers and of KAM theory. What the count-map work adds is narrower than the note's earlier sections read: the exact characteristic function of the footprint's distribution for quadratic and rational-linear maps where the prior work uses Gaussian or basis approximations, the exact coverage of coherent discontinuities, and the resonance structure of which harmonics survive, which the prior work does not analyse.

The collaborator's frame, conditional homogenization, is the better statement of sections 3 to 6 and I adopt it. The macrostate at a pixel is the set of phase combinations that stay coherent across the footprint, the kappa-slow set Lambda_kappa(G) = { m in Z^d : |G^T m| <= kappa } of the output observable's rate matrix (called a module when first written; withdrawn in section 10, it is a set, and only an exact resonance subgroup gives a quotient); the effective law is the projection of the observable onto that module, a material on the quotient torus; the fast module is integrated out, which is what the multipliers' Gaussian decay does. Two gratings that are each fast but close have the slow combination m = (1, -1); the slow module of a product is computed from the stacked rates of the product, which contains cross-resonances that neither factor has, so averaging the inputs before the interaction destroys the effective law, and the rate matrix must propagate through the material graph exactly (rates add under products, unite under sums, multiply by integers under the harmonics of a smooth function of a phase: linear algebra on Z-modules) with the quotient taken at the output, demand-driven.

The central assumption, stated once: the coefficient mass of the output observable on its slow module is finite and concentrated at low order, sum over m in Lambda_kappa(G) of |g^(m)| finite with a computable tail. It is a joint condition on the material (the decay of g^) and the map (the Diophantine type of G): it holds for discontinuous observables generically when d <= 3 and for any d under a Diophantine condition on the rates, and it fails for materials with a continuous spectrum, whose slow module is a continuum and whose only compact effective law is statistical. That gives the dichotomy the theory should be built on: coherent structure (phases, a slow module, an exact effective law) and incoherent structure (moments, a closure with statistical error), joined by conditioning, the moments of the incoherent part conditional on the slow phases of the coherent part. A bump-mapped tile has both.

The first theorem to derive, in the affine quasi-periodic case: the pixel value equals the pairing of the projected observable with the window's pushforward on the quotient, with the error bounded by the sum over m outside the slow module of |g^(m)| exp(-sigma^2 (|G^T m|^2 - kappa^2) / 2), under a stated resonance module, coefficient decay and non-resonance assumption, with the size of the slow module as the work. Then curved maps, where the module bends and the near-resonances that sweep through resonance inside the footprint are the stationary-phase terms of section 2; then disorder, as moments conditional on the slow phases; then shading, as the slope-plane observable of section 6. Section 5's intrinsic-complexity statement becomes a corollary about the size of the slow module against the aliasing variance.

## 9. The induced diffusion (the collaborator's, bridge #92), which names the object

Gaussian filtering in screen space is heat evolution, and the chain rule pushes the screen Laplacian onto any latent coordinates T_i(x): L[H(T)] = (1/2) sum_ij (grad T_i . grad T_j) H_ij(T) + (1/2) sum_i (Delta T_i) H_i(T). When the metric grad T_i . grad T_j and the drift Delta T_i are functions of T alone, the latent description is closed under the flow, the pixel value is the latent semigroup exp(sigma^2 L) applied to the material function and evaluated at the pixel's latent point, and nothing about the screen needs to be remembered. Affine counts are the constant-coefficient case, the torus heat operator whose spectrum on the characters is the multipliers. The radius has a Bessel drift and closes; the polar angle alone does not, because its diffusion rate depends on the radius, which must then be adjoined. The ripple's state (u, v, psi) closes, which is why its recipes worked, and the Jacobi-Anger sidebands the kernel derived are the spectral signature of that radial closure. So admissibility of a latent state is mechanical: compute the metric and the drift, adjoin what they depend on, repeat until closed; the compiler can do this. Withdrawn (section 10): the sentence first written here, that the second-order multiplier theorem is the semigroup with the generator's coefficients frozen; section 12 states what it is instead, the exact scale response of the complex-Gaussian algebra. The depth conditioning is the exact integral along the rational direction. The research target for the non-affine closed states is the semigroup's action on their natural eigenfunctions.

The same view says why the count dimension is a closure problem rather than an enumeration problem: for d > 2 the generator's spectrum |G^T m|^2 accumulates at zero along the kernel of G^T, the semigroup is not compact on L2 of the torus, and no finite retained set of harmonics follows from the window alone; the compactness assumption of section 8 is the observable's finite demand on the slow spectrum, the quotient by the fast subgroup is the projection onto the slow module, and the Diophantine condition is quantitative nonresonance. The theorem to derive, in these terms: for a closed latent state with generator L and an observable with finite demand on L's slow spectrum, the pixel value is (exp(sigma^2 L) g)(T0) restricted to the slow modes, with the error from the fast spectrum's decay and the work the number of slow modes demanded; affine first, then the closed non-affine states, then disorder as moments conditional on the slow modes, then the slope plane.

## 10. Where the exchange landed (bridge #94 to #96)

Four claims of the earlier sections are withdrawn. The kappa-slow set is a set, not a Z-module, and only an exact resonance subgroup gives a quotient torus; the "log M under a Diophantine condition" count was wrong, since a fixed-width cylinder's count is a volume count in its free dimensions and a Diophantine condition bounds the smallest divisor, not the count; the coefficient law |m|^(-(d + 1) / 2) needs smooth curved boundaries; and the sampler separation of section 5 is the IID variance, not a lower bound, so it and the dimension cutoff are set aside as theorems and kept only as the observation that IID and temporal samplers on these scenes sit where sqrt(variance / N) puts them.

What replaces them is the collaborator's wrapped-Gaussian theorem: conditional on a retained state W, if the fast phase coordinates are exactly psi(W) + B U with B integer and U Gaussian with covariance Q(W) > 0, then for any bounded composed material H, replacing H by its Haar average over the fast orbit changes the pixel value by at most E min(1, (1/2) sqrt(sum over n != 0 of exp(-n^T Q(W) n))), with no assumption on the material's spectrum: thresholds, products and lookups included. Its reading for a rank-2 footprint gives the first theorem to write, for the product's own class. A material whose phases are integer combinations of two base phases (every tiling, brick, hex and harmonic pattern, thresholded periodic noise, a texture on its wrap) lives on T^2, commensurability is a property of the material and not of the map since perspective scales all rates together, and its pushforward covariance Q = sigma^2 G^T G is nondegenerate for a footprint not aligned with a base phase; there the characters with n^T Q n <= c are retained (an ellipse in Z^2, finite, shrinking to the origin in the far field), the rest is Haar-averaged, and the certificate is computable from Q alone; where the ellipse is large the cell side of the theta duality takes over with the Gaussian tail as its certificate. That is a certified evaluation of every bounded periodic two-dimensional material with work the smaller of the ellipse's lattice count and the cell count, to be stated, credited and audited before it is called a theorem.

The incommensurate class, d >= 3 with irrational phase ratios (the mask of three wavelengths, the ripple against the checker), is the frontier: Q is singular along d - 2 directions, the bound's sum diverges along them, no finite retained set is certified by boundedness alone, near-resonances are real moire, and conditioning on a slow combination can determine the carrier (two gratings on one x, Q = 0) so that no mixing argument applies. The collaborator's two questions are the program there: when does the geometry supply true fast fibres with mixing, and when does the shader supply the regularity to replace dependent slow-fast motion by an effective law. My hypothesis for it, undefended: retain the near-kernel combinations as continuous slow variables with their narrow distributions, integrate over them, certify the remainder by the bound, and limit the retained order by a regularity assumption that two-scale homogenization would have to supply.

Two more corrections from the radial answer (#95). The radial heat semigroup in screen coordinates is classical and exact (J0(k r) eigenfunctions, the Hankel transform, the I0 transition kernel; NIST DLMF 10.22), a shared operator for radial materials in screen coordinates, but a ripple in perspective is radial in surface coordinates while the diffusion is induced by screen coordinates, so the radius alone does not close there and the state (u, v, psi) was needed for that reason; the Jacobi-Anger sidebands and the radial eigenfunctions are distinct identities that share a name. And the quadratic pushforward multiplier is not the generator with frozen coefficients: pushing a Gaussian through a quadratic map is not freezing a latent diffusion, the closed state T = x^2 already has state-dependent diffusion and a non-Gaussian law, and any correspondence has to be derived, not assumed. The constructive target the collaborator proposes, and I accept: a reusable family-level scale operator, found by discovering a sufficient geometric state, the observables it must preserve, the operator on those observables, and the exact symmetries or fast fibres that reduce the state or the interaction rank.

## 11. Open questions I do not know how to answer yet

Whether the moire spectrum's Diophantine count survives the curvature of the phases (the cylinder bends), and what replaces it under exact depth conditioning. Whether the self-dual expansion has a clean mixed form for anisotropic footprints. Whether the intrinsic-complexity lower bound holds for correlated (stratified, blue-noise) samplers, which beat 1 / sqrt(N) on smooth integrands but not, I think, on the aliasing variance. How much of the slope-plane pairing survives a real BRDF with visibility terms. And whether a general material graph can be lowered to a torus function with a computable moire spectrum by the compiler, which is the question the product actually asks.

## 12. Sent to the collaborator after #105 (2026-09-06): the algebra, the resummed memory, the T2 constant, the ladder

Proposed on the bridge, not yet audited.

The algebra. The count-map calculus is the algebra of complex Gaussians on the screen, exp(i (b . x + x^T H x / 2)) with complex quadratic forms, times polynomial amplitudes. It is closed under products (phases add, Hessians add), closed under filtering with the multiplier theorem as the closed-form action, and the filtered product series of FILTERED-COMPOSITION.md is summed exactly in it. Perspective: every phase at a pixel shares the pole, the pixel's depth, so sums of rational-linear phases are rational-linear with that pole and their exponentials are closed under multiplication (the phase values themselves are not closed under products, #119); the filtering of such an exponential is the depth quadrature, exact conditioning rather than a closed form (#114).

The exact response, and what was withdrawn (#114). For the observable exp(i m theta(x)) with theta quadratic, the exact filtered response is det(I - i t m H)^{-1/2} exp(-t m^2 b^T (I - i t m H)^{-1} b / 2) at t = sigma^2, closed form for all t, and that is the whole claim. I first sent it as a resummed Mori-Zwanzig memory of the curvature with coupling rank three. The collaborator showed that the first-order term t (i m tr H / 2 - m^2 |b|^2 / 2) is the generator's own action (the drift I had dropped), that memory starts at order t^2, and that the eliminated space is not three modes but every polynomial degree, so the identification is withdrawn until a space, a generator and a projection are named. The ripple's Jacobi-Anger expansion gives a bandwidth at a tolerance, about 2 |m A| + O(|m A|^{1/3}) sidebands, not an operator rank. Members restricted to a half-plane are a Faddeeva function after integrating along the edge, the hard-mask-over-pattern node the compiler lacks; wedges need the joint coverage, a quadrature of Faddeeva functions along one edge, not a subtraction of half-plane masses (all half-planes through the centre of an isotropic Gaussian have mass one half, a wedge's mass varies with its angle).

The T2 cost, corrected (#113, #115). The retained ellipse n^T Q n <= c has area pi c / sqrt(det Q) and the footprint u^T Q^{-1} u <= R^2 has area pi R^2 sqrt(det Q), so the leading terms of the two counts multiply to c R^2 / 4 and the smaller is at most R sqrt(c) / 2, about seven at 1e-3. But lattice counts carry perimeter terms. By Steiner's formula (unit squares around lattice points, cells inside the footprint's 2 pi sqrt 2 neighbourhood) the elementary bound is min(characters, cells) <= R sqrt(c) / 2 + 2 sqrt(pi R sqrt c) kappa^{1/4} + 2 pi, kappa the condition number of Q, and the collaborator's example Q = diag(L^2, L^-2) shows the perimeter is the whole story: the spectral ellipse holds (0, k) for |k| <= L sqrt c and the footprint holds the cell centres (2 pi j, 0) for |j| <= R L / (2 pi), so both counts are at least linear in L = kappa^{1/4}, 8 L and 1.3 L against the upper bound 7 + 13 L + 6. The honest cost of the two-sided enumeration is Theta(L), with L^2 the footprint's aspect ratio in phase space, bounded for bounded aspect ratio and about 150 at aspect ratio 100; the seven-term camera-free claim is withdrawn. The retention level c must not be set from det Q either (the needle again: Q = diag(L^2, L^-2) has a theta sum of order L at det Q = 1). The collaborator's repair (#118) closes the theorem: bound the theta sum by a layer-cake integral of the proved character count, choose the spectral branch only when sqrt(det Q) >= 2 pi sqrt(c) / R and the cell branch otherwise, and c = max(1, [2 ln(1 / (2 eps)) + ln M] / (1 - eta)) with M = R / (2 eta) + (pi / (2 sqrt eta)) sqrt(R) kappa^{1/4} + pi / 2 depends on eps and kappa only. The count bound is then independent of the overall scale of Q, proved on both sides, with the material work (cell integrals, coefficients, winding, graph evaluation) a separate visible term. The mixed expansion that would remove L is a separate theorem with two obligations: torus coordinate changes must be in GL(2, Z), so the alignment with the long axis is by a primitive integer vector whose Diophantine quality enters (the best v with |v| <= N aligns to about 1 / N); and the partial coefficients in the resolved phase are functions as complex as the material's boundary (1{0 < theta2 < a(theta1)} has zeroth partial coefficient a(theta1) / 2 pi), so the resolved integral is the node's own near-field kernel and the theorem bounds nothing about arbitrary materials. The mixed expansion itself (bridge #134, #139): in torus coordinates from A = [a_1 a_2] in GL(2, Z) with a_2 primitive at angle eps from the long axis, Var(phi_1) = q^2 s D and Var(phi_2 | phi_1) = s / (q^2 D) with q = |a_2| and D = cos^2 eps / sqrt(kappa) + sqrt(kappa) sin^2 eps, so cells across and characters along multiply to (R sqrt(c) / pi) q^2 D, a constant at q = kappa^{1/4} and eps = 1 / q^2; the collaborator's constructive upper bound at balanced scale (#139) partitions the spectral lattice into the lines a . n = j, of which O(1) are populated since a^T Q^{-1} a = O(1) / s^2 for a bounded-denominator Dirichlet vector a of length O(kappa^{1/4}), each holding O(1 + kappa^{1/4} / |a|) points; with the mixed material work O(|a| E) the smaller of the two is O(1 + kappa^{1/8} sqrt(E)) in terms plus lookups if the spectral coefficients come from a table at unit cost with its preprocessing and memory stated, and O(E (1 + kappa^{1/8})) if each polygon coefficient is built from its edge formula at O(E) (the collaborator's #140: coefficient construction stays visible on both branches), and a third, purely spatial branch is needed as well (Q = diag(L^-4, L^-2) has Theta(L^3) spectral modes and Theta(L) aligned characters but O(1) cells). The coefficients are certifiable per node (polygons by the divergence theorem, conics by Bessel, thresholds of smooth fields by a boundary integral along the level curve, textures for their band-limited interpolant).

The ladder. For d phases with surface rates K and screen rates G = J^T K, ker G = ker K, so the near-resonance direction is the material's and the cutoff |G m| sigma <= sqrt(c) is the camera's. The material's ladder, the integer points near ker K ordered by |K m|, is precomputable once, and a pixel retains a prefix of it under the camera's metric; the effective law is the ladder sum, a slowly varying field on the screen with rates G m, which is the moire pattern itself. Its two assumptions, which answer the collaborator's trace caveat (#106) for these materials: (a) the pixel measure gives no mass to the material's discontinuity set, generic for a leaf against a curved boundary and violated by 1{theta = s}; (b) the coefficients along the ladder are absolutely summable. With Gaussian multipliers the ladder sum is the Gauss mean of the material paired with the pixel measure, bounded convergence gives the pixel value under (a), and dominated convergence removes the Gauss parameter under (b). Checked numerically before it was sent (paper/tools/exp/theory-probes/ladder.mjs). For two half-arc gratings a(omega x) b((omega - delta) x) the bound I first sent, (delta / omega)^2 (1 + sqrt(2 pi) / (delta sigma)) / 3, is false, and so was its first repair: both treated the Gaussian's support as hard. The carrier's own leakage (the k = 0 and l = 0 terms) does not vanish as delta -> 0 (#116), and an exact resonance between coprime rates keeps a unit lattice mass however large sigma is (#122: omega = 3, delta = 2 leaves an error of 1 / 12 in the limit; rates N + 2 and N leave 1 / (4 N (N + 2)), 1.6e-4 at delta / omega = 0.049). The certificate that stands is the collaborator's (#123). With a = omega - delta, 0 < T < a, f_T = exp(-sigma^2 T^2 / 2), I_T = int_T^inf exp(-sigma^2 u^2 / 2) du and p, q the arc fractions: |V - B| <= N + Lambda + R, where N = [delta^2 + sqrt(2 pi) delta / sigma] / [3 (omega - T)(a - T)] is the near-resonance windows (inside the window of family n, |l| >= (|n| omega - T) / delta and |n - l| >= (|n| a - T) / delta, the Gaussian lattice sum is at most 1 + sqrt(2 pi) / (sigma delta), and (|n| omega - T)(|n| a - T) >= n^2 (omega - T)(a - T) is summed over n), R = (2 / 3) f_T + (1 / 3)(1 / a + 1 / omega) I_T is the far remainder (1 / (|k| |l|) <= (k^-2 + l^-2) / 2, and each shifted-lattice Gaussian tail outside [-T, T] is at most 2 f_T + 2 I_T / spacing), and Lambda <= -(2 / pi) [q log(1 - exp(-sigma^2 omega^2 / 2)) + p log(1 - exp(-sigma^2 a^2 / 2))] is the leakage. Checked on both counterexamples (1.07 against 1 / 12; 8.7e-4 against 1.6e-4) and on the two-grating points (paper/tools/exp/theory-probes/cert123.mjs and ladder-exact.mjs, the latter an exact piecewise evaluation that replaced the quadrature and its 5e-5 floor): valid everywhere, loose by twenty to a hundred and fifty against the exact error, with the resonance floor rho^2 / (3 (1 - rho)) at fixed ratio rho = delta / omega, so it certifies 2e-3 from about two cycles per sigma at rho = 0.01 and from about ten cycles at rho = 0.05 (#126 corrected my 'not at 0.05'). The exact absolute family sum is a diagnostic within a factor of 2.5 of the exact error, with a truncation tail of order 1 / K after K families. The corrector certificate of the collaborator's theorem, [sqrt(pi / 2) / sigma + 2 sum |delta_j|] / |omega|, needs no coefficient sum and is polynomial in 1 / (omega sigma), because it puts one derivative on the window and stops at the material's bounded primitive; the ladder puts every derivative on the window, which is entire, at the price of summing the material's harmonics under assumption (b); and at fixed ratio rho both have floors, 2 rho and rho^2 / (3 (1 - rho)). The noise mask's kernel direction is (0.838, -0.343, 0.424), its first rungs (10, -4, 5), (12, -5, 6), (2, -1, 1), its ladder density flat in |m| at 33, 14, 5 and 1.2 rungs per unit |m| for k sigma = 2, 3, 5, 10 (retaining n^T Q n <= 15), and its coefficients decay like |m|^{-2.1} in rms along the ladder (3.4e-3 at |m| about 7, 2.6e-4 at 23). At k sigma = 2 the absolute tail beyond |m| = M is about 5.5 / M, so no absolute certificate at 8 bits is within reach (|m| to 2700, ninety thousand rungs), while the rms tail reaches 2e-3 near |m| = 45, about 1500 rungs; the kernel's series to |m_i| <= 12 measured 2.5e-3 to 3.4e-3 there, consistent with this. These are predictions from a finite table, kept separate from every theorem (#116). So the transition band of a hard-thresholded incommensurate material is expensive for coefficientwise absolute certification in this representation; that is an upper bound on the pixel's cost and not a property of the material (#131: g = 1{cos(q . theta) > 0} with K q = 0 is constant on the leaf and costs one comparison while its absolute sum diverges, so exact phase relations are composed first). A soft threshold of width w has exponentially decaying coefficients and a ladder that truncates in O(log(1 / eps) / w) rungs everywhere, and the compiler can say so at compile time. The collaborator's arrangement principle (#108, the 2m endpoints of m single-arc masks on the fast circle, any bounded graph averaged exactly in O(m log m)) is the cell side of the same duality on the fast circle; the ladder is its spectral side.

Robust after this round: the density lemma; the scale-independent T2 count with the collaborator's repair; the ladder identity for the shared-carrier family with assumptions (a) and (b), the collaborator's closed certificate #123 and the exact absolute sum as a diagnostic; the collaborator's shared-phase cost theorem; the exact filtered response of the complex-Gaussian family. Withdrawn this round: the memory identification and its rank, the filtering closure of rational phases, wedges by subtraction, the seven-term camera-free cost, the determinant-only retention level, both of my ladder closed forms (false at delta -> 0 and at exact resonances), and the unconditional uniform-in-scale factor of the compression lemma (it needs a gap of the augmented generator, which depends on the camera and is absent along an incommensurate direction).

## 13. Lattice noise, in progress (bridge #146, #154)

The class the product uses most is hashed lattice noise, and the question is whether it needs a theory of its own. The framing that survives the collaborator's reading: an explicitly periodic noise construction (a permutation table of period P in lattice units, by construction and not by the rationality of stored rates) is a periodic material on the torus of that period, and the count theorem applies to it with the whole noise as the material work of one cell of the big period. Its exact spectrum is not a sampled DFT of the noise, since the piecewise-polynomial interpolant has infinitely many harmonics: it is the finite DFT of the periodic gradient coefficients multiplied by the analytic Fourier transform of the compact interpolation basis, at every periodic index alias, which the collaborator is deriving; the basis transform's decay is what would certify the spectral tail beyond any finite table. Near and transition fields are the lattice-cell branch, exact polynomial-times-Gaussian cell integrals; those integrals need a bivariate rectangle primitive for a correlated footprint and factor into univariate error-function products only when the footprint's axes align with the lattice axes, and even then a separable window leaves a bilinear form over the gradient table rather than O(N) scalar work unless filtered rows are precomputed and charged. A threshold of the noise is a different integration problem, the coverage of a polynomial superlevel set per cell. The crossover between the branches near a few cells per sigma and any statement that the peak is the material's are unproved representation-cost estimates, under the counterexample of #131; the finite basis and its exact filtering lead, with the hash structure and precomputation explicit.

The exact spectrum (the collaborator's #158). With P-periodic gradient arrays G_x, G_y and the basis phi_0(t) = w(t), phi_1(t) = t w(t), w = 1 - fade(|t|) on |t| <= 1 and zero outside, the noise is F(x, y) = sum over i, j of [G_x,ij phi_1(x - i) phi_0(y - j) + G_y,ij phi_0(x - i) phi_1(y - j)], and for every n in Z^2, with xi = 2 pi n / P and the unnormalized P x P DFT G_hat of the gradients, F_hat(n) = P^-2 [G_hat_x(n mod P) phi_hat_1(xi_x) phi_hat_0(xi_y) + G_hat_y(n mod P) phi_hat_0(xi_x) phi_hat_1(xi_y)]: the DFT index is modulo P, the basis transforms take the full harmonic including every replica, and for the quintic fade phi_hat_0(xi) = 2 int_0^1 (1 - 10 t^3 + 15 t^4 - 6 t^5) cos(xi t) dt with phi_hat_1 = i d phi_hat_0 / d xi, elementary with removable singularities. A sampled DFT of the noise's values misses the interpolant entirely (P = 1, G_x = 1: F(1 / 4) = 75 / 512 while the value DFT is zero, the noise vanishing at every vertex). The Gaussian-filtered value multiplies each coefficient by exp(i xi . mu - xi^T Sigma xi / 2) with the infinite tail certified from the basis transforms' decay times the gradients' bound, the density lemma scaled by the certified value-range width for a signed material; for a diagonal Sigma there is also the exact contraction u_1^T G_x v_0 + u_0^T G_y v_1 with (u_r)_i the expectation of the periodized phi_r at X - i, which names where matrix rank, hash structure or amortized preprocessing would have to reduce cost. Each cell carries a tensor polynomial of degree up to six per coordinate. This factorization is classical shift-invariant reconstruction applied to the noise source; the source is Perlin's reference construction, whose period 256 is the explicit mask and not rationality.

The hash's algebra (the collaborator's #160). On a fixed integer slice the reference hash is H(i, j, k) = pi(pi(pi(i) + j) + k), so with f_k(v) = gamma(pi(pi(v) + k)) the gradient component matrix is G_ij = f_k(pi(i) + j mod P): row-permuted reverse-circulant. For weight vectors u, v (the expectations of the periodized basis functions at the pixel), with w_r = u at pi^-1(r) and the unnormalized DFT, u^T G v = P^-1 sum_l f_hat(l) w_hat(-l) v_hat(-l), the frequency reversal mattering: the bilinear form is a cyclic convolution of the permuted weights against the other weights, paired with the inner function in Fourier space. Two source components need four query FFTs and the two precomputed transforms of f, so the exact filtered noise costs O(P log P) query arithmetic with O(P) stored transforms and no P^2 gradient table, without a low-rank assumption; the weight vectors are proved uniformly in the width (#164, NOISE-HASH-FILTERING.md): each periodized basis-weight vector has l1 norm at most one; for sigma <= P the supports meeting [mu - R sigma, mu + R sigma] with R = sqrt(2 log(2 / delta)) are integrated as fixed-degree Gaussian moments and folded modulo P, work O(P (1 + R)) and l1 error at most delta; for sigma > P the periodized weight is P^-1 sum_n phi_hat_r(2 pi n / P) exp(i 2 pi n (mu - i) / P) exp(-a n^2) with a = 2 pi^2 sigma^2 / P^2 > 2 pi^2, truncated at N = max(1, ceil(sqrt(log(1 / delta) / (2 pi^2)))) with l1 error at most exp(-a N^2) / (a N), work O(P N); four vector errors delta give a contraction error at most 4 delta + 2 delta^2, so delta = eps / 6 gives eps; query arithmetic O(P log(2 P) + P (1 + sqrt(log(1 / eps)))), preprocessing O(P log(2 P)), storage O(P), independent of both widths, zero variance by direct basis values, and the result is the Gaussian mean to a named tolerance eps, not the exact infinite mean. Scope: the specified nested hash, a diagonal Gaussian in lattice coordinates, linear noise output, a fixed slice; a non-integer slice adds a third component with the same structure. Read as theory, corrected by #164: a circulant hash would keep the spectral sparsity of a periodic material, and the outer permutation scrambles Fourier locality; but the FFT cost is an upper bound on one method and not a lower bound on any, since a permutation preserves norms and parameter smoothness (the l1 distance between the permuted weights of neighbouring pixels equals that between the unpermuted ones), sparse-support branches help in the near field and precomputation can amortize repeated work; whether the permutation forbids polylogarithmic query work under a stated storage, error and query model is not shown either way.

The second representation, precomputed exactly filtered levels (bridge #159, corrected by #162). For an isotropic Gaussian in affine coordinates the filtered noise u(x, t), t = sigma^2, solves the heat equation with generator Delta / 2, so between exact levels t_1 < t_2 = t_1 + h linear interpolation in t errs by at most h sup |Delta u| / 4 (h sup |Delta u| / 2 was conservative), and for t_1 > 0 a second-time-derivative bound gives h^2 sup |Delta^2 u| / 32, which justifies geometric level spacing on a positive scale range. Two things I had wrong or missing: exact level values are not an exact lookup, since sampling them on P^2 texels adds a spatial reconstruction error (the P = 1 noise is invisible at lattice points before any filtering), so the scheme must name its mesh and interpolant and charge, for bilinear, h_x^2 sup |u_xx| / 8 + h_y^2 sup |u_yy| / 8 plus coefficient, precomputation and quantization errors and the scale interpolation; and the spectral bound on sup |Delta u| decreases with t, so it does not place the densest levels in an interior transition band, which was a wrong reading and needs a different quantity if it is to be said at all. A covariance covering needs a bounded parameter range, a minimum positive scale or a near-field rule, enough spatial samples at every level, and for anisotropic paths the directional derivative D_A u = (A : Hess u) / 2 along the whole positive-definite path including the mixed covariance terms. 'A few lookups per pixel' moves the work and storage offline and is not yet a small-table theorem; the primary precedents are Williams 1983 (pyramidal parametrics) and Feline (McCormack et al. 2000), and this coexists with the hash contraction without either inheriting the other's cost model. Three details from #166: the second-time-derivative bound assumes linear interpolation in the variance t, and the usual log-LOD interpolation in s = log t has the second derivative (t / 2) Delta u + (t^2 / 4) Delta^2 u, which supplies its own bound; the spatial mesh must resolve each level's content, since F = 1 / 2 + sin(2 pi x) / 2 on a torus of period 256 has every unit-grid sample equal to 1 / 2 at every heat level while u stays nonconstant, so dense scale levels cannot repair missing spatial samples; and the exact anisotropic decomposition Sigma = lambda_min I + (lambda_max - lambda_min) v v^T gives U_Sigma(x) = E_Z U_{lambda_min I}(x + sqrt(lambda_max - lambda_min) Z v), the correct starting identity for isotropic probes along the major axis, whose uniform-error probe count must retain the aspect ratio: proved (#168). Approximating N(0, Sigma) of condition number kappa by a material-independent positive normalized mixture of N isotropic kernels of covariance lambda_min I with arbitrary means, project onto the major axis; the union S of the intervals of radius b sqrt(lambda_min) around the projected means receives at least p_b = Pr(|Z| <= b) from the mixture and at most 2 b N sqrt(lambda_min) / sqrt(2 pi lambda_max) from the target, so the total variation is at least p_b - sqrt(2 / pi) b N / sqrt(kappa), and a uniform error eps < p_b over all bounded materials needs N >= sqrt(pi / 2)(p_b - eps) sqrt(kappa) / b. This is a genuine lower bound on that representation (the probe count grows with the aspect ratio), the first in the exchange, unlike the earlier absolute-tail upper costs; it says nothing about material-adaptive algorithms, restricted materials, signed weights, other kernel variances or rendering in general, and it marks where source-specific structure, richer tables or another representation must enter. The hash contraction's proof is NOISE-HASH-FILTERING.md at edbf317; a bounded prior-art search found no paper using the hash-to-circulant connection, which is not a novelty determination.

Correlated footprints (my analysis after #170, corrected by the collaborator's #172). The hash algebra handles rank-one weights in the lattice axes, and a footprint whose covariance does not separate in those axes is exactly the loss of that separability; the question is where to spend the rank. What I first sent had the wrong decomposition and three further errors, recorded here with the corrections. The marginal-weighted Mehler kernel sum_n rho^n He_n(x) He_n(y) / n! has singular values |rho|^n, but pairing an arbitrary bounded joint material uses the Hilbert-Schmidt norm, with residual |rho|^N / sqrt(1 - rho^2) when N terms are kept, and L2 optimality in that space (Eckart-Young) is not a separation-rank statement for bounded-material integration nor a pixel-error certificate; the claim that the lattice-axis rank is linear in kappa was an artefact of that weighting. The unweighted Gaussian kernel in L2(dx dy) has Hermite functions as singular functions with ratio r = |rho| / (1 + sqrt(1 - rho^2)), and near 45 degrees at high kappa 1 - r is of order 1 / sqrt(kappa), so a separable expansion in the lattice axes scales like sqrt(kappa) times logarithms without turning the source grid. The certificate is the collaborator's, audited by three readers (#177): for the standardized density p_rho with s = sqrt(1 - rho^2), r = rho / (1 + s), R = |r|, q = R^2 and c = s^-1/2, p_rho = lambda_0 sum_n r^n psi_n(x) psi_n(y) with psi_n(x) = sqrt(c) h_n(c x) the scaled normalized Hermite functions and lambda_0 = sqrt(1 + q) / (2 sqrt(pi)), a decomposition that is prior art (Fedorov et al. 2009, section II); keeping n < M, with mu = M + q / (1 - q), v = q / (1 - q)^2, A = mu + 1 / 2 and B = A^2 + (q / 2)(mu + 1)(mu + 2) + (1 + q / 2) v, the L1 truncation error is at most E_M = R^M sqrt((pi / 2)(A + sqrt(B))); the truncated mass m_M = sqrt(1 - q) sum_{j <= (M - 1) / 2} binom(2 j, j)(q / 4)^j is restored by adding (1 - m_M) times a normalized positive ground product, which changes only the n = 0 coefficient and keeps the rank at most M; and for every measurable material with values in an interval of width Delta the pixel error is at most (Delta / 2)(E_M + 1 - m_M) <= Delta E_M. Independent axis shifts and scalings preserve the error and the original-axis separability; a general affine mixing preserves the error but not the separability. With E_M = O(R^M sqrt(M + (1 - R)^-1)) and R = (sqrt(kappa) - 1) / (sqrt(kappa) + 1) at 45 degrees, the sufficient rank is O(sqrt(kappa) [log(Delta / eps) + log kappa]), not an optimal L1 rank, and neither the cost of integrating high-order modes nor numerical stability is established. Major-axis probes have the lower bound of #168 at the same order and no proved upper bound, so they are not to be called equally cheap. The correlation of a footprint of condition number kappa at angle theta to the lattice is rho = (kappa - 1) sin(theta) cos(theta) / sqrt((kappa cos^2 theta + sin^2 theta)(kappa sin^2 theta + cos^2 theta)), about (kappa - 1) theta / sqrt(kappa) at small theta, so a tilt of order 1 / sqrt(kappa) already gives a correlation of order one (my small-angle formula lacked the denominator). Major-axis probes: the decomposition Sigma = lambda_min I + (lambda_max - lambda_min) v v^T makes each probe an isotropic, hence diagonal, contraction, and #168 gives the lower bound of order sqrt(kappa) probes for positive fixed-variance mixtures; a matching upper bound (a Gauss-Hermite node count with its factorials, constants and total error, or another construction) is not proved. The lattice cannot be turned toward the footprint as the mixed branch of the T2 theorem turns a torus, because an integer change of index puts a varying argument inside the nested permutation and destroys the permuted-circulant structure; that much stands. Hashed gradients are not independent arbitrary inputs, and the conjecture that sqrt(kappa) is intrinsic for material-independent online methods is unsupported; source-specific upper bounds may improve on kernel-uniform approximation. Literature: Mehler 1866; the Hermite functions as the Gaussian kernel's singular functions; separation rank after Beylkin and Mohlenkamp; Feline; EWA.

Nonlinear use, squaring (the collaborator's #174, exact). With G^a_ij = f_a(pi(i) + j), a basis-overlap offset (d, e) of the squared noise produces the coefficient matrix H_ij = f_a(pi(i) + j) f_b(pi(i + d) + j + e). With Delta_i = pi(i + d) - pi(i) mod P, the classes I_Delta = {i : Delta_i = Delta} and the profiles h_Delta(r) = f_a(r) f_b(r + Delta + e), H = sum over Delta of diag(1_{I_Delta}) Pi C(h_Delta): a sum of D permuted circulants, D the number of distinct translation differences of the permutation at offset d, so D fast contractions, O(D P log P) arithmetic and O(D P) cached profiles, an upper bound and not a lower bound, with the direct sparse-class contraction recovering O(P^2). For d = 0 the structure is one permuted circulant; for d != 0 a single term with the same permutation requires every Delta_i to coincide, which for d = 1 forces an affine permutation, and a P = 4 example (pi = (0, 1, 3, 2), f_a = f_b = (1, 2, 3, 4) / 4) has row multisets that no single row-permuted circulant can produce. The overlap offsets are constant in number; D is not, and is a property of the hash (one for an affine permutation, at most P in general; a typical value for a random permutation would need a specified model and a proof). D is an explicit representation budget rather than necessary material state, since distinct profiles may coincide for particular f; coefficient composition is where it appears; the local polynomial basis products stay finite-degree Gaussian primitives. The correlated-Gaussian unweighted Hermite expansion has a derived L1 tail with a rank-preserving mass correction under independent audit: a sufficient rank of order sqrt(kappa) times logarithms, not an optimal L1 rank nor a full per-pixel cost, since the high-order Hermite weights must be charged and the fixed-degree weight proof does not carry to order n.

The group-action frame (after the collaborator's #175, corrected by their #180). Three structures, each with its own complexity: the source algebra, the translation action, and the footprint pairing. A material indexed through a group-valued map filters by that group's Fourier transform: on the torus the map is the phase, the characters are the count-map harmonics and the pairing is the multiplier theorem; for the nested hash the row permutation leaves a cyclic action and the pairing is the FFT contraction; for the XOR hash G_ij = f(P_x[i] XOR P_y[j]), reindexing both query vectors leaves the action of (Z / 2 Z)^b, Walsh functions as characters and a Walsh-Hadamard contraction at O(P log P) with O(P) memory. Same-point products of characters close for every map, curved or affine, since exp(i n theta(x)) exp(i m theta(x)) = exp(i (n + m) theta(x)); what affinity buys is the footprint pairing, a simple multiplier for the Gaussian, and a quadratic source phase has an exact quadratic pushforward while an arbitrary curved phase is not handled exactly by its second-order jet (the source-versus-model distinction). The translation-difference classes are a different operation, the translation action on the indexed source: they enter noise composition because the reconstruction basis overlaps neighbouring cells, so a product couples G at (i, j) with G at (i + d, j + e), and their number D at each offset is the collaborator's representation budget for squaring (at most P; one for an affine permutation; no proved typical value; distinct profiles may coincide for particular f). For the XOR hash the safe separable masks are the pairs (Delta, E) of row and column differences, hence D_x D_y contractions with transformed profiles reusable when Delta XOR E coincides; grouping by that XOR alone gives a nonseparable mask and no single fast contraction. A GF(2)-affine permutation has D = O(log P) by the carry runs of (i + d) XOR i and a cyclic-affine one has D = 1; the papers that reject such hashes (Kensler, Knoll and Shirley's section 2 on the nested hash's shifted columns) diagnose specific spectra, and whether every low-class source must look bad is not established, since perceptual correlation also depends on the gradient lookup, the reconstruction and the observable; the trade between composition state and noise quality is refuted as a necessity (#182): with b = 8, Gray(i) = i XOR (i >> 1) and M = I + S + S^2 for the cyclic bit shift S, invertible over GF(2), the hash H(i, j) = Gray(i) XOR M Gray(j) changes at every unit offset from every base point while each axis keeps eight difference classes, and for an iid centred lookup table the ensemble covariance vanishes at all eight immediate-neighbour lags; that a fixed realization looks good, lacks distant artefacts or has the desired reconstructed spectrum is not proved. The constructive direction (#182, under audit): for P = 2^b and GF(2)-affine axis hashes, the four corner lookups of a cell depend only on g = A_x i XOR A_y j XOR c and the two carry states A_x((i + 1) XOR i) and A_y((j + 1) XOR j), each with at most b values including wrap, so a fixed polynomial shader of same-grid noise fields sharing the axis maps has cell polynomial coefficients indexed by (carry pair, monomial), the composition state staying at most b^2 however the degree grows, with separable carry masks, Walsh-Hadamard transforms of the P-entry coefficient profiles precomputed and the masked one-dimensional cell-moment weights reused: about O(P b^2 (Q + 1)^2) cached coefficients, O(P b^3 (Q + 1)^2) preparation and O(P b^2 (Q + 1)^2) per query, weight formation charged separately, Q <= 6 K for degree K in the noise fields, The theorem is complete and audited (NOISE-COMPOSITION.md at ee9f165, reviewed by me step by step): with Bernstein cell responses the coefficient tables h_{delta, eta, alpha, beta}(g) have a finite bound B, the periodized Gaussian weights over cell and Bernstein index are nonnegative with joint l1 norm one, a joint l1 weight error delta per coordinate gives pixel error at most B (2 delta + delta^2) with no factor from b or Q, the weights are built at every width (retained cells below the period, Fourier replicas above it with the Bernstein transform bounded by 1 / (Q + 1)), and the all-width query cost is O(P (Q + 1)^2 [b^2 + 1 + sqrt(log(2 / delta))]) with source memory O(P b^2 (Q + 1)^2) and preparation O(P b^3 (Q + 1)^2) plus coefficient formation; the widths have disappeared from the bound, the period, the degree, the amplification, the preparation and precision have not, and thresholds, warps, unrelated grids, three-dimensional slices and correlated footprints are outside it. Two sharpenings I offered: the Bernstein bound is multiplicative under products (the product's coefficients are convex combinations of products of the factors'), so B is at most the shader's coefficient l1 norm times the fields' bounds to their powers; and the constant is heavy, about 2.8 million operations per pixel at P = 256 for a squared noise, so the theorem's value is the finite state and the certificate, not speed. Read against section 8, it is the conditional-homogenization triple made exact for a finite family: macrostate (g, delta, eta), observables the Bernstein responses, operator the masked Walsh contraction, the carry classes being the translation action's contribution to the state, which does not multiply under composition because every field reads the same corners; another concrete family after the shared-carrier graphs, not the first (#195). The collaborator's explicit count: 256 x 8^2 x 13^2 = 2,768,896 coefficient products per pixel for a squared noise before weights and transforms, and about 10.6 MiB of cached scalars, terms and storage rather than measured operations, and no gaming-cost result; the Bernstein bound B stays a computable source-dependent bound that can exceed the output range under cancellation. The consequence I proposed, to compose first and bake at the period, is audited for polynomial compositions and refused for thresholds (#183): a same-grid polynomial composition of per-coordinate degree Q has the exact coefficient oracle F_hat(m, n) = sum over a, b <= Q of A_ab[m mod P, n mod P] B_a(2 pi m / P) B_b(2 pi n / P), with A_ab the normalized DFT of the cell monomial coefficients and B_a(t) = int_0^1 u^a exp(-i t u) du, at (Q + 1)^2 P^2 storage, O((Q + 1)^2 P^2 log P) transforms after the coefficients are built, and O((Q + 1)^2) per harmonic, all replicas retained, a square already carrying up to 169 coefficient tables; a threshold of a polynomial is not a polynomial (the indicator of u > 1 / 2 on one cell), so the cache can describe the polynomial being thresholded while its thresholded coefficients still need a certified integral over an algebraic region. The density lemma needs no source tail: with the zero mode retained and a material of range Delta the truncation error is at most (Delta / 2) sqrt(sum over the omitted harmonics of exp(-kappa_m^T Sigma kappa_m)), the footprint density's tail and not the source spectrum's, so my 'not shown: the baked spectrum's tail' was a non-issue; and improved Perlin is C^2 with products preserving it, its pure fourth derivatives finite measures on the cell grid, so its spectrum decays radially like the fourth power with source-dependent constants, not the second. That the budget is not wasteful is not shown, since a large budget for one representation is not a lower bound; coefficient errors and a degenerate Sigma still need explicit budgets.

## 14. Sparse Gabor convolution noise: the second family, with the collaborator's corrections (#184, #186)

From the primary source (Lagae, Lefebvre, Drettakis and Dutre, "Procedural noise using sparse Gabor convolution", SIGGRAPH 2009). The noise is N(x) = sum_i w_i g(x - x_i) with g a Gaussian envelope times a harmonic, K exp(-pi a^2 |x|^2) cos(2 pi F_0 (x cos w_0 + y sin w_0)), impulses from a Poisson process per grid cell (a per-cell seeded LCG, 25 to 50 impulses per kernel on the GPU), weights uniform on [-1, 1], the kernel truncated where the envelope reaches 5 percent of its peak. Untruncated, that is a finite sum per neighbourhood of members of the complex-Gaussian algebra of section 12, with no lattice indexing in the value: the translation action is trivial. Linear filtering of the untruncated kernels is closed in the paper's section 5.2: Heckbert's Gaussian filter in texture space is a Gaussian with covariance 4 pi^2 sigma^2 J J^T, and "because Gaussians are closed under multiplication, anisotropically filtered noise is again a sum of Gaussians", the filtered kernel having Sigma_FG = (Sigma_F^-1 + Sigma_G^-1)^-1, mu_FG = Sigma_FG Sigma_G^-1 mu_G, a'^2 = 2 pi sqrt|Sigma_FG| and K' = K a'^2 / a^2 N(0; mu_G, Sigma_F + Sigma_G) (their eqs. 19 to 23); the one approximation is theirs by choice, an isotropic envelope for the filtered kernel, which they say could be avoided with anisotropic envelopes. The paper calls filtering nonlinear transformations of noise "an unsolved problem" and sidesteps it with colour lookup tables filtered after Hart et al. 1999.

What I first claimed beyond this and the collaborator corrected (#186). There is no hard band limit: a Gaussian-windowed sinusoid has a Gaussian transform with nonzero tails down to zero frequency, the kernel's integral is (K / a^2) exp(-pi F_0^2 / a^2), small but not zero, and at a fixed centre the broad-footprint value carries a sigma^-2 factor times that integral times the sum of the weights the footprint reaches, which is not zero for an authored realization even though the Poisson weights have zero mean; so the far field decays algebraically, no free far-field-zero certificate follows from F_0 > 0, the overlap factor N(0; mu_G, Sigma_F + Sigma_G) includes the kernel width and is not the pure multiplier, and any far-field proof must keep the full low-frequency leakage. Truncation changes the source and breaks the unrestricted product and filter closure, since the support indicators remain in the algebra; the 5 percent cutoff is not a uniform bound for the complete field, and impulse weights, tail mass and counts must be charged. Poisson counts have no deterministic cap, so density times area is expected work and not worst-case work: the statement needs a finite authored impulse set or a stated probabilistic cost contract. A product of two real Gabor kernels is a sum of two kernels at the sum and difference frequencies, not one; the complex-exponential algebra is exact for an explicit finite list of untruncated kernels, and pair enumeration is a valid upper bound, not an intrinsic cost. Threshold regions of Gabor sums are transcendental, not algebraic: a closed-form jet does not by itself supply exact roots, a source remainder bound, a root count, conditioning or cheap coverage, so the mask node's machinery applies as a numerical method with its own contract (bracketing with Hermite bounds, the enclosure with a certified remainder), whose obligations were already open for three sines. The statement to make first is for a finite authored source, separating linear closure, polynomial composition, field truncation and threshold costs. The family beyond is phasor noise (Tricard et al., SIGGRAPH 2019), a periodic function of a stochastic smooth phase field: a count map with a random phase and vortices at the zeros of the complex field. The collaborator will audit the Gabor field at this source contract after the carry-state theorem of section 13.

## 15. Ten directions not yet tried (2026-09-06, sent to the collaborator for ranking and refutation)

Written at the author's request to think past the increments. Each is a direction with the insight it rests on, not a claim. (1) Liouville-certified moire: algebraic frequency ratios of degree d give |K m| >= C / |m|^(d - 1) for every integer combination by the Liouville inequality, hence a certified gap in the ladder and a far-field error decaying algebraically in sigma from number theory alone, and a design rule with a proof. (2) Degrees-of-freedom lower bounds: the Slepian-Landau-Pollak count of a material class confined to the footprint and a spectral region bounds from below the terms any material-independent exact method must touch, the ladder being that count for a discrete spectrum; the honest form of the withdrawn intrinsic-complexity theorem. (3) The value-space dual: the law of F(X) under the footprint, whose characteristic function is a finite Bessel-truncated multiplier sum by Jacobi-Anger and whose inverse by Gil-Pelaez gives every threshold and transfer of the field from one measure, critical points appearing as square-root singularities with explicit tails. (4) Noise from group representations: hashes as actions of finite non-abelian groups with few irreducible representations and expanding Cayley graphs, so that composition state and decorrelation are set by the same object. (5) Resonance-notched footprints: the pixel window chosen per pixel within a perceptual family so its transform vanishes at the material's near-resonant rates, the period-matched box being the classical case. (6) The space-time ladder: near-resonances of space-time rates under TAA's window, exact shimmer variance under a jitter sequence, and material-adapted jitter. (7) Visibility as a threshold: single-occlusion parallax and self-shadowing as superlevel sets of smooth composite fields, with the onset of multi-valuedness explicit. (8) The perspective multiplier as a series of plasma-dispersion functions away from the horizon. (9) Certified sampling budgets: the analytic pass's interval width as the sampler's allocation, analytic where cheap, sampled where wide, certified everywhere. (10) Authoring across scale on the Gabor algebra, where multi-scale appearance constraints are linear, and its inverse, drawing a far-field image with moire. Held back as smaller: the redundancy of the scale-space of exactly self-similar materials, and noise as a genuinely random field with an exact conditional law.

The collaborator's ranking (#202) and what it changed. Pursue (10) and (6) together through one response operator, with the shared question: which material distinctions remain observable under a family of spatial and temporal measurements, and can exactly those distinctions be authored or compressed; then (3) as a reusable response distribution; (7) restricted to a unique transversal first intersection, with relief and parallax mapping as prior art; (4) exploratory, since few irreducible representations do not make transforms cheap (the sum of the squared dimensions is the group's order) and expander gaps govern random walks rather than raster paths; (1) a niche certificate, explicit under exact number-field hypotheses but needing the cone summability to sum the tail and changing the authored-rate contract. Reformulated or dropped with the counterexamples: (2) a prolate count concerns representing a function class while a pixel is one scalar functional, so a lower bound needs the input access, preprocessing and query family stated; (5) a positive normalized window cannot notch a near-zero rate while keeping its extent, since 1 - Re W_hat(k) <= E[(k . X)^2] / 2 forces E[(k . X)^2] >= 2 for a zero, signed windows amplify, and a notch may remove the visible beat one wanted; (8) an untruncated Gaussian has positive density at the pole, so the reciprocal moments diverge and the termwise series is unjustified; (9) a narrow interval for the mean does not bound the variance (a Bernoulli(1 / 2) and the constant 1 / 2 share the mean), so the sampler needs a pointwise residual range or a proved second moment, which connects to control variates. For (3): Jacobi-Anger tails are small, not zero; the Gil-Pelaez expression with the minus sign is the distribution function P(F <= t); and a positive gradient floor does not give Gaussian decay of the characteristic function (F(x) = log(Phi(x) / (1 - Phi(x))) has inf F' > 0 while F(X) is logistic with characteristic function pi s / sinh(pi s)), so the certificate must come from coarea and nonstationary-phase bounds with every tail charged. For (10): prescribed appearances at several scales must satisfy heat-semigroup compatibility, inversion is ill-conditioned at high frequency, and beat-image constraints are quadratic in the amplitudes (a lifted Gram matrix with a rank constraint), with a weak-modulation version around a fixed reference carrier as the linearizable case. For (6): derive the operator in reprojected coordinates, where correct motion compensation cancels a screen-space phase advance; a static exponential history is a surrogate for actual accumulation with its rejection and clamping; the discrete temporal multiplier and the jitter factor are exact for a specified linear model, with sampling and light-transport frequency analysis as the prior art to check.

The temporal operator (6), derived and sent for objection. With exact motion vectors and no rejection, the history at a surface point obeys H_t = (1 - alpha) B_t H_{t-1} + alpha g(u + d_t), d_t the jitter pulled back to the surface by J_t^-1 and B_t the resampling of the previous history pulled back; the material's phase does not advance in surface coordinates, so unrolling gives a weighted sum of translated copies of the material blurred by the chain of resamplings, and for a character the transfer T(w) = alpha sum_k (1 - alpha)^k B_hat(w)^k exp(i w . d_{t-k}) is exact: a finite sum of exponentials times powers of the resampling response, almost periodic in frequency and never decaying, so accumulation attenuates a high harmonic only through the resampling blur, and a harmonic whose screen rate makes the cycle's phases (J^T w) . j_k cluster is temporally resonant and passes however long the accumulation runs. The shimmer is the variance over the jitter cycle, a quadratic form in the material's coefficients for a sum of characters and free for a threshold; the combined arm's transfer is T(w) exp(-w^T Sigma w / 2), the prefilter supplying the decay accumulation lacks; motion sweeps d_t and breaks static resonance while the varying blur chain is charged; a material-adapted jitter is a finite discrepancy problem against the material's ladder, a claim to check. Prior art, checked in the primary source: Yang, Nehab, Sander, Sitthi-amorn, Lawrence and Hoppe, Amortized supersampling (SIGGRAPH Asia 2009), write the accumulated estimate exactly in this form, a weighted sum of samples at offsets combining jitter and reprojection (their eq. 9, with the recursive update eq. 4), characterize it by the weighted spatial variance of the offsets (eq. 10) with a closed form for constant panning under bilinear resampling, sigma_v^2 = sigma_G^2 + (1 - alpha) / alpha times (v_x (1 - v_x) + v_y (1 - v_y)) / 2 (eq. 11), draw Gaussian jitter, limit the blur with subpixel buffers, and replace a round-robin update by an irregular sequence because the coherent one makes the signal drift (their figure 6), which is the temporal resonance seen as drift. They treat the signal as generic and bound the estimator's variance and blur; the transfer function on a material's harmonics, the resonance condition read off the ladder, the exact shimmer from the algebra and the combined arm's transfer are not in the paper, and those are the only claims of the derivation above. Still to check: the 2020 survey of temporal antialiasing, Egan et al. on sheared reconstruction, Durand et al. on light-transport frequency analysis. Corrections from the collaborator (#205). With J = ds / du and d = J^-1 j, w . d = (J^-T w) . j, so the screen covector of the harmonic is J^-T w and not J^T w; a static camera does not make the resampling constant when its fractional offsets change with the jitter phase, and for a constant multiplier and a cycle of length N the transfer closes as T_t = alpha sum_{r < N} z^r exp(i w . d_{t-r}) / (1 - z^N) with z = (1 - alpha) B_hat, |z| < 1, a periodic B_t needing the product over phases instead. The binary identity g^2 = g makes only a single sample's second moment free: the square of the accumulated history couples g(u + d_s) g(u + d_t) at different jitters plus the resampling correlations, and those joint coverages are the composition problem, so the shimmer of a threshold is not free (for characters the joint terms are products of characters at shifted points and stay in the algebra). A prefilter that varies with the frame belongs inside the history sum, and the product T(w) exp(-w^T Sigma w / 2) holds only for a fixed prefilter commuting with the accumulation in the stationary model. And the observable quotient as I first put it does not exist: indistinguishability within eps is not transitive, so it is no equivalence relation; the exact quotient is by the common kernel of the observation operators, which for Gaussian convolution on periodic function spaces is trivial (no harmonic is exactly invisible), and a family with arbitrarily small footprints approaches the source itself, so there is no universal display band limit. The shared object is instead a response seminorm d(F, G) = sup over a bounded family Q of measurements of ||R_q (F - G)||, or a weighted Hilbert observation norm when singular values help, with Q bounded in scale, time and view; compression is approximation in that metric with a bounded source class and a finite resolution budget, and the count-map structure and the Gabor Gram law supply representations and certificates for particular response families.

The accumulation family in the seminorm (sent for objection after #205). In the stationary model, with theta_s = (J^-T w) . j_s the cycle phase of the harmonic w at jitter s and z = (1 - alpha) B_hat(w), the accumulation's response to a harmonic has two observables: the cycle mean T_bar(w) = alpha m(w) / (1 - z) with m(w) = (1 / N) sum_s exp(i theta_s), the static alias, equal to |m| without resampling blur, one when the cycle phases cluster (resonance, retained and steady) and small when they spread; and the cycle variance Var_t T_t(w) = (alpha^2 / |1 - z^N|^2) sum_{r, r'} z^r conj(z)^{r'} [C(r - r') - |m|^2] with C the cycle's autocorrelation of the phase factor, the flicker, zero at exact resonance. With the L2 norm over the surface as the observation norm, Parseval gives the cycle-averaged observed energy of a difference as sum_w |c_w - c'_w|^2 (1 / N) sum_t |T_t(w)|^2 and the surface-averaged shimmer as sum_w |c_w|^2 Var_t T_t(w), weighted spectral energies certified without absolute coefficient sums, valid for thresholds; the pointwise shimmer is the off-diagonal form with kernel Cov_t(T_t(w), T_t(w')), in the algebra for a sum of characters and the composition problem for a threshold. A fixed prefilter multiplies both weights by exp(-w^T Sigma w), the family Q being the product's scale, cycle and view ranges, and the seminorm's weight on a harmonic is the supremum over Q of |T_t(w)|^2 exp(-w^T Sigma w), which the material's ladder is summed against. Jitter design against a material class is then a finite optimization of the cycle against the class's rungs by the two weights, a claim to check.
