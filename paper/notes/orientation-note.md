# The orientation note: the pair tail of polygonal materials by edge orientation

2026-09-06. A theorem draft for the collaborator's audit (bridge #349 to #355). It refines the source tails of the two-factor composition certificate (composition-certificate.md, the collaborator's GAUSSIAN-LATTICE-PAIR.md) for materials that are piecewise constant on polygonal partitions: the pair mass dropped when the sources are cut at a harmonic radius M is bounded edge pair by edge pair, uniformly in orientation at the rate 1 / M, and for edge pairs with separated normals at the rate log M / M^2. Everything is in radian frequencies with the symbols of the pair note. Certified inequalities are marked as such; one sharper form is an asymptotic and is kept apart. Nothing here is a runtime cost.

## 1. Setting

A material A is a function on the plane with values in [0, 1], periodic on a lattice Lambda_A with cell area D_A, piecewise constant on a polygonal partition of the cell: finitely many edges e, each of length l_e, unit tangent t_e, unit normal n_e, and jump |jump_e| in [0, 1], the difference of the values on its two sides. Its Fourier series uses radian frequencies on the frequency lattice Lambda_A^*, whose cell area is D = (2 pi)^2 / D_A:

    A(x) = sum_{m in Lambda_A^*} a_m e^{i m . x},   a_m = (1 / D_A) int_cell A(x) e^{-i m . x} dx.

The covering radius rho of Lambda_A^* is the largest distance from a point of a fundamental cell to its centre (for a rectangular cell, half its diagonal). A second material B has the same data with the subscript B, edges f, frequency lattice Lambda_B^* with cell area D' = (2 pi)^2 / D_B and covering radius rho'.

The pixel keeps the beats m + n inside an output set E, and the pair note's neighbour bound is the count L_B = max over m of #{n in Lambda_B^* : m + n in E}; here N_E denotes that count, and r_E denotes the reach of E, so that every k in E has |k| <= r_E (for the ellipse k^T Sigma k <= R_out^2, r_E = R_out / sqrt(lambda_min(Sigma))). The one-sided dropped set at radius M is

    P_A(M) = {(m, n) : m in Lambda_A^*, |m| > M, n in Lambda_B^*, m + n in E},

and the quantity bounded is its mass sum_{P_A(M)} |a_m| |b_n|, which dominates the change of the filtered product from dropping those pairs (each pair lands at one beat and the damping is at most one). The mirror set P_B(M) with |n| > M is bounded by the same theorems with the roles exchanged. Pairs whose beat lies outside E are the pair note's output-tail allowance and are not counted here.

## 2. Lemma 1, the per-edge bound

For m in Lambda_A^*, m != 0,

    |a_m| <= sum_e |jump_e| g_e(m . t_e) / (D_A |m|),   g_e(s) = min(l_e, 2 / |s|).

Proof. Write A = sum_c v_c 1_{P_c} over the cells of the partition. For one cell, the divergence theorem with the field e^{-i m . x} m / (-i |m|^2), whose divergence is e^{-i m . x}, gives int_{P_c} e^{-i m . x} dx = (1 / (-i |m|^2)) sum_{edges of P_c} (m . n_out) I_e(m), I_e(m) = int_e e^{-i m . x} ds. Summing over cells, each edge appears once from each side with opposite outward normals, so D_A a_m = (1 / (-i |m|^2)) sum_e (v_right - v_left)(m . n_e) I_e(m) with n_e pointing from the right cell into the left one (the left cell's outward normal on e is -n_e and the right cell's is n_e); the sign matters for a coefficient oracle and not for the envelopes below [sign corrected after the collaborator's audit, #365]. Along the edge, m . x = m . p + s (m . t_e), so |I_e(m)| = |int_0^{l_e} e^{-i s (m . t_e)} ds| <= min(l_e, 2 / |m . t_e|). With |m . n_e| <= |m| the claim follows. Certified.

The bound is a star: inside the strip |m . t_e| <= 2 / l_e the e-term is |jump_e| l_e / (D_A |m|), and outside it |jump_e| 2 / (D_A |m| |m . t_e|), the inverse square with an angular factor. This is a quantitative finite-edge envelope and nothing more: the edge Fourier formula and its angular decay are Lemma 2.2 of Brandolini, Colzani and Travaglini (1997), the organization of interactions by spectral orientation is in Durand and others (2005), and a polygon's wavefront set contains the corner covectors as well as the edge conormals, so the result below is not a product criterion on wavefront sets but an envelope on a lattice, with constants.

## 3. Lemma 2, the counting lemma (the collaborator's, #353)

For a lattice Lambda with cell area D and covering radius rho, a unit vector t, and R, u > 0,

    #{m in Lambda : |m| <= 2R, |m . t| <= u} <= 4 (2R + rho)(u + rho) / D,   #{m in Lambda : |m| <= 2R} <= pi (2R + rho)^2 / D.

Proof. The translated fundamental cells centred at the counted points are disjoint, each of area D, and each lies inside the rectangle |x . t| <= u + rho, |x . n| <= 2R + rho (respectively inside the disc of radius 2R + rho), whose area bounds their number. Certified; no comparison of a point value with a cell supremum is used.

## 4. Lemma 3, the layer-cake sums on a dyadic annulus

For the annulus R < |m| <= 2R of Lambda_A^*, a unit vector t, a length l > 0 and a shift c >= 0, let f_{l,c}(s) = min(l, 2 / (|s| - c)_+) (the l branch when the denominator vanishes), and write A_R = 4 (2R + rho) / D and Psi(x) = x for x <= 1, Psi(x) = 1 + log x for x > 1. Then

    sum_{annulus} f_{l,c}(m . t) <= A_R [ l (rho + min(c, 2R)) + 2 Psi(l (2R - c)_+ / 2) ] <= A_R [ l (rho + c) + 2 + 2 log_+(l R) ],
    sum_{annulus} f_{l,c}(m . t)^2 <= A_R [ 4 l + (rho + c) l^2 ].

The edge factors are g_e = f_{l_e, 0} and the widened partner factors g~_f = f_{l_f, r_E}; the sums S_1^{(e)}, S_2^{(e)}, S~_1^{(f)}, S~_2^{(f)} of section 5 are these with t = t_e, c = 0 and t = t_f, c = r_E respectively.

Proof (the collaborator's form, #356, reproduced). For 0 < lambda <= l, {f_{l,c} >= lambda} = {|m . t| <= 2 / lambda + c}; since |m . t| <= |m| <= 2R on the annulus, the count is at most the strip count of Lemma 2 with u = min(2 / lambda + c, 2R), at most A_R (min(2 / lambda + c, 2R) + rho). The layer-cake formula gives sum f_{l,c} <= A_R int_0^l [rho + min(2 / lambda + c, 2R)] dlambda; the integrand is at most rho + min(c, 2R) + min(2 / lambda, (2R - c)_+), and int_0^l min(2 / lambda, K) dlambda = 2 Psi(l K / 2) for K > 0 (the constant branch K up to lambda = 2 / K, then the logarithm), which gives the first line, and the cruder second line follows from min(c, 2R) <= c and Psi(l R) <= 1 + log_+(l R). For the square, {f_{l,c}^2 >= mu} = {|m . t| <= 2 / sqrt(mu) + c} for 0 < mu <= l^2, so sum f_{l,c}^2 <= A_R int_0^{l^2} (2 / sqrt(mu) + c + rho) dmu = A_R [4 l + (c + rho) l^2], the integral of mu^{-1/2} converging at zero without a ball cap. Certified.

## 5. Theorem A, uniform in orientation

Let M >= 2 r_E. For every edge e of A and f of B, whatever the angle between their normals,

    sum_{(m, n) in P_A(M)} |a_m^{(e)}| |b_n^{(f)}| <= 32 N_E |jump_e| |jump_f| (1 + rho / (3 M)) sqrt(U_e U_f) / (D_A D_B D M),

    U_e = 4 l_e + rho l_e^2,   U_f = 4 l_f + (r_E + rho) l_f^2,

where a_m^{(e)} and b_n^{(f)} denote the e-term and the f-term of Lemma 1, and D = (2 pi)^2 / D_A is the cell area of Lambda_A^*. Summing over the edge pairs bounds the one-sided dropped mass: the rate is 1 / M for every orientation, and this is the complete angular contribution for shared and nearly parallel normals, strips and outside alike, including the coherent pairs at a fixed offset from a shared normal line (the collaborator's example, #351).

Proof. Fix the dyadic annulus R < |m| <= 2R with R = 2^j M. For (m, n) in P_A(M) with m in the annulus, |n| >= |m| - r_E >= R - r_E >= R / 2, and |n . t_f| >= |m . t_f| - r_E, so g_f(n . t_f) <= g~_f(m . t_f). By Lemma 1 the pair weight is at most |jump_e| |jump_f| g_e(m . t_e) g~_f(m . t_f) / (D_A D_B |m| |n|) <= 2 |jump_e| |jump_f| g_e(m . t_e) g~_f(m . t_f) / (D_A D_B R^2). Each m has at most N_E partners, so the annulus contributes at most (2 N_E |jump_e| |jump_f| / (D_A D_B R^2)) sum_{annulus} g_e(m . t_e) g~_f(m . t_f), and by the Cauchy-Schwarz inequality the sum is at most sqrt(S_2^{(e)}(R) S~_2^{(f)}(R)) <= A_R sqrt(U_e U_f) = 4 (2R + rho) sqrt(U_e U_f) / D by Lemma 3. The annulus therefore contributes at most 8 N_E |jump_e| |jump_f| (2R + rho) sqrt(U_e U_f) / (D_A D_B D R^2), and with R = 2^j M, sum_j (2R + rho) / R^2 = (2 / M) sum_j 2^{-j} + (rho / M^2) sum_j 4^{-j} = (4 / M)(1 + rho / (3 M)). Certified, given Lemmas 1 to 3.

Remark. The collaborator's independent derivation (#356) gives this bound with the same constant; the two derivations differ only in the order of the steps. For the half square against itself on the unit torus (D_A = D_B = 1, l = 1 / 2, jumps 1, rho = pi sqrt 2 in radians, N_E = 1, r_E small), one edge pair with a shared normal gives 32 (1 + rho / (3 M)) (2 + pi sqrt 2 / 4) / ((2 pi)^2 M) and more, against the coherent tail 1 / (18 pi^4 M_cycles) = 2 pi / (18 pi^4 M) of the example, which the bound must and does dominate; the constant is a certificate's, and the angular route of section 8 gives a smaller one under an asymptotic remainder.

## 6. Theorem B, separated normals

Let delta = |det(t_e, t_f)| = |sin psi| > 0 with psi the angle between the two normals (equivalently between the two tangents), and let M >= max(4 r_E / delta, 2 r_E). Then

    sum_{(m, n) in P_A(M)} |a_m^{(e)}| |b_n^{(f)}| <= (256 / 3) N_E |jump_e| |jump_f| (2 + rho / M) [Q_M + (4 / 3) log 2] / (delta D_A D_B D M^2),

    Q_M = rho (l_e + l_f) + r_E l_f + 4 + 2 log_+(l_e M) + 2 log_+(l_f M),

and for M >= rho the factor (2 + rho / M) / 3 may be replaced by 1. This includes every strip and off-strip contribution and has no remainder term.

Proof. Step 1, one tangent distance is large. For any m, max(|m . t_e|, |m . t_f|) >= delta |m| / 2. Otherwise m lies within the angle arcsin(delta / 2) of both normal lines, whose angle is psi (taken in (0, pi / 2] modulo the line symmetry), so psi <= 2 arcsin(delta / 2), that is sin(psi / 2) <= delta / 2 = sin(psi / 2) cos(psi / 2), impossible for 0 < psi < pi. Step 2, the case split (the collaborator's, #353). Let (m, n) be in P_A(M) with m in the annulus R < |m| <= 2R, R = 2^j M. If |m . t_e| >= delta |m| / 2 >= delta R / 2 then g_e(m . t_e) <= 4 / (delta R) and the product of the two tangent factors is at most (4 / (delta R)) g~_f(m . t_f). If instead |m . t_f| >= delta R / 2 then |n . t_f| >= delta R / 2 - r_E >= delta R / 4 by R >= 4 r_E / delta, so g_f(n . t_f) <= 8 / (delta R) and the product is at most (8 / (delta R)) g_e(m . t_e). In both cases g_e(m . t_e) g_f(n . t_f) <= (8 / (delta R)) [g_e(m . t_e) + g~_f(m . t_f)]: the sum of the two clipped factors replaces their product, and no double angular singularity is multiplied. Step 3, the annulus. With |n| >= R / 2 and at most N_E partners per m, the annulus contributes at most (N_E |jump_e| |jump_f| / (D_A D_B R (R / 2))) (8 / (delta R)) [S_1^{(e)}(R) + S~_1^{(f)}(R)] <= (16 N_E |jump_e| |jump_f| / (delta D_A D_B R^3)) A_R [rho l_e + 2 + 2 log_+(l_e R) + (rho + r_E) l_f + 2 + 2 log_+(l_f R)] = 64 N_E |jump_e| |jump_f| (2R + rho) Q_R / (delta D_A D_B D R^3) by Lemma 3, with Q_R as displayed at radius R. Step 4, the dyadic sum. With R = 2^j M, Q_R <= Q_M + 4 j log 2, and (2R + rho) / R^3 = 2 / (4^j M^2) + rho / (8^j M^3); using sum_j 4^{-j} = 4 / 3, sum_j j 4^{-j} = 4 / 9, sum_j 8^{-j} = 8 / 7 <= 4 / 3 and sum_j j 8^{-j} = 8 / 49 <= (4 / 3)(1 / 3), the sum is at most (4 / 3)(2 / M^2 + rho / M^3)[Q_M + (4 / 3) log 2], which gives the constant 64 (4 / 3) = 256 / 3 with the factor (2 + rho / M). Certified, given Lemmas 1 to 3.

Remark. The collaborator's independent derivation (#356) gives this bound with the same constant and the same Q_M; my earlier form (research log at a757bf3) had the cruder ball term 3 pi / 2 in place of 4 and required M >= rho and M >= 1 / l, which the refinement min(2 / lambda + c, 2R) in Lemma 3 removes.

## 7. The edge-pair partition and the composed certificate

[Superseded for the count by Theorem C of section 7b, which needs no partition; kept as the disc-shaped statement.] For each edge pair take the smaller of Theorem A and Theorem B (Theorem B only where its conditions hold; as delta shrinks the condition M >= 4 r_E / delta fails first and Theorem A takes over, so nearly parallel normals degrade continuously to the 1 / M rate and no separation is assumed, only paid for). Summing over edge pairs and adding the mirror set,

    dropped mass with beat in E <= sum_{e, f} [T_{ef}(M) + T'_{fe}(M)],

with T the chosen bound on P_A(M) and T' the same on P_B(M) with the roles of the lattices exchanged (D, rho, N_E replaced by the B-side quantities). The full-response certificate composes with the pair note's cuts: cutting both sources at M and the output at reach r_E, the filtered product changes by at most that dropped mass plus the output-tail allowance exp(-R_out^2 / 4) K_{Sigma / 2} ||a||_2 ||b||_2 of GAUSSIAN-LATTICE-PAIR.md (one set, no factor two), and the retained pairs are enumerated by its neighbour box, at most N_A(M) (1 + L_B) candidates with N_A(M) <= pi (M + rho)^2 / D; a fully explicit partner bound is N_E <= pi (r_E + rho_B)^2 / D' by Lemma 2 on the B side, or the shifted integer-box count of the pair note. Zero frequencies are separate area terms and enter neither tail.

What this buys. If every edge pair of the two materials has separated normals, the dropped mass is of order log M / M^2 and M of order (C_T log(1 / epsilon) / epsilon)^{1/2} meets a budget epsilon, so the retained candidates N_A(M) (1 + L_B) are of order (C_T / epsilon) log(1 / epsilon) (1 + L_B), against the general count of order epsilon^{-2} log(1 / epsilon) of the pair note. The reach r_E enters through c_0 (the term r_E l_f), through the condition M >= 4 r_E / delta, and through L_B; since the output tail forces R_out of order sqrt(log(1 / epsilon)), r_E grows like sqrt(log(1 / epsilon)) / sqrt(lambda_min(Sigma)), and the interaction with the angle threshold delta must be carried in any assembled price: for a footprint whose reach exceeds delta M / 4 the transverse theorem does not apply at that M. If some edge pair has parallel or nearly parallel normals, Theorem A governs that pair, M is of order C_U / epsilon, and the retained count is of order epsilon^{-2} again: the general bound is valid there but gives no improved list. An improved list for shared normals needs the coherent direction represented and enumerated on its own (the pairs near the common normal line, a one-dimensional family), which this note does not do; the two parallel gratings of the one-dimensional moire are the model problem.

## 7b. Theorem C, hyperbolic regions: the angle split removed (the collaborator's, #358; verified here)

The retained set need not be a disc, and once it is edge-adapted the orientation cases disappear. For a unit tangent t and a floor h > 0 define the hyperbolic weight

    X_t(m) = |m| max(h, |m . t|),

and retain the region X_t(m) <= T, the disc of radius T / h along the normal line thinning to radius T / u at tangential distance u: a hyperbolic cross adapted to the edge (classical; the count and tail laws are those of Dung, Temlyakov and Ullrich, eq. 2.16, no novelty claimed).

Lemma 4 (count and tail). On a lattice of cell area D and covering radius rho, with c = (1 + rho / h)^2 and T >= h^2,

    N_t(T) = #{m : X_t(m) <= T} <= (4 c T / D) [1 + (1 / 2) log(c T / h^2)],
    sum_{X_t(m) > T} X_t(m)^{-2} <= (4 c / (D T)) [3 + log(c T / h^2)].

Proof. Buffering: for m in the region with |m| >= h and m' within rho of m, |m'| <= |m| (1 + rho / h) and max(h, |m' . t|) <= max(h, |m . t|) (1 + rho / h), so X_t(m') <= c X_t(m) <= c T; for |m| < h, X_t(m) = h |m| < h^2 <= T and the point lies in the disc of radius h, whose buffered version lies in the region as well. The disjoint translated cells of the counted points therefore lie in {X_t <= c T}, whose area in tangent and normal coordinates is at most 2 (2 c T / h) h for |m . t| <= h plus int_h^{sqrt(c T)} 2 (2 c T / u) du = 2 c T log(c T / h^2) for the rest (the cross-section at tangential distance u is bounded by |normal| <= c T / u rather than the exact sqrt((c T / u)^2 - u^2), so this is an upper bound and not the area), that is at most 4 c T [1 + (1 / 2) log(c T / h^2)], and dividing by D gives the count. For the tail, sum_{X > T} X^{-2} = int_T^inf 2 [N_t(tau) - N_t(T)] tau^{-3} dtau <= int_T^inf 2 N_t(tau) tau^{-3} dtau <= (8 c / D) int_T^inf [1 + (1 / 2) log(c tau / h^2)] tau^{-2} dtau = (8 c / D)[1 / T + (log(c T / h^2) + 1) / (2 T)] = (4 c / (D T)) [3 + log(c T / h^2)]. Certified.

Lemma 5 (the clipped factors under the hyperbolic weight). If h >= 2 / l_e then g_e(s) = min(l_e, 2 / |s|) <= l_e h / max(h, |s|); if h >= r_E + 2 / l_f then g~_f(s) = min(l_f, 2 / (|s| - r_E)_+) <= l_f h / max(h, |s|). Proof. For |s| <= h both sides of the first are at most l_e and the right side equals it; for |s| > h, 2 / |s| <= l_e h / |s| is h >= 2 / l_e. For the second, |s| <= h as before; for |s| > h, 2 / (|s| - r_E) <= l_f h / |s| is |s| (l_f h - 2) >= l_f h r_E, which holds at |s| = h exactly when h >= r_E + 2 / l_f and then for all larger |s|. Certified.

Theorem C. Let h_e >= 2 / l_e, h_f >= r_E + 2 / l_f, T_e >= max(h_e^2, 4 r_E^2), T_f >= max(h_f^2, 4 r_E^2), and retain in Lambda_A^* the union of the two regions X_{t_e}(m) <= T_e and X_{t_f}(m) <= T_f (the second edge's region is taken in the outer lattice, the reach r_E having carried its factor to m). Outside the union, |m| > 2 r_E, so |n| >= |m| / 2, and the pair weight of the edge pair is, by Lemma 1, Lemma 5 and 2 g h <= g^2 + h^2,

    |a_m^{(e)}| |b_n^{(f)}| <= (2 |jump_e| |jump_f| / (D_A D_B)) g_e(m . t_e) g~_f(m . t_f) / |m|^2 <= (|jump_e| |jump_f| / (D_A D_B)) [ l_e^2 h_e^2 / X_{t_e}(m)^2 + l_f^2 h_f^2 / X_{t_f}(m)^2 ],

so the omitted edge-pair mass, with at most N_E partners per m and the two tails of Lemma 4, is at most

    (4 N_E |jump_e| |jump_f| / (D_A D_B D)) [ c_e l_e^2 h_e^2 (3 + log(c_e T_e / h_e^2)) / T_e + c_f l_f^2 h_f^2 (3 + log(c_f T_f / h_f^2)) / T_f ],

c_e = (1 + rho / h_e)^2, c_f = (1 + rho / h_f)^2, and the retained region has at most N_{t_e}(T_e) + N_{t_f}(T_f) points, of order T log T. No inverse sine and no angle enters. Certified, given Lemmas 1, 4 and 5. For a shared tangent the product form is sharper: g_e h~_f <= l_e l_f h^2 / max(h, |s|)^2 with one h >= max(2 / l_e, r_E + 2 / l_f), giving at most 8 c N_E |jump_e| |jump_f| l_e l_f h^2 (3 + log(c T / h^2)) / (D_A D_B D T) for T >= max(h^2, 4 r_E^2): the collaborator's repair of the off-strip coherent pairs of #351, which the hyperbolic region contains to the radius T / u at every offset u.

What changes. The retained set of the product in the outer lattice is the union over the edges of both materials of edge-adapted hyperbolic crosses, of size of order (E_A + E_B) T log T with E the edge counts, and the omitted mass is of order log T / T; at a budget epsilon and a fixed reach this is T of order (1 / epsilon) log(1 / epsilon) and a candidate count of order (1 / epsilon) log^2(1 / epsilon) times explicit constants, for every polygonal pair, aligned edges included (with the reach growing against the budget the count is section 7d's). Theorems A and B remain true and are now the disc-shaped special cases; the partition of section 7 is no longer needed for the count. The region is a count-map object, a lattice count in a region shaped by the material's own edges rather than by the footprint, and the footprint enters only through the reach r_E (in h_f and the conditions on T) and the partner count N_E. Enumeration, the collaborator's plan: cover each region by a central rectangle and dyadic tangent bands, and enumerate each rotated rectangle by integer-index rows, solving the other coordinate's interval, at O(T log T) candidates and O(T) row overhead at a fixed lattice and floor; the constants and their growth with r_E, N_E and h against the error budget are being assembled on their side.

## 7c. The common outer union (the collaborator's assembly, #360), challenged and found sound

Construction. Take one set S in Lambda_A^*, the union over every edge e of A of {X_{t_e} <= T_e} and every edge f of B of {X_{t_f} <= T_f} (the B-edge regions in A's frequency lattice, the reach having carried their factor to m), and evaluate the whole coefficients a_m b_n for every m in S and every n in Lambda_B^* with m + n in E. No separate cut on n: S is finite and each m has at most N_E partners, so the retained pairs number at most |S| N_E <= N_E [sum_e N_{t_e}(T_e) + sum_f N_{t_f}(T_f)], and the retained sum uses whole coefficients, so the pair note's Schur contract for its rounding is untouched by the edgewise envelopes, which enter only the omission bound.

Omission bound. For m not in S, m lies outside both regions of every edge pair, so by Lemma 1 (|a_m| |b_n| <= sum_{e, f} |a_m^{(e)}| |b_n^{(f)}|) and Theorem C's pointwise inequality the omitted mass is at most sum_{e, f} of the two tails of Lemma 4 times N_E |jump_e| |jump_f| / (D_A D_B); each edge's tail appears once per partner edge.

Checks made here. (i) Theorem C's pointwise inequality needs |m| > 2 r_E for m outside the union; this follows from the conditions on T and is not automatic: if |m| <= 2 r_E then X_t(m) <= 2 r_E max(h, 2 r_E) <= max(2 r_E h, 4 r_E^2) <= max(h^2, 4 r_E^2) <= T (the middle step by cases h >= 2 r_E and h < 2 r_E), so every m with |m| <= 2 r_E lies in every region, hence in S; contrapositively m outside S has |m| > 2 r_E. (ii) Zero modes: m = 0 has X = 0 and is in S; n = 0 needs m in E, |m| <= r_E, which is in S by (i); both are exact finite terms. (iii) The B side needs no mirror cut, since every pair with m in S is retained whatever n is; the earlier two-sided formulation is unnecessary here. (iv) The widened factor uses the reach r_E of E in every direction; for an anisotropic ellipse the bound |n . t_f| >= |m . t_f| - sup_{k in E} |k . t_f| holds with the support function of E in the direction t_f, which is smaller than r_E in the ellipse's short directions and would lower h_f there; a sharpening, not a correction. (v) Nesting in the reach. X_h(m) <= T holds exactly when |m| <= T / h and |m| |m . t| <= T, so the regions are nested whenever both T and T / h are nondecreasing; a set built for a larger output-frequency reach (a smaller footprint, larger r_E, larger h_f) contains the set for a smaller one under that rule and not otherwise. For a compact family of footprints lambda_- I <= Sigma <= lambda_+ I the collaborator's answer (#365) is simpler than nesting: take one uniform output radius and the worst reach b*, floor h* and partner bound N_E* over the family, and certify one common set S* directly, without asserting that optimized individual regions nest. None of these is an objection to the construction; (i) is a lemma the note now states, (iv) and (v) are for the price.

## 7d. The budget selector and the enumeration (the collaborator's, #361 and #362; verified here)

Selector. With one common floor h = max(2 r_E, r_E + 2 / l_min, rho) (the second term is what Lemma 5 needs for the widened factor; max(2 r_E, 2 / l_min, rho) would not imply it), c = (1 + rho / h)^2 <= 4, C_0 = 4 sum_{e, f} |jump_e| |jump_f| (l_e^2 + l_f^2) / (D_A D_B D), and one common threshold T = h^2 u with u >= 1 (so T >= h^2 and T >= 4 r_E^2 by h >= 2 r_E), the omission bound of section 7c reads at most K [3 + log(c u)] / u with K = C_0 N_E c, since each Theorem C term is c l^2 h^2 [3 + log(c u)] / (h^2 u). For a source budget eta, put v = max(1, K / eta), a = 3 + log c and u = 2 v [a + log(2 v)]; then with L = a + log(2 v) >= 1, a + log u = L + log L <= 2 L, and the omitted mass is at most K 2 L / (2 v L) = K / v <= eta. Explicit and sufficient; a constant source (all jumps zero) is a separate exact case.

Growth at a fixed footprint and fixed polygons (the collaborator's reading, reproduced): the output cut forces R_out of order sqrt(log(1 / epsilon)), so r_E, h and sqrt(N_E) are of that order, K is of order log(1 / epsilon), u of order epsilon^{-1} log^2(1 / epsilon), T of order epsilon^{-1} log^3, the union count of order epsilon^{-1} log^4, the neighbour box of order log, and the candidate arithmetic of order epsilon^{-1} log^5(1 / epsilon), plus whole-coefficient acquisition and precision. Deliberately conservative, and not a per-pixel speed claim.

Enumeration of one region {X_t <= T} without scanning its bounding box. Let J = ceil(log_2(sqrt(T) / h)) and a_j = 2^j h for j = 0, ..., J - 1. In the coordinates s = m . t (tangential) and w = m . n (normal), the region lies in the central rectangle |s| <= h, |w| <= T / h together with, for each j, the two rectangles a_j <= +-s <= min(2 a_j, sqrt(T)), |w| <= T / a_j (beyond |s| > sqrt(T) the region is empty, since |m| >= |s| gives X > T). A rectangle of width w_r and height z_r contains at most (w_r + 2 rho)(z_r + 2 rho) / D lattice points by cell packing, and summing (checked here term by term, including J = 0) the candidate visits number at most

    [ 4 T (J + 1) + 4 rho h 2^J + (20 - 16 2^{-J}) rho T / h + (8 J + 4) rho^2 ] / D.

Rows: with m = B (p, q) for the lattice basis B, alpha = e_1^T B^{-1} t and beta = e_1^T B^{-1} n, a rectangle's projection on the index p has length |alpha| w_r + |beta| z_r; enumerate those integer rows and, for each p, intersect the two linear coordinate constraints as intervals in q (a zero slope is a feasibility test). The rows number at most 2 |alpha| h 2^J + (10 - 8 2^{-J}) |beta| T / h + 2 J + 1 <= (4 sqrt(T) + 10 T / h) / sigma_min(B) + 2 J + 1, either index coordinate may serve as the row, and the work is O(J + rows + visits), not the bounding-box area. Ownership: the central rectangle owns |s| <= h and band j owns a_j < |s| <= min(2 a_j, sqrt(T)); each visited point passes the exact test |m|^2 max(h^2, (m . t)^2) <= T^2, boundary points are visited at most twice per region and emitted once. For the common union over the E_A + E_B edge regions, assign each point to the first region containing it by testing the preceding regions directly, O(E_A + E_B) membership checks per candidate and no global deduplication or sort. Coefficients are evaluated once per outer point and once per partner (repeated partner acquisition is priced conservatively); index magnitudes are at most sqrt(2) T / (h sigma_min(B)); precision and certified comparisons are charged separately. This establishes the enumeration, not a GPU speed. Scope, as the collaborator's independent audit states it (#363): finite periodic polygonal pairs with fixed nonsingular lattices and a fixed positive-definite footprint; the finite contraction is an entrywise-selected Gaussian Schur matrix, so the whole-coefficient error bounds survive; repeated acquisition of a partner coefficient b_n must return a consistent approximation per n (a deterministic fixed-tolerance oracle suffices), since otherwise the error is pair-indexed and needs a different contract; per-coefficient precision targets follow from the l2 budgets and the finite index bounds, while coefficient acquisition, parameter conditioning, arithmetic precision and certified boundary comparisons are explicit extra costs; the edge and anisotropy constants can be large. It is not a GPU result and not a general shader theorem.

The price, read honestly. The selector's counts are envelopes and upper bounds, not enumerated cardinalities: for two half-square masks on unit cells (radian units, N_E = 1, source budget 1 / 512) at the reach b = 1.2566 radians per cell, which is the disc radius 0.2 cycles of the pair-count probe, the floor is h = 5.257, c = 3.405, C_0 = 0.8106, K = 2.7598, u = 3.440e4, T = 9.505e5, the omitted mass 1.177e-3, and the common union holds at most 2.241e6 points per edge region, at most 8.965e6 over the four distinct tangent directions (the square's opposite edges share a tangent, so the eight-edge sum 1.793e7 counts each region twice) (budget-selector-example.mjs, output read before this line); at a budget 1 / 8192 and reach 0.2 radians the four-region bound is 2.205e8, at reach 3 radians with N_E = 4 it is 4.507e7. Against this, the pair-count probe (beat-pair-count.mjs) found that for the same masks a few dozen pairs keep the dropped weight within its enumerated radius M = 256 below 1 / 256 and 1 / 4096; that figure controls only the mass inside radius 256 and includes no remainder beyond it, so it is not a matched total-error comparison with the selector, only an indication of how far the envelope sits above the coefficients (the square's separable zeros above all). A certified short list is priced as follows: for a certified finite pair list P and a shorter prefix Q of it, the total error is the outside-P bound plus sum_{P minus Q} |a_m b_n| w_Sigma(m + n) plus the numerical error; sorting does not transfer the outside bound to the prefix, the omitted finite weights need certified upper bounds of their own, and the sorting and storage are additional costs. The product this points to is therefore a compile-time list sorted by certified weight with the selector on its outside and the prefix sum on its inside, built by the enumeration above without scanning boxes.

## 7e. Indexing by classes, what it is and is not, and block sums along a shared direction

Lemma 6 (class indexing; not a compression). Let L be the linear map with Lambda_B^* = L Lambda_A^* (it exists for any two lattices). Every pair (m, n) with n = L m' has beat k = m + n = (I - L) m + L delta, delta = m + m' in Lambda_A^*, and the classes delta partition the pairs. (a) If I - L is invertible, each pair is recovered from its class and beat, m = (I - L)^{-1}(k - L delta), n = L (delta - m); the class beat sets (I - L) Lambda_A^* + L delta are shifted lattices, but they are disjoint only as labelled pair classes and not as sets of beats: for L = 2 I every class has beat set Z^2, and coincident beats from different classes sum coherently (the commensurate case, Lambda_B^* subset of Lambda_A^*). Beats are unique exactly when Lambda_A^* and Lambda_B^* meet only at zero, which is a different condition from the invertibility of I - L. (b) If I - L is singular its image need not be a discrete line lattice: for L = [[1/2, -sqrt 2], [0, 1]], invertible, (I - L) Z^2 = {(p / 2 + sqrt 2 q, 0)} is dense on a line; and for L = I every class k = delta contains the full convolution over m. (c) One coefficient evaluation per class point is one per pair: the indexing organizes the pairs and removes no evaluation; a class can be dropped under the region certificate only when every pair of it with beat in E lies outside the region, or its extra mass is bounded separately; and no fixed class list stays valid when L changes, without a family certificate. [After the collaborator's audit, #370; my earlier statement of a disjoint union of beat sets and of a "compression" is withdrawn.]

Block sums along a shared direction (proposal, the identities exact, the costs and certificate to be audited). Write x = (x_1, x_2) in coordinates where the shared direction is x_2 and both materials have the period 1 in x_2 (a direction in which the two lattices are commensurate; after a common affine change this is the case of interest, the incommensurate directions being where decomposition is unique and nothing sums). The partial coefficient functions A_d(x_1) = int_0^1 A(x_1, x_2) e^{-2 pi i d x_2} dx_2 = sum_p a_{(p, d)} e^{2 pi i p x_1} carry the whole spectrum of A on the line of tangential index d, and the same for B. For a beat k = (kappa, k_2) the sum over all pairs with tangential indices d and k_2 - d is

    sum_p a_{(p, d)} b_{(kappa - p, k_2 - d)} = the kappa-th Fourier coefficient of the product A_d(x_1) B_{k_2 - d}(x_1),

a one-dimensional closed form: for piecewise-constant sources on polygons, A_d is piecewise a sum of exponentials of linear functions of x_1 (the chord integrals between the polygon's edges), with breakpoints at the x_1-projections of the vertices, so the product's coefficient is an integral of products of exponentials over O(V_A + V_B) intervals, at a cost independent of the number of harmonics p summed. When the x_1 spacings are incommensurate as well, the sum over p has at most one term and the identity is the unique decomposition. The sum over d is truncated with the directional heat content (section 8 of the research log): the energy of A on the lines |d| > D is at most c* V_{t} / D with V_t the variation of A along the shared direction's normal, so the omitted blocks are certified by that law and the block count at a budget is of order sqrt(1 / epsilon) in |d| times the beats in E. This is the projection-slice structure of polygonal spectra (a line of the spectrum is the spectrum of a partial coefficient function), and it is where the coherent one-dimensional moire of two aligned materials is summed without enumerating its harmonics; the same-lattice case is the case where every direction is shared and the identity reproduces the refinement's exact coefficients by separation of variables. The spatial identity, the Gaussian integral over the cells of the arrangement, is the block sum of all pairs at once, cheap when the footprint holds few cells.

Preparation, restated. The pair coefficients a_m b_n do not depend on the pixel, so a certified list is prepared once per material pair: the pairs of the disc region are evaluated exactly (for two half squares at a source budget 1 / 512 about 5.5e5 of them, from schur-pair-certificate.mjs's printed 6.398e-3 at M = 128 scaling as 1 / M), sorted, and the absolute sum of every pair beyond the retained prefix is computed exactly, which is the certified omitted weight that section 7d's prefix formula asks for; per patch of pixels only the prefix enters the moment state, so the per-patch preparation is the prefix times O(n^2) and the per-pixel work is the moment recurrence. The compile-time count per material pair is the certified region's, the envelope's constants deciding it, and the block sums above reduce it along shared directions; the collaborator's point stands that this is the price of one construction and not a lower bound.

## 7f. Acquiring the moment state from the arrangement: an alternative acquisition with its own moment error (proposal for audit; corrected after #373)

The collaborator's response state (REUSABLE-PAIR-RESPONSE.md, f167c5a, sections 4 and 5) is prepared from a certified pair list P = {(m, n) : m in S, |k| <= b}: H_alpha = sum_P d_{mn} (k / b)^alpha with d_{mn} = a_m b_n exp(-k^T Sigma_0 k / 2 + i k . mu_0), Sigma_0 = lambda_- I the smallest footprint of the family and mu_0 the patch centre. The moments also have a spatial form that needs no pairs, and this section states it as a different acquisition with its own error, not as the pair list's certificate transferred.

Identity. Let I(mu) = int (A B)(x) G_{Sigma_0}(x - mu) dx be the filtered product, whose Fourier series is sum over all pairs of a_m b_n exp(-k^T Sigma_0 k / 2) e^{i k . mu}. Since k^alpha e^{i k . mu} = (-i partial_mu)^alpha e^{i k . mu}, the full-spectrum moment is H_alpha^{full} = b^{-q} (-i partial_mu)^alpha I(mu_0), q = |alpha|, and with y = x - mu_0 and partial_mu G(x - mu) = -(partial_y G)(y),

    H_alpha^{full} = b^{-q} i^q int (A B)(x) (partial_y^alpha G_{Sigma_0})(x - mu_0) dx

[sign corrected after #373: i^q, not (-i)^q]. For piecewise-constant sources on polygons, A B is piecewise constant on the arrangement of the two edge families within the spatial reach, whose cells are polygons, so H_alpha^{full} = b^{-q} i^q sum over the cells C within the reach of v_C int_C partial^alpha G_{Sigma_0}(x - mu_0) dx, each term a Gaussian times a polynomial of degree q integrated over a polygon. Every pair interaction is summed inside its cell coherently, with no enumeration, no envelope and no coefficient acquisition.

Moment error, three parts. (i) The spectrum outside the certified list. H^{full} - H^{P} = sum over pairs with m not in S and |k| <= b, plus sum over pairs with |k| > b. The first has (|k| / b)^alpha <= 1 and Gaussian weight at most one, so it is at most the region cut's omitted mass eta per moment. For the second, reserve half the Gaussian weight for the Schur bound: with |k| = t b, t > 1, G_0(k)^{1/2} = exp(-lambda_- |k|^2 / 4) = exp(-R^2 t^2 / 4) and (|k| / b)^q <= t^{2 n}, so each term is at most |a_m b_n| G_0(k)^{1/2} sup_{t > 1} exp(-R^2 t^2 / 4) t^{2 n}, and summing with the Schur bound for the weight exp(-k^T (Sigma_0 / 2) k / 2),

    |second part| <= K_{Sigma_0 / 2} ||a||_2 ||b||_2 sup_{t > 1} exp(-R^2 t^2 / 4) t^{2 n},   sup = exp(-R^2 / 4) if R^2 >= 4 n, (4 n / (e R^2))^n otherwise

[corrected after #373: the supremum sits at the boundary t = 1 when R^2 >= 4 n, and the Gaussian weight is not used twice]. (ii) The spatial tail. The kernel partial^alpha G is signed and Hermite-weighted, |partial^alpha G_{Sigma_0}(y)| <= G_{Sigma_0}(y) P_q(|y| / sqrt(lambda_-)) lambda_-^{-q/2} with P_q a polynomial of degree q, so the cells beyond a spatial radius r sqrt(lambda_-) are dropped with error at most b^{-q} lambda_-^{-q/2} int_{|z| > r} G_I(z) P_q(|z|) dz = R^{-q} int_{|z| > r} G_I(z) P_q(|z|) dz, an absolute (not positive-mass) tail whose required r grows with the moment order: r of order R plus a term of order sqrt(n) makes it small, with the polynomial factor explicit once P_q is written (Hermite coefficients). (iii) The base oracle. Each cell term is a Gaussian-polynomial integral over a polygon; the base oracle is the kernel's existing one, edge integrals of the bivariate normal (the panels of the disc and quadratic regions) with the moment recurrences of GABOR-WEIGHTED-MOMENTS.md for polynomial weights, at a cost per edge and per moment order to be stated with the recurrence's stability and precision; an identity is not a finite cheap evaluation until that is priced. The per-moment acquisition error eta_H^{spatial} = (i) + (ii) + (iii) enters the collaborator's contract as eta_H, amplified by the query's coefficient bound e^{A_q} of their equation (15), and not as a one-time addition to eps_K [corrected after #373].

Price, with the reach counted. Within a spatial reach of r_s periods around mu_0 the arrangement holds the translated copies of the authored edges, E_reach of order (E_A + E_B) r_s^2 per unit period area, and the crossings I of order E_A E_B r_s^2 rho_X with rho_X the crossing density per period area; the plane sweep costs O((E_reach + I) log(E_reach + I)) once per relative placement of the sources (a control that moves one layer against the other changes I and the cells, the collaborator's section 7 in spatial form); the cells number of order r_s^2 rho_X, and each cell carries (n + 1)(2 n + 1) moment integrals with O(edges of the cell) base-oracle calls each. Over a family from the near field to the horizon, a dyadic pyramid of reference footprints at scales s_l periods with patches of centres of size L_mu comparable to s_l: the patches number of order (texture extent / s_l)^2, the reach r_s is of order s_l (R + sqrt n), so a level costs of order (texture area in periods) rho_X (R + sqrt n)^2 (n + 1)(2 n + 1) cell integrals and (texture area)(E_A + E_B + E_A E_B rho_X)(R + sqrt n)^2 log for the sweeps, the same at every level, and the pyramid that times the number of levels; levels whose footprint is below one or two periods need no moment state, the exact-cell branch of the single-material kernel serving them directly. These are orders with the reach and moment-order factors shown; no constant is claimed small, and the storage of (n + 1)(2 n + 1) complex entries per patch per level is large at low levels.

Admission and scope: two sources piecewise constant on polygonal partitions of their period cells, at fixed relative placement per prepared pyramid; a footprint family covered by levels and patches; the query the collaborator's O((n + 1)^3) recurrence with its conditioning bound. What survives: everything, the acquisition being exact up to the three errors above; what is truncated: the spatial tail of the Hermite kernel at the reach and the query's Taylor remainder; what is charged: the sweep, the cells, the moments per cell, the base oracle, the storage, and the rebuild of the affected patches when a control moves a layer. The frequency route (sections 7c to 7e) remains the acquisition where the arrangement is unavailable and the only route for sources that are not piecewise constant on polygons; the collaborator notes that the common fast-phase arc-mask family already has a carrier-independent block oracle in SHARED-PHASE-AVERAGING and SHARED-PHASE-COST, so the open target is broader two-dimensional source and control structure, not that family's overlap law again.

## 7g. The orthonormal Hermite state (the collaborator's, #375), audited here

State. Whiten the reference Gaussian gamma = N(mu_0, Sigma_ref) to N(0, I) by z = Sigma_ref^{-1/2}(x - mu_0), write F(z) for the product material A B in these coordinates (values in [0, 1]), and let h_alpha = He_alpha / sqrt(alpha!) be the orthonormal Hermite functions of L^2(gamma). The state is H_alpha = E_gamma[F h_alpha] for total degree |alpha| <= N, and Bessel gives sum_alpha |H_alpha|^2 <= E_gamma[F^2] <= 1.

Query. A pixel's Gaussian N(m, Q) in whitened coordinates, Q = I + D, has density ratio r = dN(m, Q) / dgamma, and its response is E_{N(m, Q)}[F] = E_gamma[F r] = sum_alpha H_alpha c_alpha with c_alpha = E_gamma[r h_alpha] = E_{N(m, Q)}[h_alpha], valid when r is in L^2(gamma), which holds for ||D|| <= r_0 < 1 (Q < 2 I). Checked here: the generating function sum_alpha t^alpha He_alpha(z) / alpha! = exp(t . z - |t|^2 / 2) gives E_{N(m, Q)} exp(t . z - |t|^2 / 2) = exp(t . m + t^T D t / 2), so the scaling c_alpha(sqrt w m, I + w D) = w^{|alpha| / 2} c_alpha(m, Q) is the substitution t -> sqrt w t; and S(w) = sum_alpha w^{|alpha|} |c_alpha|^2 = ||r_w||^2_{L^2(gamma)} for the ratio of N(sqrt w m, I + w D), which by direct Gaussian integration is det(I - w^2 D^2)^{-1/2} exp[w m^T (I - w D)^{-1} m] (verified in one dimension: the integral of N(m', 1 + w d)^2 / phi is (1 - w^2 d^2)^{-1/2} exp[w m^2 / (1 - w d)]). Hence, for w > 1 with w r_0 < 1, the truncation to total degree N has response error at most (sum_{|alpha| > N} |H_alpha|^2)^{1/2} (sum_{|alpha| > N} |c_alpha|^2)^{1/2} <= sqrt(S(w)) w^{-(N + 1) / 2}, uniformly S(w) <= (1 - w^2 r_0^2)^{-d / 2} exp[w L^2 / (1 - w r_0)] for ||m|| <= L, d = 2. Certified, given the collaborator's statement and these checks.

Errors in l2. An acquired state with l2 error eta changes the response by at most eta ||c||_2 = eta sqrt(S(1)), bounded for the query family with no monomial amplification e^{A_q}. Truncating the source spatially to |z| <= R changes the state by the coefficients of F 1_{|z| > R}, of l2 norm at most ||F 1_{out}||_{L^2(gamma)} <= sqrt(P_gamma(|z| > R)) = exp(-R^2 / 4) in two dimensions, simultaneously for every degree: the Hermite-tail obstacle of section 7f disappears when the error is measured collectively, which is the right measure for this state. The base oracle's per-coefficient errors eps_alpha combine as eta = (sum eps_alpha^2)^{1/2}.

Acquisition. From the arrangement (section 7f, now with the orthonormal weights): H_alpha = sum over the cells within radius R of v_C int_C h_alpha dgamma, Gaussian times Hermite polynomial over a polygon, all degrees up to N from one recurrence over each cell's edges, the cells sharing edges; the geometry and base-oracle costs of 7f apply and remain to be stated. From characters, for a single periodic material or any retained character list: since E_gamma[e^{i k . x} h_alpha] = e^{i k . mu_0} exp(-|k~|^2 / 2) (i k~)^alpha / sqrt(alpha!) with k~ = Sigma_ref^{1/2} k, the state is H_alpha = sum_k f^_k e^{i k . mu_0} exp(-|k~|^2 / 2) (i k~)^alpha / sqrt(alpha!), the count theorem's characters supplying it directly; a caution: a single character has l2 norm exactly one in the full Hermite basis (sum_alpha exp(-|k~|^2) |k~|^{2 |alpha|} / alpha! = 1), its mass sitting at degrees near |k~|^2, so omitting characters is harmless only for the truncated state, whose low-degree coefficients of an omitted character are exp(-|k~|^2 / 2) |k~|^{|alpha|} / sqrt(alpha!), small when |k~|^2 is well above N; the ellipse of the count theorem must therefore be taken at a reach that grows with sqrt N, a bound to be written.

Prior art and attribution (the collaborator's finding, #376). The Gaussian density-ratio norm and the weighted norm S(w) above are Theorems 6.1 and 7.1 (equation 11) of Jae Wan Shim, Parameter-Space Heat Flow, Gaussian Density Ratios, and Sharp Hermite Truncation Rates (July 2026 preprint, arXiv 2607.07712), with t^2 = w, and Theorem 8.1 there gives the sharp root rate sqrt(||Q - I||); the contribution here is the bounded-material query application, the collective spatial-truncation bookkeeping by Bessel, and the cost distinctions, not the identity. The collaborator documents the application in GAUSSIAN-QUERY-STATE.md; its numerical contract is preferred within an admitted patch without a claim that it dominates the finite-band monomial cache everywhere.

Why the admission is a band and not a one-sided range (the collaborator's argument, #376). For a periodic stripe F = A^2 = A with mu_0 inside a stripe, the heat response f(lambda) tends to one with an exponentially small correction as lambda tends to zero, so it is not analytic at zero, and its Taylor series in the covariance at lambda_0 has radius at most lambda_0; the full-source Hermite state at a reference Sigma_ref reproduces that series for Delta mu = 0 and cannot converge for a covariance increment beyond the reference, which is what ||D|| <= r_0 < 1 says. The finite-band cache avoids this by being an entire function of the covariance. Consequently a smaller reference footprint worsens both convergence and conditioning, and the natural direction is a larger reference with an admitted local patch [my suggestion of a smaller reference in #374 is withdrawn].

Query arithmetic (the collaborator's, #376, independently checked on their side). The normalized coefficients obey c_{alpha + e_i} = [m_i c_alpha + sum_j D_ij sqrt(alpha_j) c_{alpha - e_j}] / sqrt(alpha_i + 1), c_0 = 1, so a canonical predecessor per target computes all K_N = binom(N + d, d) coefficients in O(d K_N) and the contraction costs O(K_N); with state l2 error eta_H and coefficient l2 error eta_c the error is at most sqrt(S(1)) eta_H + eta_c + eta_H eta_c plus the summation error, and finite-precision accuracy of the recurrence still needs its certificate. On a fixed admitted patch the degree is N = O(log(1 / epsilon)), so state and query are O(log^2(1 / epsilon)) in two dimensions, with no claim about acquisition.

Admission and degree. The query family is a band around the reference, covariances within the factor (1 +- r_0) of Sigma_ref in every direction and centres within L in whitened units, with w r_0 < 1 for the truncation rate w^{-(N + 1) / 2}; a pyramid of references at covariance ratio 2 can be centred to r_0 = 1 / 3. The degree must be chosen from the full bound sqrt(S(w)) w^{-(N + 1) / 2}, whose prefactor diverges as w r_0 approaches one and grows exponentially with the centre offset L (optionally halved for F in [0, 1] and N >= 0, since the omitted source energy is at most Var(F) <= 1 / 4), and not from the root rate alone; the earlier sentence here that read eight bits into N = 12 and 91 coefficients from the rate is withdrawn as an inference the bound does not make until the patch size and every budget are specified [after #378]. Near-degenerate covariances and wide centre ranges cost through S; independent material controls still need extra state (the collaborator's section 7). This is the collaborator's candidate for a clean universal final-query contract; it claims neither cheap preparation nor general composition.

The arrangement oracle on the normalized state, with its arithmetic as orders (hermite-oracle-cost.mjs, output read before this paragraph; a construction's operation counts, not measurements). Per cell C, the base Gaussian measure by the kernel's edge panels (of order a hundred operations per edge), then every Hermite moment up to total degree N by the divergence identity int_C He_{alpha + e_i} phi = - sum over the edges of C of n_i int_e He_alpha phi ds, each edge integral a one-dimensional Gaussian-moment contraction: the truncated moments M_j of the Gaussian along the edge for j <= N by the three-term recurrence, and the Hermite polynomial along the edge expanded to its degree by a recurrence in alpha, O(|alpha|) per coefficient per edge; per edge about 931 operations and per cell of five edges about 4655 at N = 12 (K_N = 91). Under a reference footprint of reach 4 to 5 standard deviations at a level of std s periods the cells number pi (R_s s)^2 rho_X, about 800 to 1300 at s = 1 with rho_X = 16 crossings per period area, and the patches (T / s)^2 for a texture of T periods on a side, so a level costs about 3.7e12 to 5.9e12 operations for T = 1000, the same at every level, with storage of 91 complex coefficients per patch, about 1.5 GB at s = 1 down to 1 MB at s = 32; the frequency-side acquisition from a retained pair list of |P| pairs costs |P| K_N per patch, 4.6e9 over the million patches for |P| = 50 (the measured order, uncertified) and 5.0e13 or 8.2e14 for the certified lists of 5.5e5 or 9.0e6 pairs. The reading: the arrangement route is exact and its cost is these counts (feasibility is not asserted from them; see the scope paragraph below), the frequency route with the actual list is cheap and its certificate is the envelope's, and neither is a run-time cost. The coefficients are real, so the storage figures are 8 bytes each, half of what the script printed.

The all-order edge recurrence (the collaborator's, #380; signs and recurrence derived independently here). In whitened coordinates let an oriented edge be z(u) = a n + u t with (n, t) orthonormal, u in [u_0, u_1], and let I_alpha = int_edge gamma h_alpha ds, B_alpha = [gamma h_alpha](u_1) - [gamma h_alpha](u_0), I_0 = phi(a)[Phi(u_1) - Phi(u_0)]. From the multiplication identity z_i h_alpha = sqrt(alpha_i + 1) h_{alpha + e_i} + sqrt(alpha_i) h_{alpha - e_i} (the probabilists' identity divided by sqrt(alpha!)), v_i := sqrt(alpha_i + 1) I_{alpha + e_i} = int z_i h_alpha gamma - sqrt(alpha_i) I_{alpha - e_i}, and since n . z = a on the edge, n . v = a I_alpha - sum_j n_j sqrt(alpha_j) I_{alpha - e_j} =: R_alpha. From the derivative identity partial_i(h_alpha gamma) = -sqrt(alpha_i + 1) h_{alpha + e_i} gamma (the probabilists' identity partial_i(He_alpha phi) = -He_{alpha + e_i} phi, normalized), v_i = -int_edge partial_i(h_alpha gamma) ds, so t . v = -int_{u_0}^{u_1} (d / du)[h_alpha gamma](z(u)) du = -B_alpha. Hence v = R_alpha n - B_alpha t and

    I_{alpha + e_i} = [n_i R_alpha - t_i B_alpha] / sqrt(alpha_i + 1),

one predecessor per multi-index, all moments through degree N in O(K_N) = O(N^2) per edge, the endpoint values by the Hermite recurrence in O(N^2), no division by a tangent component. Both derivations agree with the collaborator's. For a compact piecewise-constant F with signed jumps J_e = F_+ - F_- across the edge's normal n_e pointing from the minus to the plus side, the distributional gradient is sum_e J_e n_e delta_e, and integrating by parts with h_alpha gamma = -(1 / sqrt(alpha_i)) partial_i(h_{alpha - e_i} gamma) for alpha_i > 0 gives H_alpha = (1 / sqrt(alpha_i)) sum_e J_e n_{e, i} I^{(e)}_{alpha - e_i}, the shared cell boundaries combined before acquisition and the spatial window's boundary included as an actual jump to zero; H_0 = -sum_e J_e n_{e, 1} int_e Phi(z_1) phi(z_2) ds from the field (Phi(z_1) phi(z_2), 0) whose divergence is gamma, a one-dimensional Gaussian-CDF base oracle per edge. The acquisition price is therefore O(E_split N^2) plus E_split base-oracle calls, plus the arrangement and the precision, with E_split every split translated boundary segment in the spatial window; no Fourier recipe count enters, and this supersedes the per-cell count of the paragraph above. A precision remark for the certificate still owed: the upward recurrence multiplies an error in I_alpha by factors of order (|a| + 2 sqrt N) / sqrt(alpha_i + 1) per step (the coefficients a, n_j sqrt(alpha_j), t_i, and the division), so the amplification along a chain of N steps must be bounded for the chosen predecessor order, which should raise the largest index; the terms themselves are bounded by Cramer's bound on the Hermite functions, |h_alpha gamma| <= C gamma^{1/2}, so the absolute errors are additive with bounded coefficients and the question is their product along the chain.

The base probability and the forward error recurrence (the collaborator's, #382; verified here). For an edge n_1 z_1 + n_2 z_2 = a with n_1 != 0, s = sign(n_1) and v_0 < v_1 its endpoint z_2 coordinates in increasing order, the edge's contribution to H_0 is -J_e s [Phi_2(v_1, s a; s n_2) - Phi_2(v_0, s a; s n_2)], Phi_2(x, y; rho) the bivariate normal distribution function with correlation rho; an edge with n_1 = 0 contributes nothing. Derivation: along the edge z_1 = (a - n_2 z_2) / n_1 and ds = |dz_2| / |n_1|, so n_1 ds = s dz_2 in the increasing direction, and the conditional identity Phi_2(x, y; rho) = int_{-inf}^x Phi((y - rho z) / sqrt(1 - rho^2)) phi(z) dz applies with y = s a, rho = s n_2, sqrt(1 - rho^2) = |n_1|, since (a - n_2 z_2) / n_1 = (s a - s n_2 z_2) / |n_1|. No division by n_1 appears; a near-degenerate correlation still needs a certified Phi_2, and an absolute error xi in it contributes at most 2 |J_e| xi per edge plus arithmetic. The truncation window is a whitened square containing the disc of radius R, polygonal, with Bessel's tail bound exp(-R^2 / 4) unchanged. For the upward chain, raising the largest target index beta_i (so sqrt(beta_i) >= sqrt(q / 2) at order q = |beta|), the largest error delta_q among the edge moments of order q with exact geometry obeys

    delta_q <= |a| sqrt(2 / q) delta_{q - 1} + sqrt(2 (q - 1) / q) delta_{q - 2} + sqrt(2 / q) deltaB_{q - 1} + tau_q,

with deltaB the endpoint-value error, tau_q the certified local arithmetic error and missing negative orders zero (checked: |n_i|, |t_i| <= 1, the lowered moments' coefficients sum by Cauchy-Schwarz to at most sqrt(q - 1), and the division by sqrt(beta_i) contributes the sqrt(2 / q)); a usable forward error recurrence, not a statement that ordinary floating point suffices. Conditional sensitivity illustrations from it (edge-chain-amplification.mjs, output read before this paragraph; the recursion evaluated in ordinary double precision, which is not an outward-rounded bound, with no seed, endpoint, local, geometry, assembly, state-norm or query allowance priced) [relabelled twice after #402 and #403: the factors are one conditional input to a certificate, not the certificate]. Homogeneous amplification A_q(a), the factor by which an error in the seed I_0 can appear in the moments of order q with all other sources zero: at |a| = 0 it is 1.58, 3.80, 9.74 and 25.7 at q = 6, 12, 18 and 24 (the two-back coefficient tends to sqrt 2, so the growth is of order 2^{q / 4}); at |a| = 1 it is 11.7, 90.7, 577 and 3280; at |a| = 3 it is 535, 3.77e4, 1.38e6 and 3.52e7; at |a| = 5 it is 6.80e3, 3.14e6, 5.56e8 and 5.64e10. The zero-seed response to a unit local error at every order q >= 1 is 13.5, 121, 1.29e3, 1.21e4 and 5.41e5 at q = 12 for |a| = 0, 1, 2, 3 and 5. Multiplying the seed amplification by the binary64 and binary32 unit roundoffs 2^{-53} and 2^{-24} on a unit-size seed (4.2e-12 and 2.2e-3 at order 12 for |a| = 3; 3.5e-10 and 0.19 for |a| = 5) illustrates the scale of one propagation path and nothing more: a certificate for a stated tolerance needs the actual seed primitive error, the endpoint error deltaB (with Cramer's bound on h_alpha gamma and the endpoint evaluation's own error), the per-operation local errors tau_q with the magnitudes of the intermediate quantities, the geometry and the jump assembly, the collective state norm and its query amplification sqrt(S(1)), none supplied here; and since an upper bound that is loose cannot show that another precision or algorithm fails, the binary32 figures say only that this bound does not certify binary32 at these parameters. Withdrawn as inferences the majorant does not establish: that the recurrence exhibits minimal-solution behaviour, that the raw-moment route loses the same order by cancellation, and that node-wise evaluation of the normalized Hermite functions is a relatively stable route (their recurrence is not uniformly relatively stable near the functions' zeros; that alternative would need an absolute evaluation bound plus quadrature and summation errors). What stands: the forward recurrence is a valid conditional error-propagation tool, and the next useful result is either to instantiate all its inputs for a stated source, reach, order and tolerance, or to derive a bound that uses the actual Gaussian decay of the seeds and endpoints instead of unit-size forcing; node-wise evaluation remains a candidate for the curved-boundary oracle with its budgets to be established, and it is the same oracle as for curved boundaries.

Lemma 8 (closed form of the homogeneous amplification, and its Gaussian-weighted bound uniform in the offset). Let x_q solve x_q = |a| sqrt(2 / q) x_{q - 1} + sqrt(2 (q - 1) / q) x_{q - 2} with x_0 = 1, x_1 = |a| sqrt 2 (the seed path of the forward recurrence with the largest-index predecessor order). With y_q = x_q sqrt(q!) 2^{-q / 2} the recurrence becomes y_q = |a| y_{q - 1} + ((q - 1) / sqrt 2) y_{q - 2}, y_0 = 1, y_1 = |a|, which is the Hermite recurrence with the sign of the second term reversed and its coefficient scaled, whose solution is the positive-coefficient polynomial y_q = sum_{k = 0}^{floor(q / 2)} [q! / (k! (q - 2 k)!)] 2^{-3 k / 2} |a|^{q - 2 k} (the polynomials P_q(x) = sum_k [q! / (k! (q - 2 k)!)] c^k x^{q - 2 k} satisfy P_q = x P_{q - 1} + 2 c (q - 1) P_{q - 2}, and 2 c = 1 / sqrt 2). Hence

    A_q(a) = x_q = (2^{q / 2} / sqrt(q!)) sum_{k = 0}^{floor(q / 2)} [q! / (k! (q - 2 k)!)] 2^{-3 k / 2} |a|^{q - 2 k},

checked against the recurrence to the printed precision at q = 6, 12, 18, 24 and |a| = 0, 3, 5 (edge-chain-closed-form.mjs; at |a| = 0 only the term k = q / 2 survives, so A_q(0) = 2^{-q / 4} sqrt(q!) / (q / 2)! for even q, 3.7997 at q = 12, and A_q(0) = 0 for odd q), the factor for m = q - 2 k = 0 being one throughout [audited, #408]. Since the seed I_0 = phi(a)[Phi(u_1) - Phi(u_0)] carries the factor e^{-a^2 / 2} and the endpoint values h_alpha gamma carry e^{-(a^2 + u_k^2) / 4} by Cramer's bound, the amplification that matters is Gaussian-weighted, and sup_a |a|^m e^{-a^2 / 4} = (2 m / e)^{m / 2} gives the bound uniform in the offset

    sup_a A_q(a) e^{-a^2 / 4} <= (2^{q / 2} / sqrt(q!)) sum_k [q! / (k! (q - 2 k)!)] 2^{-3 k / 2} (2 (q - 2 k) / e)^{(q - 2 k) / 2},

an explicit finite sum, equal to 64.5, 8.55e3, 1.22e6 and 1.81e8 at q = 6, 12, 18 and 24 in ordinary doubles (the actual maximum over |a| <= 8 being 56.5, 7.57e3, 1.09e6 and 1.61e8, so the bound is within fifteen percent); its exponential rate is lim U_q^{1 / q} = sqrt(4 + sqrt 2) = 2.3268 (the collaborator's Stirling computation with k / q tending to 1 / (2 + 4 sqrt 2), #408; checked here by log-space evaluation: U_q^{1 / q} = 2.126, 2.208, 2.259, 2.288, 2.305, 2.315 and 2.320 at q = 12, 24, 48, 96, 192, 384 and 768, increasing toward it), and the true supremum has the same rate since max_k sup_a term_k <= sup_a sum_k term_k <= U_q <= (floor(q / 2) + 1) max_k sup_a term_k; the finite sum is the statement and the rate a remark [the phrase "of order (2.2)^q" is withdrawn as an asymptotic claim, #408]. With the seed's stronger factor e^{-a^2 / 2} the same sum with (m / e)^{m / 2} applies and is smaller. The forcing paths (a local error tau_j or an endpoint error deltaB_{j - 1} entering at order j) propagate by the same recurrence from order j onward, but their transfer T_{q, j} is not A_{q - j}, because the recurrence's coefficients depend on the absolute order (at a = 0 a unit impulse at j = 1 gives T_{3, 1} = sqrt(4 / 3) while A_2(0) = 1, the collaborator's example, #408); T_{q, j} is the solution of the recurrence with its absolute-index coefficients started by a unit at order j, a polynomial in |a| with positive coefficients to be derived on its own, after which the same monomial maximization applies [derived as Lemma 8b below]. The Gaussian factor may be attached to a forcing term only once that term's absolute error bound is supplied: the decay of the exact seed and endpoint values does not by itself give their numerical errors the same envelope [after #408]. What remains for a certificate, unchanged: the primitive errors of the seed and of the endpoint evaluations, the local errors with their intermediate magnitudes, the geometry and jump assembly, the state norm and the query amplification, all for a stated source, reach, order and tolerance; the lemma is a conditional propagation result for the seed path, exact in closed form, and under those conditions it turns the growth in the offset into a growth in the order alone. The state errors are then summed with the absolute jump weights and converted to an l2 allowance eta_H. The normalized state is real, not complex (F, h_alpha and gamma are real), so the storage figures above halve [corrected after #382].

Lemma 8b (the forcing transfers, exact and bounded). Let T_{q, j}(a), 1 <= j <= q, be the solution of the recurrence of Lemma 8, x_q = |a| sqrt(2 / q) x_{q - 1} + sqrt(2 (q - 1) / q) x_{q - 2}, started by x_j = 1 and x_{j - 1} = 0: the factor by which a unit error entering at order j (a local error tau_j, or an endpoint error deltaB_{j - 1} through its coefficient sqrt(2 / j)) can appear at order q with every other source zero. Since every coefficient is nonnegative, the forward error recurrence gives delta_q <= A_q(a) delta_0 + sum_{j = 1}^{q} T_{q, j}(a) [sqrt(2 / j) deltaB_{j - 1} + tau_j], the seed error delta_0 entering as x_0 and propagating through x_1 = |a| sqrt 2 x_0 as in Lemma 8. In the rescaled variable y_q = x_q sqrt(q!) 2^{-q / 2} the recurrence is y_q = |a| y_{q - 1} + ((q - 1) / sqrt 2) y_{q - 2}, and its solution started by y_j = sqrt(j!) 2^{-j / 2}, y_{j - 1} = 0 is a sum over the coverings of the orders j + 1, ..., q by monomers (one order at a time, weight |a|) and dominoes (two orders at a time, the domino landing at order l carrying the weight (l - 1) / sqrt 2):

    T_{q, j}(a) = 2^{(q - j) / 2} sqrt(j! / q!) sum_{k = 0}^{floor((q - j) / 2)} 2^{-k / 2} e_k(j, q) |a|^{q - j - 2 k},

where e_k(j, q) is the sum, over the k-subsets {u_1 < ... < u_k} of {j + 1, ..., q - 1} whose consecutive elements are at least two apart, of the products u_1 ... u_k, a positive integer (u_i is the left end of the i-th domino). At j = 0 the subsets run over {1, ..., q - 1} and the sum is q! / (k! (q - 2 k)! 2^k), which recovers Lemma 8; in general the integers obey e_k(j, q) = e_k(j + 1, q) + (j + 1) e_{k - 1}(j + 2, q) (condition on whether j + 1 is a left end), so they are computed exactly by a two-index recurrence in order (q - j)^2 operations, and the dependence on the absolute order that makes T_{q, j} differ from A_{q - j} is the weight u_i of each domino, larger by j than in the relative index. Since the k left ends are distinct, at least two apart and at most q - 1, each product is at most (q - 1)(q - 3) ... (q - 2 k + 1), and there are C(q - j - k, k) coverings with k dominoes, so

    e_k(j, q) <= C(q - j - k, k) prod_{i = 0}^{k - 1} (q - 1 - 2 i),

a closed sum with explicit coefficients (the late-landing form). Both the exact and the late-landing sums have positive coefficients, so the monomial maximization of Lemma 8 applies term by term and gives the bound uniform in the offset

    sup_a T_{q, j}(a) e^{-a^2 / 4} <= 2^{(q - j) / 2} sqrt(j! / q!) sum_k 2^{-k / 2} e_k(j, q) (2 (q - j - 2 k) / e)^{(q - j - 2 k) / 2},

the factor for q - j - 2 k = 0 being one, and the same with (m / e)^{m / 2} under the stronger weight e^{-a^2 / 2}. Checks (edge-chain-transfer.mjs, output read before this paragraph; ordinary doubles, the integers exact): e_k(0, 8) = 1, 28, 210, 420, 105, the Hermite integers; e_k(3, 8) = 1, 22, 87 against the late-landing coefficients 1, 28, 105; the tiling sum equals the recurrence to the printed five digits at (q, j, |a|) = (8, 3, 2), (12, 1, 3), (12, 6, 3) and (24, 12, 5), the late-landing form exceeding it there (2.04e1 against 1.68e1, 2.02e4 against 8.30e3, 6.67e1 against 5.59e1, 2.21e4 against 1.48e4); the weighted supremum over |a| <= 8 on a grid, the monomial bound with the exact coefficients and the monomial bound with the late-landing coefficients are 1.26e3, 1.45e3 and 3.10e3 at (q, j) = (12, 1), 6.66, 9.39 and 10.8 at (12, 6), 1.90e7, 2.16e7 and 1.40e8 at (24, 1), 73.0, 98.1 and 144 at (24, 12), and the three coincide at j = q - 1 (0.350 at q = 12, 0.248 at q = 24, a single monomial). The sum over all entry orders of the exact-coefficient bounds, sum_{j = 1}^{q} sup_a T_{q, j}(a) e^{-a^2 / 4}, is 26.9, 2.00e3, 2.15e5 and 2.64e7 at q = 6, 12, 18 and 24, smaller at each order than the seed bound U_q of Lemma 8 (64.5, 8.55e3, 1.22e6 and 1.81e8): a unit forcing at every order, each carrying the Gaussian weight, costs less than the seed path. The lemma prices propagation only, and its conditions are those of Lemma 8: the Gaussian weight may be attached to a forcing term only when that term's absolute error bound carries it, and the local and endpoint errors, the geometry, the jump assembly, the state norm and the query amplification are inputs. Its scope is the real edge chain of the polygon oracle alone: the complex, frequency-dependent plane-wave chain of section 7h(d) and the one-dimensional rectangle endpoint recurrence of the implicit polynomial class are different recurrences with their own propagation bounds still to be written, and neither acquisition contract inherits a floating-point guarantee from Lemmas 8 and 8b [scope after #418]. No product formula exists for j >= 1: with c = 1 / sqrt 2 and n = q - j, the tiling sums z_n satisfy z_n = |a| z_{n - 1} + c (n + j - 1) z_{n - 2}, whose exponential generating function obeys Z'' - (|a| + c t) Z' - c (1 + j) Z = 0 and reduces to exp(|a| t + c t^2 / 2) only at j = 0; they are the associated Hermite polynomials of Askey and Wimp (1984) with the sign of the second coefficient reversed, so the finite sum above is the closed form and the late-landing sum its explicit majorant.

Lemma 8c (the endpoint recurrence's propagation; derivation only, no probe). The rectangle moments of the implicit class (the collaborator's note, section 5) and of the bracket states of 7j use the endpoint values h_n(x) phi(x) through J_n(a, b) = [phi(a) h_{n - 1}(a) - phi(b) h_{n - 1}(b)] / sqrt n, with h_n evaluated by the normalized recurrence h_n(x) = [x h_{n - 1}(x) - sqrt(n - 1) h_{n - 2}(x)] / sqrt n, h_0 = 1, h_1 = x. A forward error delta_n in the computed h_n obeys delta_n <= (|x| / sqrt n) delta_{n - 1} + sqrt((n - 1) / n) delta_{n - 2} + tau_n, whose coefficients are those of the edge chain divided by sqrt 2. With y_n = x_n sqrt(n!) the homogeneous recurrence becomes y_n = |x| y_{n - 1} + (n - 1) y_{n - 2}, the positive-coefficient Hermite recurrence with c = 1, so the seed amplification is A^end_n(x) = (1 / sqrt(n!)) sum_k [n! / (k! (n - 2 k)! 2^k)] |x|^{n - 2 k}, and the transfers are T^end_{n, j}(x) = sqrt(j! / n!) sum_k e_k(j, n) |x|^{n - j - 2 k} with the same integers e_k(j, n) as Lemma 8b and no factor 2^{-k / 2} (the domino landing at order l weighs l - 1), majorized by the same late-landing sum without that factor. Term by term the edge chain's amplification exceeds this one by 2^{(n - k) / 2} (positive recurrences are monotone in their coefficients), so at equal offset and order the endpoint recurrence is never the larger of the two, and it is the amplification the evaluation of the Hermite functions carries in every route. Since the endpoint value enters J_n multiplied by phi(x) = e^{-x^2 / 2} / sqrt(2 pi), an absolute error delta_{n - 1} in h_{n - 1}(x) enters J_n as phi(x) delta_{n - 1} / sqrt n, and if the propagated error is bounded by a multiple of a seed or forcing error the monomial bound sup_x |x|^m e^{-x^2 / 2} = (m / e)^{m / 2} gives sup_x phi(x) A^end_n(x) <= (1 / sqrt(2 pi n!)) sum_k [n! / (k! (n - 2 k)! 2^k)] ((n - 2 k) / e)^{(n - 2 k) / 2}, and the same for the transfers; but the local errors tau_n of a floating-point recurrence at a large |x| are relative to computed intermediates that grow like the polynomial, so the Gaussian weight applies to the product phi(x) h_{n - 1}(x) only when that product is what is computed, with its own rounding priced, and a certificate needs the intermediate magnitudes, the endpoint arithmetic and the accumulation, none supplied here [a separate recurrence with its own bound, no inheritance from Lemma 8, after #418].

Scope of the spatial oracle (the collaborator's clarification, #382). The direct spatial acquisition works for any compact piecewise-constant F with an explicitly supplied finite polygonal net-jump boundary: F may already be a graph of three or more layers (products, sums, thresholds and lookups of piecewise-constant layers are piecewise constant on the arrangement of all their edge families), so only the spectral hyperbolic theorem of sections 7b to 7d is pair-specific. The price then includes building the actual arrangement (the translated copies and the intersections of all layers within the window, not the authored edge counts) and evaluating the graph's values and jumps on the cells. The remaining acquisition gaps are non-polygonal or implicit sources and moving geometry, which are different from the number of layers. The phrase "offline-feasible at hours of GPU time" in the price paragraph above is withdrawn: operation counts with an assumed crossing density do not establish feasibility without hardware, memory movement, base-oracle and precision costs; the counts stand as counts.

Character-side acquisition, qualified (#378): the identity E_gamma[e^{i k . x} h_alpha] = e^{i k . mu_0 - |L_ref^T k|^2 / 2} (i L_ref^T k)^alpha / sqrt(alpha!) with L_ref L_ref^T = Sigma_ref is agreed; a truncated character list needs its own collective low-degree tail bound for the state, since the filtered-pixel error of the count theorem does not certify these moments by itself.

Lemma 7 (the truncated-state mass of a character is a Poisson tail). For a character e^{i k . x} with whitened frequency k~ = L_ref^T k and lambda = |k~|^2, its coefficients c_alpha(k) = e^{i k . mu_0} e^{-lambda / 2} (i k~)^alpha / sqrt(alpha!) have l2 mass on the degrees |alpha| <= N equal to

    sum_{|alpha| <= N} |c_alpha(k)|^2 = e^{-lambda} sum_{q <= N} lambda^q / q! = P(Poisson(lambda) <= N),

since sum_{|alpha| = q} k~^{2 alpha} / alpha! = (k~_1^2 + k~_2^2)^q / q! by the multinomial theorem (the Hermite degree of a plane wave under a Gaussian is Poisson distributed with mean lambda, the photon statistics of a coherent state in Glauber's sense; the full mass is one, as noted). Hence a truncated character list S changes the truncated state by an l2 error at most sum_{k not in S} |f^_k| sqrt(P(Poisson(|k~|^2) <= N)), and for |k~|^2 = lambda > N >= 1 the Chernoff bound P(Poisson(lambda) <= N) <= e^{-lambda} (e lambda / N)^N applies, the case N = 0 being the exact value e^{-lambda} (the Chernoff expression is not defined there) [audited by the collaborator, #387]. Numerically (printed by a one-line evaluation before this paragraph): at N = 12 the exact tail is 5.402e-3, 1.677e-4, 2.096e-7 and 4.950e-14 at lambda = 24, 30, 40 and 60, its square root 7.350e-2, 1.295e-2, 4.579e-4 and 2.225e-7, the Chernoff bound within a factor of about five to seven above the exact value; at N = 20 the square root is 1.919e-2 at lambda = 40 and 3.368e-8 at lambda = 80. Consequences. (i) The retained ellipse of the count theorem must be taken at a whitened radius squared c beyond N, of order N plus the budget's logarithm, and the omitted characters' sum is then a lattice sum outside the ellipse of e^{-lambda / 2} (e lambda / N)^{N / 2}, a Gaussian with a polynomial factor, which the count theorem's tail machinery bounds with the exponent halved and the polynomial absorbed into a shifted level, with |f^_k| <= 1 for a bounded material. (ii) The state of a single periodic material is then H_alpha = sum_{k in S} f^_k c_alpha(k), at |S| K_N operations, the characters the single-material kernel already retains, with no arrangement. (iii) A product's state from the frequency route is the same sum over the retained pairs' beats with f^_k replaced by the pair coefficients, with the pair list's own certificate on the omitted pairs (section 7d) and this lemma on the omitted beats' degrees. The bound in (i) is stated here as the form and not carried to a constant.

The coherent two-source version and the enlarged reach (after the collaborator's #384, derived independently here). Write s = k^T Sigma_ref k = lambda for the beat k = m + n. The truncated-state vector of the character at k has l2 norm exactly sqrt(P(Poisson(s) <= N)) by Lemma 7, at most one, and the collaborator's bound sum_{j <= N} s^j / j! <= a^{-N} e^{a s} for 0 < a < 1 with a = 1 / 2 gives norm <= 2^{N / 2} e^{-s / 4}, the Chernoff bound at a fixed exponent; the optimal a = N / s gives e^{-s / 2} (e s / N)^{N / 2}, sharper for s well above N. For the product's state acquired from pairs, the omitted contributions are of two kinds and both are coherent sums, so the triangle inequality over pairs is used and never a sum of squared single-character energies. Pairs with m outside the region S and beat inside the ellipse s <= R^2: each has truncated-state norm at most one, so their total is at most the region cut's omitted mass eta. Pairs with beat outside, s > R^2: their total is at most sum |a_m b_n| times the truncated-state norm sqrt(P(Poisson(s) <= N)), split into a reserved Gaussian factor for the Schur bound and a remainder whose supremum over s >= R^2 is the price of the degree. Fixed exponent (the collaborator's a = 1 / 2): the norm is at most 2^{N / 2} e^{-s / 4}; reserve e^{-s / 8} = exp(-k^T (Sigma_ref / 4) k / 2) for K_{Sigma_ref / 4}, and the remainder 2^{N / 2} e^{-s / 8} has supremum 2^{N / 2} e^{-R^2 / 8}, so ||omitted state||_2 <= 2^{N / 2} e^{-R^2 / 8} K_{Sigma_ref / 4} ||a||_2 ||b||_2. Optimized exponent (a = N / s, N >= 1): the norm is at most e^{-s / 2} (e s / N)^{N / 2}; reserve e^{-s / 4} = exp(-k^T (Sigma_ref / 2) k / 2) for K_{Sigma_ref / 2}, and the remainder e^{-s / 4} (e s / N)^{N / 2} decreases for s >= 2 N, so for R^2 >= 2 N its supremum is at the boundary and ||omitted state||_2 <= e^{-R^2 / 4} (e R^2 / N)^{N / 2} K_{Sigma_ref / 2} ||a||_2 ||b||_2; N = 0 is the pure Gaussian branch. Either is a valid state-reach rule, the second better once R^2 exceeds 2 N by a margin, the exact Poisson form best [prose matched to the reserved factors after #388]. The state errors are amplified by the normalized query bound sqrt(S(1)) only. A sufficient state reach in the collaborator's form is R^2 >= 4 N log 2 + 8 log_+(K_{Sigma_ref / 4} ||a||_2 ||b||_2 / eps_state), against the response's own output reach of the form R_out^2 = 4 log_+(K ||a|| ||b|| / eps_G), kept distinct; at N = 12 the state's reach squared is larger by 4 N log 2 = 33.3 plus twice the logarithm. What that costs in the pair list is not a fixed factor and must be carried through the selector's parameters: with b^2 multiplied by c, the partner count N_E and the floor h^2 scale with b^2, K = C_0 N_E c_buffer with N_E, u with N_E times its logarithm, and T = h^2 u of order c^2 up to logarithms, with the source and neighbour counts adding their own factors [my earlier sentence reading a uniform factor of two to three into the reach-area comparison is withdrawn, #388]. For a single lattice source the Hilbert-Schmidt route is sharper: ||sum_{k not in S} f^_k c(k)||_2 <= ||f^||_2 (sum_{k not in S} P(Poisson(s_k) <= N))^{1/2} by Cauchy-Schwarz over k, with ||f^||_2 <= 1, the lattice sum of the Poisson tails outside the ellipse being a Gaussian-with-polynomial lattice sum for the count theorem's tail machinery. An explicit conservative constant (the collaborator's, #388): with Theta = sum_{k in Lambda} e^{-s_k / 4}, the lattice Gaussian sum for the covariance Sigma_ref / 2 (the pair note's theta bound applies with B^T (Sigma_ref / 2) B), P(Poisson(s_k) <= N) <= 2^N e^{-s_k / 2} = 2^N e^{-s_k / 4} e^{-s_k / 4} <= 2^N e^{-R^2 / 4} e^{-s_k / 4} for s_k > R^2, so sum_{s_k > R^2} ||v_N(k)||^2 <= 2^N e^{-R^2 / 4} Theta and the state error is at most ||a||_2 2^{N / 2} e^{-R^2 / 8} sqrt(Theta), a constant without numerical summation and not necessarily the sharpest.

## 7h. Broader acquisition: what else feeds the same state, and what does not (proposal for audit)

The final query representation accepts any fixed bounded F; the question (the collaborator's, #387) is a cheap enough interface from material programs whose parts are not piecewise constant on polygons. Four extensions of the polygonal boundary oracle of GAUSSIAN-QUERY-STATE.md section 8, each with a validity and cost form and none with a constant, and two exclusions.

(a) Piecewise polynomial on polygonal cells [corrected after #390]. Let F = sum_c p_c 1_{P_c} with p_c of degree at most d. Do not integrate by parts with the polynomial weight: the identity h_beta gamma = -(1 / sqrt(beta_i)) partial_i(h_{beta - e_i} gamma) needs beta_i > 0 and the recursion can reach beta = 0 before the polynomial is constant (already H_0 with q = z_1). Instead expand first: by the multiplication identity z_i h_beta = sqrt(beta_i + 1) h_{beta + e_i} + sqrt(beta_i) h_{beta - e_i}, a polynomial q of degree d times h_alpha is a combination of h_{beta} with |beta| <= |alpha| + d, so int_{P_c} p_c h_alpha gamma = sum_beta C_{c, alpha, beta} M_{c, beta} with M_{c, beta} = int_{P_c} h_beta gamma the unweighted cell moments through degree N + d, which the collaborator's section 8 oracle provides (cell moments assembled from the edge recurrence and the Phi_2 seed at degree N + d). The complete conservative construction (the collaborator's, #391, independently audited on their side; verified here). For each supplied cell c compute the unweighted moment vector M^c_beta = int_{P_c} h_beta gamma through degree N + d, including H_0 with its Phi_2 seed, by the section 8 oracle. Let X_i be the multiplication-by-z_i operator on moment vectors, (X_i v)_alpha = sqrt(alpha_i + 1) v_{alpha + e_i} + sqrt(alpha_i) v_{alpha - e_i}, symmetric and sparse, X_1 and X_2 commuting. Since p_c(z) h_alpha(z) = sum_beta [p_c(X)]_{beta alpha} h_beta(z) by the multiplication identity applied monomial by monomial, int_{P_c} p_c h_alpha gamma = [p_c(X_1, X_2) M^c]_alpha, and the state is sum_c [p_c(X_1, X_2) M^c]_alpha for |alpha| <= N; the padding to degree N + d covers the beta = 0 case automatically. Cost, for dense supplied bivariate monomial coefficients: nested Horner in two variables needs O((d + 1)^2) applications of the sparse operators, each O((N + d + 1)^2), so a safe total is O([E_inc + C (d + 1)^2] (N + d + 1)^2) plus O(E_inc) seed special-function calls, the geometry, the input and basis conversion and the precision, with C the supplied cells and E_inc their boundary incidences (including interfaces with a continuous value but different polynomial pieces); cells can be streamed; for fixed d and C = O(E_inc) the scaling of the piecewise-constant oracle is recovered with a degree-dependent constant. A sufficient algorithm and not a lower bound against better contractions; coefficient magnitudes and cancellation enter the numerical budget. Validity is that of piecewise-constant sources with the polynomial pieces supplied.

(b) Curved count boundaries (conic arcs, the circles material). The divergence identity int_C h_{alpha + e_i} gamma = -(1 / sqrt(alpha_i + 1)) oint_{partial C} h_alpha gamma n_i ds holds for every piecewise-smooth boundary, so a curved arc contributes a one-dimensional integral of h_alpha gamma n_i along it; along an arc the Gaussian is not a Gaussian in the parameter, so the arc needs a quadrature edge oracle instead of the closed recurrence: Gauss-Legendre panels on the arc's parameter (the kernel's disc panels are this oracle without a certificate), with the standard panel error bound from the derivatives of the integrand, which is entire and bounded through Cramer's bound on h_alpha gamma times the arc's parametrization derivatives, polynomial in the curvature and the degree. Validity: the arc explicit and its parametrization's derivatives bounded; cost: panels per arc times nodes times the O(K_N) Hermite evaluations per node, with the certificate's derivative constants to be written; the same for any explicit smooth boundary.

(c) Smooth amplitude factors on a fixed mask. For F = g F' with F' one of the above and g smooth on the window, replace g by its Taylor polynomial T_d g at mu_0 and use (a); the state error is collective, ||(g - T_d g) F'||_{L^2(gamma)} <= sup_window |g - T_d g| plus the Gaussian tail outside the window, by Bessel, for every degree at once, once the Taylor remainder is certified. Validity: a derivative bound on g over the window; cost: (a) at degree N + d. Lighting enters here. A smooth warp of a discontinuous mask does not: it moves the mask's boundaries, so it changes the arrangement and is not an amplitude factor; its affine part is a change of the lattices and its remainder is the projective correction's business, and the warped boundaries must be supplied as curves for (b) [distinction kept after #390].

(d) Gabor atoms times polygonal masks [corrected after #390: the standard recurrence does not hold verbatim under a Gaussian factor]. Let the atom be g(z) = exp(-z^T A z / 2 + i k . z) in whitened coordinates, A symmetric with I + A positive definite (the admitted covariance), and let I^g_alpha = int_edge h_alpha gamma g ds in the ORIGINAL Hermite basis of gamma, which is the basis of the state. The multiplication identity is weight-free, so with v_i = sqrt(alpha_i + 1) I^g_{alpha + e_i} the normal equation n . v = a I^g_alpha - sum_j n_j sqrt(alpha_j) I^g_{alpha - e_j} =: R^g_alpha holds as before. The derivative identity acquires the factor's gradient: partial_i(h_alpha gamma g) = -sqrt(alpha_i + 1) h_{alpha + e_i} gamma g + h_alpha gamma (i k_i - (A z)_i) g, and expanding (A z)_i h_alpha by the multiplication identity, sqrt(alpha_i + 1) h_{alpha + e_i} gamma g = -partial_i(h_alpha gamma g) + i k_i h_alpha gamma g - sum_j A_ij [sqrt(alpha_j + 1) h_{alpha + e_j} + sqrt(alpha_j) h_{alpha - e_j}] gamma g, so along the edge, taking the tangential component (the only component of the gradient integral that is an endpoint quantity),

    t^T (I + A) v = -B^g_alpha + i (k . t) I^g_alpha - t^T A (sqrt(alpha_j) I^g_{alpha - e_j})_j,

with B^g_alpha the endpoint difference of h_alpha gamma g. Together with n . v = R^g_alpha this is a two-by-two linear system for v with matrix [n^T; t^T (I + A)], whose determinant is (up to sign) t^T (I + A) t > 0, so it is solvable for every admitted atom: a recurrence in the original basis with two extra lowered terms and no basis conversion, O(1) per multi-index and O(K_N) per edge. For a pure plane-wave shift (A = 0) it reads I^g_{alpha + e_i} = [n_i R^g_alpha - t_i (B^g_alpha - i (k . t) I^g_alpha)] / sqrt(alpha_i + 1), the extra term being the collaborator's point that the shifted and original polynomial bases differ; for the collaborator's example g = exp(-tau z_2^2 / 2) on the edge z_1 = 0 with vanishing endpoints, the system gives the tangential second moment over the zeroth as [1 / (1 + tau) - 1] / sqrt 2, not zero (checked: with n = e_1, t = e_2, a = 0, alpha = e_2, the tangential equation reads (1 + tau) sqrt 2 I_{2 e_2} = -tau I_0, so I_{2 e_2} / I_0 = -tau / ((1 + tau) sqrt 2) = [1 / (1 + tau) - 1] / sqrt 2). The seeds: I^g_0 = int_edge gamma g ds is a one-dimensional Gaussian integral along the edge with a complex linear term, a complex-argument Phi (the Faddeeva family), and the cell's zeroth moment needs a complex bivariate probability primitive. The cell assembly closes with a fixed two-by-two inverse (the collaborator's, #395, verified here; it replaces the order-by-order system first proposed): with C = I + A, M_alpha = int_P h_alpha gamma g, e_alpha = oint_{partial P} n h_alpha gamma g ds (the weighted edge moments from the recurrence above), l_{alpha, j} = sqrt(alpha_j) M_{alpha - e_j} and v_{alpha, j} = sqrt(alpha_j + 1) M_{alpha + e_j}, integrating the derivative identity for each coordinate over the cell, where the divergence theorem makes both components of the gradient integral boundary terms, gives C v_alpha = -e_alpha + i k M_alpha - A l_alpha, so v_alpha = C^{-1}(-e_alpha + i k M_alpha - A l_alpha) with one predecessor per target; the right side has only degrees |alpha| and |alpha| - 1 and the edge moments, so after the complex seed every cell moment costs O((N + 1)^2) with no basis conversion. Numerical obligations: the conditioning of C^{-1}, the weighted edge solves, the complex seeds, coefficient magnitudes and cancellation. Validity: I + A positive definite. This is the count-map algebra of complex Gaussians written in the oracle's form, with the basis kept as the state's.

Implicit boundaries, one class covered (the collaborator's, #411, constants under their audit; counts checked here). A rational polynomial threshold program in two spatial dimensions, with total predicate degree D, constant predicates removed exactly and the Boolean or value circuit kept as input, admits certified acquisition with no supplied curve arrangement: on each dyadic cell an exact two-variable existential test (a fixed-dimensional real-algebraic decision, priced in the degree, the coefficient bits and the dyadic depth) decides whether any predicate vanishes in the cell; if none does, every sign is constant and one centre evaluation of the circuit fixes the cell's value; otherwise the cell is subdivided or paid as uncertainty. The count: on a k by k grid the zero set of the product of the predicates meets at most 10 D k + 2 D^2 + 7 D cells (a non-identically-zero restriction to any of the 2 (k + 1) grid lines has at most D roots, each touching at most four cells; at most D grid lines are identically zero, each touching at most 2 k cells; and every remaining hit cell contains a whole connected component, of which there are at most D (2 D - 1) by the Milnor bound quoted in Wongkew 1993), with no general-position or regular-boundary assumption; a quadtree through depth L therefore visits at most 1 + 40 D (2^L - 1) + 4 (2 D^2 + 7 D) L nodes. For the window [-R, R]^2 and h = 2 R / 2^L, filling the uncertain and outside cells with zero gives a squared L^2(gamma) source error at most [20 D R h + (2 D^2 + 7 D) h^2] / (2 pi) + exp(-R^2 / 2), which Bessel transfers to the whole Hermite state; the resolved cells are axis-aligned rectangles, whose moments separate into products of one-dimensional Gaussian-Hermite endpoint integrals with the closed antiderivative of He_n phi. The cost is of order eta^{-2} times degree and reach factors for a state error eta, plus the exact cell decisions and the numerical accumulation; not a real-time result and not necessarily better than sampling, but an explicit contract for one implicit family, tangencies and hidden small components included. A sharper query budget for the same acquisition (the collaborator's, #413; verified here). When the target is the query response rather than an arbitrary L^2 state error, choose the reference Gaussian broader than every query, so that q = N(m, Q) has 0 < Q < I in reference coordinates; completing the square in the density ratio q / gamma = det(Q)^{-1/2} exp(-(1 / 2)(y - m)^T Q^{-1} (y - m) + |y|^2 / 2), whose quadratic part has matrix (Q^{-1} - I) / 2 > 0, gives ||q / gamma||_infinity = det(Q)^{-1/2} exp((1 / 2) m^T (I - Q)^{-1} m) (checked: the maximizer is (Q^{-1} - I)^{-1} Q^{-1} m and the value simplifies through Q^{-1} (Q^{-1} - I)^{-1} Q^{-1} - Q^{-1} = (I - Q)^{-1}), uniformly at most K_infinity = q_-^{-d / 2} exp(L^2 / (2 delta)) when q_- I <= Q <= (1 - delta) I and |m| <= L. The resolved-cell values with zero on the uncertain and outside cells form one coherent source F^ with 0 <= F^ <= F <= 1, so the query's source error is E_q[F - F^] = int (F - F^)(q / gamma) gamma <= K_infinity times the Gaussian mass of the uncertain and outside cells, with no square root; the Gaussian Hermite tail is then applied to F^ itself (bounded, so the factor one half stands) and the numerical state error added. The same certified quadtree therefore works at a cell size h of order epsilon / (K_infinity D R) for a response tolerance epsilon, instead of epsilon^2 / (D R), and visits of order K_infinity D^2 R^2 / epsilon nodes plus logarithmic terms and the exact cell decisions: the sufficient query acquisition improves by matching the norm to the query, while the L^2 state rate is unchanged. The price is the broader reference (the largest footprint of the patch, which changes the degree selector's S and the patch structure) and the explicit K_infinity, which grows like exp(L^2 / (2 delta)) with the centre range and the closeness of the queries to the reference's width; no universal small constant follows. The same device applies to every coherent source approximation that differs from the source on a set: the spatial window cut of the state costs the query K_infinity e^{-R^2 / 2} against the l2 bound e^{-R^2 / 4}, and the uncertain cells of any adaptive acquisition cost K_infinity times their Gaussian mass. Not covered by the class: black-box real coefficients, trigonometric level sets, and polynomial degree hidden by a compact circuit. Noise is not one exclusion [narrowed after #419]: polynomial-fade gradient or value noise is piecewise polynomial once its lattice cell and its hash values are fixed (a two-dimensional quintic gradient-noise cell has bidegree at most (6, 6) and total degree at most 11: fade of degree five in each variable times the linear gradient term; checked), so thresholds of a finite, explicitly partitioned noise program with any finite lookup or permutation hash fall under the class cell by cell, without the carry theorem's XOR structure; the price is the common cell partition of all octaves and input grids, and a compact octave loop can hide exponentially many finest-scale cells, which the older carry-state theorem avoids for its restricted polynomial responses and this admission pays. Prior art (the collaborator's reading, #419): Heidrich, Slusallek and Seidel 1998 acquire procedural shaders adaptively under conservative range bounds, noise, steps and conditionals included; the distinction claimed here is the complete fixed-dimensional decision with its stopping rule and the Gaussian-query error and cost contract, not adaptive source approximation as such. [This replaces the earlier blanket statement that implicit boundaries have no interface but the threshold oracle's; for trigonometric and noise predicates that statement stands.] The direct query-family budget, which supersedes the broader-reference route for this class (the collaborator's, #417 and section 7 of IMPLICIT-POLYNOMIAL-SOURCE.md at cc97a27, under their audit; verified here). The reference Gaussian of the Hermite basis need not be the measure that prices the acquisition error. If the whole query family in reference coordinates satisfies q_- I <= Q <= q_+ I and |m| <= L_q, then every query density is at most 1 / (2 pi sqrt(det Q)) <= 1 / (2 pi q_-), and for R > L_q the query mass outside [-R, R]^2 is at most the mass outside the disk of radius R, itself at most P(|Y - m| > R - L_q) <= P(q_+ |Z|^2 > (R - L_q)^2) = exp(-(R - L_q)^2 / (2 q_+)) with Z standard normal, because |Q^{1/2} Z|^2 <= q_+ |Z|^2 and |Z|^2 is exponential with mean two in the plane (checked; exact, no polynomial prefactor). Hence the one resolved-cell source F^ has, uniformly over the family, 0 <= E_q F - E_q F^ <= [20 D R h + (2 D^2 + 7 D) h^2] / (2 pi q_-) + exp(-(R - L_q)^2 / (2 q_+)), the uncertain area above times the density bound plus the tail. The selector R >= L_q + sqrt(2 q_+ log(2 / epsilon_A)) puts the tail at or below epsilon_A / 2, and h <= min(2 R, pi q_- epsilon_A / (40 D R), sqrt(pi q_- epsilon_A / (2 (2 D^2 + 7 D)))) puts each area term at or below epsilon_A / 4 (checked by substitution), so one mesh gives every query of the family a source contribution at most epsilon_A; with R within a constant factor of its bound and the smallest admissible depth, the tested nodes are of order D^2 R^2 / (q_- epsilon_A) plus logarithmic terms (the quadratic step constraint is dominated because R^2 / (q_- epsilon_A) >= 2 log 2 under the selector; checked). No density-ratio bound and no Q < I enters this step, so the exponential centre-range factor of K_infinity is gone from the geometric acquisition; the Gaussian Hermite representation of the query keeps its own strict admission Q < 2 I with a declared margin and its own degree and numerical amplification, which may still depend strongly on the centre range, and the response budget is epsilon_A plus the bounded-source query truncation of F^ plus the numerical allowances, with ||F - F^||_2 not charged again. The K_infinity corollary above stays valid as a bound and is no longer needed for this construction. Two cautions taken with it: a refused boundary quadrature panel of the curved-boundary oracle (7h(b)) is not thereby a source region of known area, so it enters this envelope only once its area or Gaussian mass is supplied [#417]; and neither this contract nor the control-band one inherits a floating-point guarantee from Lemmas 8 and 8b, whose recurrence is the real edge chain [#418].

Exclusions. Implicit boundaries of trigonometric or noise predicates, the level sets of sums of atoms, have no supplied arrangement and no exact cell decision: their contribution to the state is a threshold problem, and the interface is the threshold oracle's band certificates (threshold-oracle-contract.md), not a boundary integral; this is the interface that is not cheap, and it is where a source-moment oracle would be needed. Moving geometry, with the distinction the collaborator drew (#393). A common invertible affine motion of the entire source needs no rebuild of the state, only the query's parameter work (patch admission, whitening, the coefficient recurrence): if F_theta(x) = F_0(A_theta x + b_theta) then the pixel's Gaussian N(mu, Sigma) queried against F_theta is exactly the Gaussian N(A_theta mu + b_theta, A_theta Sigma A_theta^T) queried against the same state of F_0 (the affine image of a Gaussian variable is Gaussian), valid as long as the transformed Gaussian lies in the state's admitted patch; translations, rotations, shears and scales of the whole material, and a camera that moves over a static material, are of this kind, which is the count-map program's rule that an affine warp moves the lattices and nothing else, now in the Hermite frame. A common nonlinear diffeomorphism T keeps the same state as a candidate but changes the query to the pushforward density, q_T(y) = p(T^{-1} y) / |det DT(T^{-1} y)| in reference-whitened coordinates, with ||q_T / gamma||^2_{L^2(gamma)} = int p(x)^2 / [gamma(T x) |det DT(x)|] dx as the admission condition; the moving complexity is transferred into the query coefficients, which are then the moments E[h_alpha(T(X))] of the Hermite functions under the pushforward and no longer come from the Gaussian recurrence, and that loss of the cheap recurrence is the actual gap, not motion as such. The perspective case is the one this program has priced: for a projective warp with whitened parameter k (the whitened rational count of the candidate-selection contract), the projective density theorems (PROJECTIVE-DENSITY.md, and the summary in the lecture notes' section 8) give the pushforward's density relative to the affine Gaussian as 1 + (k . z')(3 - |z'|^2) up to a remainder whose response error is at most K_2 W |k|^2, K_2 = 1.8920950773, W the material's range; the correction is a cubic polynomial in the query's own whitened coordinate z', and the frame question is settled by an affine substitution [after #394's open point]: the state and its multiplication operators live in the reference frame z = L_ref^{-1}(x - mu_0), the query's whitened coordinate is z' = L_q^{-1}(x - mu_q) with L_q the affine part's factor, so z' = G z + g with G = L_q^{-1} L_ref and g = L_q^{-1}(mu_0 - mu_q), and p(z) := (k . (G z + g))(3 - |G z + g|^2) is a cubic polynomial in the reference coordinate with at most ten monomials whose coefficients are explicit in (G, g, k). Then, since p h_alpha = sum_beta [p(X)]_{beta alpha} h_beta in the reference basis, the projective query coefficients are c^{proj} = c^{aff} + p(X_1, X_2) c^{aff} for |alpha| <= N, where c^{aff} are the affine query's coefficients from the Gaussian recurrence carried to degree N + 3 (the cubic raises the degree by three); the cost is the recurrence to N + 3 plus at most ten monomials' worth of sparse operator applications, O(K_{N + 3}) per query, and no Hermite basis conversion is needed because the operators act on the reference-basis coefficient vector. The collaborator reached the same construction independently (#397: with the affine query N(m, Q) in reference coordinates and Q = B B^T, P(y) = 1 + [k . B^{-1}(y - m)][3 - |B^{-1}(y - m)|^2] and d = P(X) c, the coefficients generated through N + 3 with the valid degree shrunk after each sparse application; applying the untransformed cubic to the reference vector is wrong, which is the point of the substitution). The tail of the corrected query is not the Gaussian tail, since the cubic moves degrees, and the exact certificate is theirs: with R = q / gamma and S = ||R||^2 from Shim's formula, q^2 / gamma divided by S is the Gaussian N(v, V) with V = (2 Q^{-1} - I)^{-1} and v = 2 V Q^{-1} m (checked: the exponent -(y - m)^T Q^{-1} (y - m) + |y|^2 / 2 has quadratic part -(1 / 2) y^T (2 Q^{-1} - I) y and linear part 2 m^T Q^{-1} y), so ||P R||^2 = S E_{N(v, V)}[P^2], a degree-six Gaussian moment, and the omitted squared coefficient norm is exactly this total minus sum_{|alpha| <= N} |d_alpha|^2, to be certified with interval arithmetic since the difference cancels; for F in [0, 1] the query tail bound is half its square root, the constant coefficient being retained; the signed projective-model error K_2 W |k|^2 is added separately. This keeps the L^2 admission on the polynomial-corrected model and not on the true rational pushforward, which has pole-induced heavy tails and need not be square-integrable against a Gaussian [after #397; my earlier sentence that the admission is trivially met is corrected to this]. Independent motion of one layer against another is the case the relative-phase counterexample (the collaborator's section 7) excludes from this treatment: it changes the arrangement and needs its own state or a rebuild of the affected patches. Prior art for the change of variables itself: Heckbert (1989), section 3.4, equation 3.2, expresses exact general-warp filtering through the transformed footprint and its Jacobian, so nothing here is new about warping a filter; the potentially useful part is a certified reusable finite representation with cheap coefficient updates. Two results of the collaborator's on general warps (#399, MOTION-AS-QUERY.md, verified here where indicated). The obstruction: for the shear T_b(x, y) = (x + a sin(b y), y), with unit determinant and displacement at most a, the density ratio is R_b = exp(a x sin(b y) - a^2 sin^2(b y) / 2) (checked: the pushforward density at (x, y) is gamma(x - a sin(b y), y)), its squared norm E_y exp(a^2 sin^2(b y)) <= e^{a^2} (checked by the Gaussian moment in x) tends to the phase average of exp(a^2 sin^2 phi) as b grows, while every fixed Hermite coefficient tends to that of the phase-averaged ratio, so the squared norm of the limit falls short of the limit of the squared norms by at least a^2 / 2 (the phase-dependent residual projected on h_1(x)): every fixed degree leaves at least that much asymptotic squared query tail, and the bounded material F_b = 1_{x sin(b y) > 0}, which varies with b, has exact response tending to the average of Phi(a |sin phi|) > 1 / 2 while the retained response tends to 1 / 2, a gap at least 2 a phi(a) / pi. For a fixed material the uniform norm still gives convergence through the source tail; what is obstructed is a source-independent degree selector from the displacement, the determinant and the query norm alone. The safe branch: for a near-affine warp Psi(z) = b + B [z + u(z)] with ||Du||_op <= kappa < 1, the Gaussian change of variables and integration by parts give KL((Id + u)# gamma || gamma) = E[tr Du - log det(I + Du) + |u|^2 / 2] (checked: the log density ratio at the image point is z . u + |u|^2 / 2 - log det(I + Du), and E[z . u] = E[div u] by Gaussian integration by parts) <= E|u|^2 / 2 + E||Du||_F^2 / [2 (1 - kappa)], and Pinsker bounds the response difference between the warped query and the affine query N(b, B B^T) for every bounded material by (1 / 2) sqrt(E|u|^2 + E||Du||_F^2 / (1 - kappa)), with no material smoothness and no arrangement; a cutoff variant charges the Gaussian tail plus local displacement and derivative bounds. Classical transport and entropy machinery as an explicit sufficient certificate, not sharp; it is the universal first-order branch beside the projective second-order correction above. The collaborator's MOTION-AS-QUERY.md (6891207, 306 lines, audited on their side) holds the full statements: common affine reuse, the exact nonlinear admission and approximation questions, the bounded-displacement shear obstruction with its varying-source caveat, the near-affine certificate with a finite-window cutoff, and the polynomial-query energy formula, its projective subsection two-dimensional; what it certifies is a sufficient common-warp contract, and it is not a general cheap-warp result, which remains open. So the open interfaces are two: implicit boundaries, and general nonlinear warps beyond the projective first order, together with independent layer motion; the common-affine and perspective cases are covered. The four extensions above are groupings of the existing oracle with their own validity and cost, and are offered as such.

## 7i. Controls as phases: the two-layer shift identity and a three-column class (structural; the collaborator's qualifications and class, #409)

Identity. For two layers with a relative shift, F_delta(x) = A(x) B(x + delta), the state at the reference is H_alpha(delta) = sum over the retained pairs (m, n) of a_m b_n e^{i n . delta} e^{i (m + n) . mu_0} e^{-|k~|^2 / 2} (i k~)^alpha / sqrt(alpha!) with k = m + n (the sign of the phase matching the shift as written): the control's Fourier modes are the controlled layer's frequencies n in the pair list, and the joint Hermite-Fourier state is the list re-indexed by n. Three qualifications (the collaborator's, #409). The pair cutoff for this state is the enlarged state-reach certificate of Lemma 7 and its two-source form, not the response's output ellipse; the absolute pair-state tail is uniform in delta because the control phases have modulus one, and the query amplification is charged after it. Retaining the original bounded-source camera tail together with that state error preserves the factor one half, while the truncated pair polynomial itself need not stay in [0, 1]. The list's projection onto n gives candidate control modes and not a lower bound on the modes required: partner cancellation, zeros, or another response representation can reduce it, and neither the selector's upper bounds nor a finite enumeration establishes a certified minimal work or a universal count [my earlier "certified large, dozens in practice" is read this way]. For p independently shifting layers the candidate modes are the (p - 1)-tuples of the beat set with sum in the ellipse, polynomial in the region at fixed p and exponential in p, the open r >= 3 count.

A class with no pair enumeration (the collaborator's, #409; CONTROL-PHASE-STATE.md, their claim c49). Let A(y) be any fixed bounded material and b : T^p -> [0, 1] a supplied finite Fourier polynomial in p phases, with B(x) = b(M x); independent translations of the two layers give A(x + s_A) B(x + s_B) = A(y) b(M y + phi) with y = x + s_A and phi = M (s_B - s_A), so the control enters only through phi and the state's mode fields are b_k A(y) e^{i k . M y}, one Hermite column per mode k of b: a single cosine-controlled layer multiplying a fixed A needs three columns regardless of the carrier's magnitude, and this is not a linear storage guarantee for products: independently controlled cosine layers need the full retained control support, 2 J + 1 modes when the J layers share one character and up to 3^J when their characters are unrelated (the collaborator's product example in CONTROL-PHASE-STATE.md, 7827f96, 224 lines, audited on their side, which holds the full finite-control-band representation, the uniform Gaussian truncation without a mode-count factor, the separate numerical and phase costs, the translated family A(y) b(L y + phi) with direct plane-wave polygon acquisition, and the white-reference frequency and constant phase factor made explicit) [wording after #414]. When A has supplied polygonal cells, each column is the plane-wave-modulated material A e^{i k . M y}, which the weighted edge and cell oracle of section 7h(d) acquires directly with the envelope matrix zero (a pure complex shift), the complex seed's accuracy and the phase precision still priced. This is the count-map class idea in its sound form: a finite control basis supplied with the source, a fixed column count per control mode, acquisition by the spatial oracle rather than by a pair list. Common few control characters in a product can keep polynomial support; unrelated independent characters still grow exponentially, and the broader band-acquisition problem remains open.

## 7j. Bracket acquisition under the query family (derivation after the collaborator's question, #418; the envelope confirmed sound and the count corrected in #421)

Setting. Reference coordinates; F with values in [0, 1]; a mesh of the window W = [-R, R]^2 into closed rectangles with disjoint interiors; on each cell C a certified bracket, functions l_C <= F <= u_C on C supplied by a conservative range oracle, constant or affine. F_lo = sum_C l_C 1_C on W and 0 outside; F_hi = sum_C u_C 1_C on W and 1 outside; the gap G = F_hi - F_lo >= 0; gap_W = integral over W of G = sum_C integral_C (u_C - l_C).

Theorem D (the bracket envelope). For every query q = N(m, Q) with q_- I <= Q <= q_+ I and |m| <= L_q < R,

    0 <= E_q F - E_q F_lo <= E_q G <= gap_W / (2 pi q_-) + exp(-(R - L_q)^2 / (2 q_+)).

Proof: 0 <= F - F_lo <= G pointwise (on W by the bracket, outside W because 0 <= F <= 1 = G there); E_q G <= ||q||_infinity gap_W + q(W^c), and the density and tail bounds are those of 7h. The midpoint state (F_lo + F_hi) / 2 has two-sided error at most half of the same bound. If the state of G is compiled alongside (the same rectangle moments), each query also gets its own a posteriori bound E_q G, up to the truncation and numerical allowances of both states. What is not charged: no L^2 norm of F - F_lo and no accuracy of any state coefficient beyond the numerical evaluation of the exact moments of F_lo; the refinement is driven by the L^1 gap over the window alone [confirmed sound, #421]. The Hermite query's own admission and conditioning are untouched.

The count, and a correction. With constant brackets, a material that varies over positive area forces a cell side of order epsilon on that area, hence of order epsilon^{-2} cells on a uniform isotropic mesh: the gap of a cell of side h is its oscillation times h^2, of order Lambda h^3 for a Lipschitz constant Lambda, so the gap over an area A is of order Lambda A h, h must be of order epsilon / (Lambda A), and the cells number A / h^2 [my message #420 claimed a count linear in 1 / epsilon for piecewise-Lipschitz materials with constant brackets; withdrawn, the collaborator's correction, #421: the gap is linear in h, the count is not]. Jump neighbourhoods admit an adaptive count of order 1 / epsilon; smooth interiors need higher-order leaves, which is Theorem E, or another acquisition route.

Theorem E (order-two brackets: a count linear in 1 / epsilon). Let the jump set J of F be a finite union of N_J rectifiable curves of total length l_J, and let the oracle return on every cell C not meeting J an affine bracket with u_C - l_C <= kappa_2 (diam C)^2 on C, and on every cell at worst the trivial bracket [0, 1]. Take the dyadic grid of W at side h_S = 2 R / 2^{L_S}, and refine every cell meeting J to side h_J = 2 R / 2^{L_J} <= h_S, children not meeting J stopping where they are. Then:
(a) the closed cells of side h meeting J number at most 8 l_J / h + 4 N_J (cover each curve by at most 2 l / h + 1 arcs of length at most h / 2; a set of diameter at most h / 2 contains at most one grid abscissa and one grid ordinate, so it meets at most two columns and two rows, four closed cells);
(b) the terminal cells number at most 1 + (2 R / h_S)^2 + 32 l_J / h_J + 16 N_J (L_J - L_S), and the tested nodes at most one plus four thirds of that (four children per J-cell at each intermediate level, and sum_{l < L_J} 2^l / (2 R) <= 1 / h_J) [the root added after #425];
(c) gap_W <= 8 kappa_2 R^2 h_S^2 + 8 l_J h_J + 4 N_J h_J^2 (the non-J terminal cells have side at most h_S and disjoint interiors in W, so their gap is at most kappa_2 (sqrt 2 h_S)^2 times the window area 4 R^2; the J-cells at the finest level number at most 8 l_J / h_J + 4 N_J and carry gap at most h_J^2 each);
(d) with R >= L_q + sqrt(2 q_+ log(2 / epsilon_A)), h_S the largest dyadic side at most min(2 R, sqrt(pi q_- epsilon_A / (16 kappa_2 R^2))) (h_S = 2 R when kappa_2 = 0) and h_J the largest dyadic side at most min(h_S, pi q_- epsilon_A / (32 l_J), sqrt(pi q_- epsilon_A / (16 N_J))) (h_J = h_S when the jump set is empty; when the smooth constraint is the binding one, h_J = h_S and the count's second term is only an overestimate) [caps and cases after #425], the three parts of gap_W are at most pi q_- epsilon_A / 2, / 4 and / 4, so gap_W / (2 pi q_-) <= epsilon_A / 2, the tail is at most epsilon_A / 2, and every query of the family has source error at most epsilon_A from one mesh, with terminal cells at most 256 kappa_2 R^4 / (pi q_- epsilon_A) + max(2048 l_J^2 / (pi q_- epsilon_A), 256 l_J sqrt(N_J / (pi q_- epsilon_A))) + 16 N_J (L_J - L_S) (the dyadic rounding at most halves each side, hence the factors four and two).
The count is linear in 1 / epsilon_A with kappa_2 R^4 and l_J^2 as the geometric factors. Consistency with the polynomial class: a curve of degree D inside the window has length at most 4 D R by Crofton's formula (a line meets it in at most D points, and the lines meeting the square have measure equal to its perimeter 8 R), so l_J^2 is at most 16 D^2 R^2 and the two counts have the same order in D, R and 1 / epsilon_A; the polynomial class's count came from grid-line roots and components rather than length, and its interiors are exact (gap zero off the zero set), so the kappa_2 term is absent there.

The oracle's contract is the input. Interval arithmetic gives constant brackets whose excess width is of first order in the cell size for Lipschitz programs, affine arithmetic gives affine brackets with second-order error for smooth operations (Comba and Stolfi 1993; Stolfi and de Figueiredo 1997), and the overestimation of a particular program (the dependency problem) sits inside kappa_1 or kappa_2; nothing here bounds it, and a program whose range extension does not tighten with the cell, a hash evaluated anew at every scale, has no useful kappa and returns to enumeration. The polynomial class is the limit case of an exact oracle with zero gap off the zero set. Prior art (the collaborator's reading, #419 and #421): Heidrich, Slusallek and Seidel 1998 acquire procedural shaders adaptively under affine-arithmetic range bounds, and Greene and Kass 1994 (Greene's dissertation 1995, sections 5.4.3 and 5.4.4) price the uncertainty of such an acquisition under the actual pixel filter and refine the cell of largest contribution, which is the a posteriori rule of Theorem D with E_q G split by cell; the addition here is the family-uniform budget from the density and tail bounds, so that one mesh serves a band of footprints, and the state form of the result.

Acquisition of the affine-bracket state: the moments of (alpha + beta_1 x + beta_2 y) 1_C against h_p(x) h_q(y) gamma separate, and x h_p(x) = sqrt(p + 1) h_{p + 1}(x) + sqrt p h_{p - 1}(x) turns the linear factor into two endpoint integrals of the same kind as the constant case, so the state of F_lo needs the same one-dimensional endpoint primitives as the polynomial class; their numerical allowance is separate and unpriced here.

Checks (bracket-acquisition.mjs and bracket-adaptive.mjs, outputs read before this section; ordinary doubles; run before the collaborator's request in #421 to stay with derivations, and no further probes for this section). Material 1_{|z - c| <= 1.7} (1 / 2 + sin(2 x) / 4 + cos(2 y) / 4), c = (0.4, -0.2), window R = 3, query N((0.5, -0.3), I / 2), so q_- = q_+ = 1 / 2 and L_q = 0.5831: the mass outside the window is 2.726e-4 against the bound 2.905e-3; E_q F = 0.632703 by midpoint quadrature on a 2400^2 grid. On uniform grids from h = 1.5 to h = 0.0469 the envelope holds for both bracket types (at h = 0.0469 the constant bracket has gap_W = 1.045, bound 0.3356 and actual error 0.0242, the affine bracket gap_W = 0.6446, bound 0.2081 and actual error 0.00273), the cells meeting the circle number 9, 17, 37, 73, 145 and 289 against the bound 8 l_J / h + 4 = 61, 118, 232, 460, 916 and 1827, and both gaps halve per level on the uniform grid because the J-cells' term 8 l_J h dominates there, which is what the adaptive mesh removes. Adaptive meshes (L_S, L_J): (6, 11) has 17587 terminal cells (count bound 120846), gap_W = 0.0773 (gap bound 0.4086) and query bound 0.0275; (6, 13) has 59365 cells (bound 470886), gap 0.0474 (bound 0.2208) and query bound 0.0180; the uniform grid at level 11 has 4194304 cells for gap 0.0399 and bound 0.0156. Bounds (a) to (c) hold in every row.

Repairs after #425 (the collaborator's audit: the envelope and the arc-length area proof sound under their input contracts; the selector and the decision fixed here). The jump set is not a decision oracle. The algorithm refines a cell exactly when the oracle's bracket width on it exceeds kappa_2 (diam C)^2; by the contract every cell not meeting J passes, so the cells refined at each level are among the cells meeting J, and (a) to (c) hold for the mesh this rule produces with l_J and N_J entering only the bound, never a decision; a J-cell that passes at some level stops there with a gap within the smooth term, and the failed cells at the finest level are the ones charged h_J^2. The affine brackets need not stay in [0, 1]: on a passed cell the lower bracket satisfies l_C >= u_C - gap_C >= F - gap_C >= -gap_C and l_C <= F <= 1, so F_lo takes values in [-g, 1] with g = 2 kappa_2 h_S^2, the failed cells carrying the constant bracket [0, 1]; the affine change F' = (F_lo + g) / (1 + g) has values in [0, 1] and the same rectangle moments exactly, so the bounded-source Hermite truncation with its factor one half applies to F' and the response bound of the state is multiplied by 1 + g, with no clipping and no added cells (clipping would cut the cells along lines and lose the rectangle moments).

## 7k. Correlated footprints on the hashed grid: the Mehler separation (derivation after #424 and #425; for audit)

The prototype-moment contraction (the collaborator's NOISE-PROTOTYPE-MOMENTS.md, equations 3 to 5) needs the pixel kernel to be a product of one axis kernel in x and one in y, so that its cellwise polynomial coefficients factor over the cell indices. A correlated Gaussian on the lattice axes does not factor: already at degree zero its cell values r_i s_j exp[-(Sigma^{-1})_{12} (i + 1 / 2)(j + 1 / 2)] are generically of full rank over the index pairs (#425), and whitening would rotate the source grid, which the carry state does not allow. The separated expansion that keeps the grid is Mehler's; what it costs is a rank, a larger degree and larger signed weights, all explicit below.

Setting. Native covariance Sigma with axis widths sigma_x, sigma_y >= 1 in cell units and correlation rho, |rho| < 1; centre mu; X = (x - mu_x) / sigma_x, Y = (y - mu_y) / sigma_y. Then p(x, y) = phi_rho(X, Y) / (sigma_x sigma_y) with phi_rho the standard bivariate normal density of correlation rho, and Mehler's formula

    phi_rho(X, Y) = sum_{n >= 0} (rho^n / n!) He_n(X) phi(X) He_n(Y) phi(Y)

is the eigen-expansion of the Gaussian kernel in L^2(phi) (eigenvalues rho^n, orthonormal eigenfunctions h_n = He_n / sqrt(n!)). Each term is a product of one signed axis kernel p_n^x(x) = He_n(X) phi(X) / sigma_x and one p_n^y, with ||p_n^x||_1 = integral |He_n| phi <= sqrt(n!) by Cauchy-Schwarz against integral He_n^2 phi = n!.

(i) Rank and truncation. With the terms n <= N_rho kept, p_N = sum_{n <= N_rho} (rho^n / n!) p_n^x p_n^y has ||p - p_N||_1 <= sum_{n > N_rho} |rho|^n = |rho|^{N_rho + 1} / (1 - |rho|) =: delta_rho, and a response in [0, 1] loses at most delta_rho; N_rho = 0 when rho = 0, and otherwise N_rho + 1 >= log(1 / (delta_rho (1 - |rho|))) / log(1 / |rho|) suffices. The separated rank is N_rho + 1, of order log(1 / delta_rho) for |rho| <= 1 / 2 and growing like log(1 / (delta_rho (1 - |rho|))) / (1 - |rho|) as the footprint elongates along a diagonal of the grid, the logarithm included [wording corrected after #433]; this is the rank the collaborator asked for, and it is a property of the kernel alone.

(ii) The axis kernels' cell polynomials. On a unit cell centred at c, with H = 1 / sigma <= 1, y = (c - mu) / sigma and s = u - 1 / 2, u in [0, 1]: X = y + H s, and by the Appell property He_n(y + H s) = sum_{m = 0}^{n} C(n, m) He_{n - m}(y) (H s)^m is an exact polynomial of degree n in u, while phi(X) = phi(y) e^{z(u)} with the collaborator's z(u) = -H y s - H^2 s^2 / 2. Take P^{(n)}_C(u) = sigma^{-1} phi(y) He_n(y + H s) sum_{a = 0}^{N - 1} z^a / a!, of degree n + J with J = 2 (N - 1): their cell polynomial times the exact Hermite factor. Its error on the cell is sigma^{-1} phi(y) |He_n(y + H s)| |e^z - sum_a z^a / a!| <= sigma^{-1} phi(y) |He_n(y + H s)| e^{M_C} M_C^N / N! with their M_C = H |y| / 2 + H^2 / 8, and Cramer's inequality |He_m(t)| <= k sqrt(m!) e^{t^2 / 4} (k = 1 by DLMF 18.14.9, the sharpened form of Abramowitz and Stegun 22.14.17 due to Szasz 1951, found by the collaborator in #426; k is kept in the formulas below as a marker and equals one) at t = y gives |He_n(y + H s)| <= sum_m C(n, m) k sqrt((n - m)!) e^{y^2 / 4} (H / 2)^m <= k sqrt(n!) e^{y^2 / 4} e^{H sqrt n / 2}, because C(n, m) sqrt((n - m)!) = sqrt(n!) sqrt(n! / (n - m)!) / m! <= sqrt(n!) n^{m / 2} / m!. Since phi(y) e^{y^2 / 4} = (2 pi)^{-1/2} e^{-y^2 / 4} and -y^2 / 4 + M_C <= -(|y| - 1)^2 / 4 + 3 / 8 for H <= 1, the cell error is at most sigma^{-1} (2 pi)^{-1/2} e^{3 / 8} e^{-(|y| - 1)^2 / 4} k sqrt(n!) e^{sqrt n / 2} M_*^N / N!, and the midpoint-variation bound of their section 3 (cell width H <= 1 in the variable y) sums the cells to at most A_1 := e^{3 / 8} (2 sqrt 2 + sqrt(2 / pi)) (the integral of (2 pi)^{-1/2} e^{-(|y| - 1)^2 / 4} is at most 2 sqrt 2 and its total variation at most 4 (2 pi)^{-1/2}). So the n-th axis kernel has retained-cell L1 error at most

    epsilon_n <= k A_1 sqrt(n!) e^{sqrt n / 2} M_*^N / N!.

The omitted tail of the n-th axis kernel beyond reach R' is integral_{|X| > R'} |He_n| phi <= sqrt(n!) (2 Phi(-R'))^{1/2} <= sqrt(n!) e^{-R'^2 / 4}, a square root weaker than the Gaussian's, so the Hermite terms need the reach R' = sqrt 2 R to have the tail e^{-R^2 / 2} = delta / 4 per unit of sqrt(n!); the retained cells and M_* = R' / 2 + 3 / 8 grow accordingly.

(iii) Total kernel error [corrected after #433: the displayed total had omitted the spatial tails, which a fixed reach with rho = 0 and N to infinity already shows cannot be dropped]. Write r = |rho|, a = k A_1 (k = 1), E = M_*^N / N!, t = e^{-R'^2 / 4} and S_j(r) = sum_{n <= N_rho} r^n e^{j sqrt n / 2} for j = 0, 1, 2 (S_0 <= 1 / (1 - r)). The n-th axis kernel has, over the whole line, error at most e_n = sqrt(n!) [a e^{sqrt n / 2} E + t] (the retained-cell error of (ii) plus the omitted tail), so for each n, on the period torus after folding, ||p_n^x p_n^y - P_n^x P_n^y||_1 <= e_n ||p_n^y||_1 + ||P_n^x||_1 e_n <= 2 sqrt(n!) e_n + e_n^2, and with the weights r^n / n!

    ||p - P||_1 <= delta_rho + a^2 S_2 E^2 + 2 a (1 + t) S_1 E + (2 t + t^2) S_0,

the collaborator's form, checked here term by term. The sums are finite for r < 1 with the explicit majorant sum_n r^n e^{c sqrt n} <= e^{c^2 / (2 (1 - r))} / (1 - r e^{(1 - r) / 2}) (from c sqrt n <= (1 - r) n / 2 + c^2 / (2 (1 - r)) and log s + (1 - s) / 2 < 0 on (0, 1)), which may replace the finite sums. An allocation that meets an allowance delta: delta_rho <= delta / 4 by the rank of (i); (2 t + t^2) S_0 <= delta / 4, which holds when t <= delta / (12 S_0), that is R'^2 >= 4 log(12 S_0 / delta), so the reach sqrt 2 R of (ii) is not by itself enough and the reach must be chosen against S_0; 2 a (1 + t) S_1 E <= delta / 4 and a^2 S_2 E^2 <= delta / 4 by the exponential order N, with E <= 2^{-N} once N >= 2 e M_* as in their (6). A response in [0, 1] loses at most ||p - P||_1.

(iv) Weights and the table amplification. The Bernstein coefficients of P^{(n)}_C: the monomials s^m = (u - 1 / 2)^m have Bernstein coefficients of magnitude exactly 2^{-m} (the collaborator's remark, #425; they are (-1 / 2)^m (-1)^i), products and degree elevation are convex combinations of coefficient products (their section 2), so max_alpha |beta^{(n)}_{C, alpha}| <= sigma^{-1} phi(y) [sum_m C(n, m) |He_{n - m}(y)| (H / 2)^m] [sum_a M_C^a / a!] <= sigma^{-1} (2 pi)^{-1/2} e^{-y^2 / 4} k sqrt(n!) e^{sqrt n / 2} e^{M_C}, and the folded weights of the n-th term satisfy sum_{r, alpha} |u^{(n)}_alpha(r)| <= sum_C max_alpha |beta^{(n)}_{C, alpha}| <= k A_1 sqrt(n!) e^{sqrt n / 2} =: W_n (the average over the coefficients is at most the maximum, the fold is contractive). The whole correlated query therefore amplifies a uniform table error eta_M by at most sum_n (|rho|^n / n!) W_n^2 = k^2 A_1^2 S_2(rho), in place of 16 eta_M for the product Gaussian; finite for every |rho| < 1 and growing like exp(1 / (2 (1 - |rho|))) as |rho| tends to one. The tables M themselves are untouched, being source-only, but must be acquired at the common degree J' = J + N_rho that the largest admitted correlation needs, every term's cell polynomial elevated to J' and every folded weight normalized by J' + 1 [after #433].

(v) Cost. The query performs N_rho + 1 contractions (5) with the weights u^{(n)}, v^{(n)} and the scalars rho^n / n!, or forms the array sum_n (rho^n / n!) w^^{(n)}(k) z^^{(n)}(k) per Walsh index and contracts once; either way the query cost of their (15) at degree J' is multiplied by N_rho + 1, and the per-axis kernel work grows with the reach sqrt 2 R.

(vi) Not covered, and the honest limits. Widths below one cell on either axis (the cell polynomials need sigma >= 1 as before); |rho| close to one, where the rank and S_2 blow up (an elongated diagonal footprint on a grid whose axes the hash fixes); the numerical formation of the Hermite factors He_{n - m}(y) at the cell centres (a three-term recurrence whose propagation is Lemma 8c's, the product with phi(y) to be computed as a whole) and the dynamic range of signed weights of size up to W_n inside the Walsh transforms against tables in [0, 1], a cost of the same kind as the unnormalized Walsh tables of their section 4. Mehler's expansion is canonical in L^2(phi) but no optimality of the L1 rank among separable expansions is claimed.

(vii) Broad widths by conditioning (the collaborator's completion, #433; audited here and found valid). The Haar replacement of their section 3 applies to a Gaussian factor uniformly in its mean, and conditioning supplies a Gaussian factor: X given Y is Gaussian with variance sigma_x^2 (1 - rho^2) and a Y-dependent mean. If sigma_x sqrt(1 - rho^2) >= P max(1, R / (2 pi)), the wrapped conditional density is within L1 distance delta of the uniform density on the period for every value of the conditional mean, by their (10), and integrating over Y gives ||p_wrapped - p_Y / P||_1 <= delta: the kernel is replaced by the product of the uniform density in x and the Gaussian marginal in y, independent, and the product theorem of their section 3 applies with the Haar weights on the x axis, no Mehler term and no signed factor; symmetrically in y. If neither conditional width reaches the threshold, then sigma_x, sigma_y < P max(1, R / (2 pi)) / sqrt(1 - rho^2), and the Mehler route of (i) to (v) retains at most 2 R' sigma + 2 cells per axis, of order P R' max(1, R / (2 pi)) / sqrt(1 - rho^2), bounded independently of the actual widths with the correlation's cost explicit. This is conditioning plus a uniform Gaussian bound, not whitening (which would rotate the grid) and not a Haar replacement of a signed Hermite factor, which is not claimed anywhere.

## 7l. The Hermite state of a hashed material from the prototype tables, and its composition with polygons by clipping (derivation, parked for audit after 7k at the collaborator's request, #432; proposed in #430)

Two routes now exist and had not met: the Hermite query state of 7g, acquired for polygonal and implicit materials, and the collaborator's prototype-moment contraction for carry-state noise (NOISE-PROTOTYPE-MOMENTS.md at 3b9b3fc), which queries the pixel's Gaussian directly. They meet because the state's kernels are products over the grid axes of exactly the Hermite-term axis kernels of 7k.

(i) The state of a hashed material. Let G be a carry-state material on the unit grid with period P: on cell (i, j), G(i + t, j + u) = f_{d_i, e_j, g_ij}(t, u), any bounded pointwise response of the noise fields with values in [0, 1], with the prototype tables M_{d, e, a, b}(g) = (J' + 1)^2 integral f_{d, e, g} B_a^{J'} B_b^{J'} of their (2) at a degree J' fixed below (a table at degree J' serves every lower degree by convex degree elevation). Take a patch whose reference Gaussian is N(mu_ref, c^2 I) in physical coordinates with c >= 1: the isotropic whitening x = mu_ref + c z keeps the grid axes and makes the cells squares of side H = 1 / c in reference coordinates, and the entry H_alpha = E_gamma[G h_alpha] = integral G h_{alpha_1}(z_1) phi(z_1) h_{alpha_2}(z_2) phi(z_2) dz is an integral against a product kernel over the grid axes. On the cell (i, j), z_1 = y_i + H (t - 1 / 2) with y_i = (i + 1 / 2 - mu_ref,1) / c, and the axis factor with the Jacobian is (1 / c) h_n(y_i + H s) phi(y_i + H s) = p_n^x(i + t) / sqrt(n!) with n = alpha_1: the Hermite-term axis kernel of 7k with sigma = c, divided by sqrt(n!), of L1 norm at most one. So its cell polynomial is 7k's P^{(n)}_C / sqrt(n!), of degree n + J with J = 2 (N_T - 1) the exponential order, with retained-cell L1 error at most A_1 e^{sqrt n / 2} M_*^{N_T} / N_T! and tail at most e^{-R'^2 / 4} beyond the reach R' = sqrt 2 R, and its folded weights u^{(n)}_a(r) = (1 / (J' + 1)) sum_{i = r mod P} beta^{(n)}_{i, a} have l1 norm at most A_1 e^{sqrt n / 2}. Since the prototype depends on the cell only through the residues (i mod P, j mod P), the fold of their (3) and (4) applies entry by entry, and the masked Walsh contraction of their (5) gives

    H_alpha = (1 / P) sum_{d, e, a, b, k} M^_{d, e, a, b}(k) w^^{(alpha_1)}_{d, a}(k) z^^{(alpha_2)}_{e, b}(k)

with the same tables and the Hermite-pair weights; the table degree is J' = N + J. The state has K_N = (N + 1)(N + 2) / 2 entries; the axis transforms are shared, (N + 1)(D_x + D_y)(J' + 1) Walsh transforms of length P in all, and the contractions cost (N + 1)^2 P D_x D_y (J' + 1)^2, plus the cell polynomials of the N + 1 Hermite kernels per axis on the K_c <= 2 R' c + 2 retained cells. Errors: the kernel error of the entry alpha is at most 2 epsilon_bar + epsilon_bar^2 with epsilon_bar = A_1 e^{sqrt N / 2} M_*^{N_T} / N_T! + e^{-R'^2 / 4} (the two axis factors have L1 norm at most one, as in their (12)); a uniform table error eta_M enters the entry as at most A_1^2 e^{(sqrt(alpha_1) + sqrt(alpha_2)) / 2} eta_M <= A_1^2 e^{sqrt N} eta_M, subexponential in the degree; the collective state error is then at most sqrt(K_N) times the largest entry error, and the query amplification sqrt(S(1)) of 7g applies as always. Nothing of the prototype's shape enters: a thresholded noise and a smooth one have the same cost. A remark on wide references: for c much larger than P the wrapped Hermite kernel of degree n >= 1 has Fourier coefficients only at the period's harmonics omega_k = 2 pi k c / P, each at most (2 n / e)^{n / 2} e^{-omega_1^2 / 4} / sqrt(n!), so the entries with |alpha| >= 1 are exponentially small in (c / P)^2 with a factor growing like 2^{N / 2} in the degree, and H_0 is the mean of the prototypes' masses; this is the collaborator's Haar branch for the state, with the reach in c / P growing like sqrt N.

(ii) A polygonal mask. Let B be a polygonal material with constant value v_f on each face Pi_f of a polygonal partition, and F = B G. Each face is treated on its own with the weight v_f. Full cells, those inside Pi_f, form a two-dimensional set, so the fold over residues does not separate; two exact ways remain. By rows: the full cells of a face in row j form at most one run per convex face (at most the number of edges crossing the row otherwise), and with j fixed the residue s = j mod P is fixed, so a run contributes sum_r u^{run}_a(r) v^{(alpha_2)}_b(j) M_{d_r, e_s, a, b}(A_x r xor A_y s) with u^{run} the fold of the run's cells alone, P (J' + 1)^2 operations per run per entry, and the face's full cells cost (rows in the window) x P (J' + 1)^2 per entry. By a two-dimensional residue table: W_{a, b}(r, s) = sum over the full cells with residues (r, s) of beta^{(alpha_1)}_{i, a} beta^{(alpha_2)}_{j, b}, built in (cells in the window) x (J' + 1)^2 operations per entry, then contracted over the P^2 residue pairs. Either way the cost is linear in the window's cell count with no arrangement, and the Walsh transform is the only thing lost. Cut cells, those meeting the boundary of Pi_f, number at most 8 l_f + 4 N_e for a face of perimeter l_f in cell units and N_e edges (Theorem E (a) at cell side one), and each contributes the integral over the clipped cell of the prototype times the exact kernel h_alpha gamma: for a polynomial prototype (polynomial-fade noise, total degree at most 11 K for a response polynomial of degree K, the collaborator's section 6) the monomials t^p u^q times h_alpha(z) gamma(z) are, by z h_n = sqrt(n + 1) h_{n + 1} + sqrt n h_{n - 1}, combinations of h_beta gamma with |beta| <= N + deg f, so the cut cell's contribution is a sum of Gaussian-Hermite moments of a polygon, which is exactly what the edge oracle of 7g computes (Lemma 8's chain, with the offsets of the clipped cell's edges and the degree N + deg f); no new primitive. This needs source access: the prototype as an explicit function on the cell, which the polynomial-fade class supplies once the corner keys are fixed; a prototype known only through its stored moments gives no integral over part of a cell (two bounded functions can share every stored moment and differ on the clipped part), and then the cut cells need partial moments acquired from the source, a separate oracle with its own cost, which is the distinction of section 2 of the collaborator's note [made explicit after #432]. For a thresholded prototype the region inside the cut cell is semialgebraic, and the quadtree of the implicit class restricted to the clipped cell resolves it into rectangles clipped by the face's edges, convex polygons with exact moments, and uncertain sub-rectangles priced by their area under the reference density as in 7h; the uncertain area is the only inexact part.

(iii) What this gives. A hashed noise, with or without a threshold, becomes a fixed material with a compact certified query state at a compile-time cost of order K_N x [P D_x D_y (J' + 1)^2 + transforms], and its product with a polygonal material is acquired by cell clipping at a cost linear in the window's cells and the faces' perimeters, in place of an arrangement or a pair enumeration: for this pairing the composition question is answered by clipping, and the beat machinery of 7b to 7e is not needed. The result is per patch. A fractional shift of the noise layer against the mask moves the mask's vertices and the reference centre in the noise's frame, so it is a re-fold with new weights (the kernel expanded afresh over entire translated cells, which is legitimate) and a re-clip with the cut cells re-acquired from the source, a state rebuild at the cost above and not a table rebuild; the tables are not re-expanded, and the phrase in #430 that a shift is a Bernstein re-expansion of the tables is withdrawn [#432]. Two hashed layers on the same key maps with different profiles form one prototype family (the joint local function is fixed by the same state), so their product is covered by (i); two layers on unrelated grids are not, as in the collaborator's scope.

(iv) Not covered, and what remains to price. References narrower than a cell (c < 1), as in the prototype note; the numerical formation of the Hermite-term cell polynomials (Lemma 8c's recurrence for He_{n - m}(y) with the product with phi(y) computed whole) and the dynamic range of the weights inside the Walsh transforms; the cut cells' oracle errors (Lemmas 8, 8b, 8c, conditional on their inputs); and the per-patch structure, since a moving pixel band crosses patches. Constants are those of 7k and of the collaborator's note; no probe was run for this section.

## 8. The angular route, kept apart

The research log derives, for separated normals, the circle integral of the product of the two clipped factors by the identity cot phi - cot(phi + psi) = sin psi / (sin phi sin(phi + psi)) on the four arcs between the zeros: in cycle units the outside-strip part is (4 / (pi^2 r r' sin psi)) log(pi^2 l_e l_f r r' sin^2 psi) + o(1), the constant in the logarithm being 2 log sin psi (checked to four decimals against the numerical integral by angular-log-constant.mjs). This is an asymptotic statement with an unquantified remainder at any admitted radius, and it is not a certified inequality; its lattice step (the comparison of an annulus sum with an angular integral) is exactly what Lemma 2 replaces. It is recorded because its constant is smaller than Theorem B's by a large factor, and because the layer cake of Lemma 3 applied to the hyperbolic level sets of the product of the two factors on an annulus (the set |m . t_e| |m . t_f| <= 4 / mu within the annulus has angular measure of order mu^{-1} / sin psi with no logarithm, the logarithm arising only from the layer-cake integral) is the route by which that constant could be certified; that route needs the cell corrections of the hyperbolic regions written out, which this note does not do.

## 9. Conditions collected, and what is not covered

Conditions. Theorem A: M >= 2 r_E. Theorem B: M >= max(4 r_E / delta, 2 r_E). Theorem C: h_e >= 2 / l_e, h_f >= r_E + 2 / l_f, T_e >= max(h_e^2, 4 r_E^2), T_f >= max(h_f^2, 4 r_E^2). Lemma 3 in its present form has no condition on the radius or the edge lengths (the earlier M >= rho and M >= 1 / l were artefacts of a cruder split). The partner count N_E is the pair note's L_B for the ellipse E, or pi (r_E + rho_B)^2 / D'; the edge data (lengths, jumps, tangents) is the authored partition's; values in [0, 1] enter only through the jumps. Not covered: boundaries that are not polygonal (excluded as a family; no decay rate is asserted for them here, since a smooth boundary with flat pieces has edge-like directions and a curved arc's rate needs nondegenerate curvature); three or more factors; and the practical list against the envelope (section 7d). Covered since the first draft: the assembled price against the error budget with the reach carried through, in sections 7c and 7d, at a fixed footprint and fixed polygons, and the common set for a compact family of footprints by the worst-case reach. Shared normals are covered by Theorem C (the hyperbolic region contains the coherent pairs at every offset from the line), so the earlier exclusion is lifted.

## 10. Status and credits

Lemma 1, Theorem A and Theorem B are certified given Lemma 2, whose proof is the collaborator's disjoint-cell argument (#353), and Lemma 3, whose form with the shift c and the function Psi is the collaborator's (#356). The two theorems were derived independently on both sides of the bridge and agree in every constant (#356 and this note). The case split of Theorem B, the sum of clipped factors, the widened partner factor and the layer-cake plan are the collaborator's (#353); the Cauchy-Schwarz uniform bound, the per-edge bound in jump amplitudes and the angular constant of section 8 are mine. The checks in the research log (polygon-star-bound.mjs for Lemma 1 pointwise, star-class-tails.mjs for the classes and rates) are exploratory and no constant here rests on them. Prior art: Brandolini, Colzani and Travaglini (1997), Lemma 2.2, for the edge Fourier formula and its angular decay; Durand and others (2005) for interactions organized by spectral orientation; the collaborator's literature search continues so that no classical direction is labelled new. The scalar bounds are proved, and the assembled work count of section 7d (the common union, the selector, the enumeration) is the collaborator's, independently audited on their side (#363, #365) and verified here; the source coefficient acquisition and the numerical contracts remain the pair note's frame. The directional reach R sqrt(t_f^T Sigma^{-1} t_f) is worth carrying in the shifted edge envelope and does not replace the full ellipse partner count (#365).
