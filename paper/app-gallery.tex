\section{Gallery, with settings}
\label{app:gallery}

Every figure in this paper is reproducible from a short parameter list, because a
layer is a parameter list: there is no asset, no baked texture, and no captured
raster anywhere in the pipeline. This appendix gives the settings for the teaser and
for two further compositions, in the same units the interface uses: world units for
lengths, degrees for layer rotation, radians for the per-member rotation
$\theta$.

\subsection{Teaser (Figure~\ref{fig:teaser})}

Eight square panels, each $960\times960$ device pixels on white with $3\times3$
supersampling; the default framing is $369$ world units across (some panels pull
back, see the table), stroke thickness $1.6$ world units unless stated, one ink
throughout, \texttt{\#0e1013}. At the printed panel width of about $1.7$\,in
that is over $550$\,dpi and a hairline well under half a point: fine enough to
survive the press, and what lets a panel carry sixty members across. Each panel
is cut at the dashed rule: composite to the left, the envelope view to the
right at the per-panel contrast $c$ below, $24$ taps.

% The fringes each panel produces are named twice over -- by the panel's own
% name in the first column and at length in the teaser's caption -- and the
% column carrying them a third time was what pushed this table past the column
% and off the page.
\begin{center}\footnotesize
\begin{tabular}{@{}llll@{}}
  \toprule
  Panel & Family A & Family B & $c$ \\
  \midrule
  1 rings     & circles, $s{=}5.5$ at $x{=}{-}72$ & circles, $s{=}5.5$ at $x{=}72$ & $4.5$ \\
  2 terrain   & lines, $s{=}5$, $t{=}3$, golden $\alpha$ & the same $+$ \textsc{terrain}, $a{=}6$, ext.\ $300$ & $3.8$ \\
  3 spirals   & spiral, $s{=}5$, $B{=}90$ & spiral, $s{=}5$, $B{=}{-}60$ & $3.8$ \\
  4 hex twist & hex lattice, $s{=}10$ & the same, turned $5^\circ$ & $4.2$ \\
  5 flow      & lines, $s{=}5$, golden $\alpha$ & the same $+$ \textsc{swirl}, $a{=}3.5$, ext.\ $130$ & $3.6$ \\
  6 walking   & hexagons, $s{=}4$, $\varphi{=}2$, &---& $4.2$ \\
              & $\delta{=}(0.9,0.25)$, $\theta{=}0.025$ & & \\
  7 triangles & triangles, $s{=}5$ & triangles, $s{=}5.4$, turned $6^\circ$ & $4.4$ \\
  8 vortex    & circles, $s{=}8.3$, $t{=}3.5$, & the same, $\delta{=}(0,0.5)$, pose $-5.8^\circ$, & $3$ \\
    \ pair    & $\delta{=}(0,{-}0.5)$, pose $50^\circ$ & at $(9.61,-54.4)$ & \\
  \bottomrule
\end{tabular}
\end{center}

\noindent Panels 2 and 8 frame $565$ and $600$ world units across, panel 4
frames $417$, panel 5 frames $480$. Panel 8 is the instrument's opening scene, kept as its author
exported it from \Moire{}; the scene file, camera included, is in the
repository.

Three observations, since this is the first thing a reader sees.

The split is the claim of \S\ref{sec:envelope} made checkable by eye: every
band that crosses a rule continues on the other side, carrier-textured on the
left and smooth on the right, because the right half is the same two fields
averaged over one shared period of phase: not a blur, and not a filter
applied to the left half.

Panel~6 is a \emph{single} family, not a pair. Its fringes come from its own
members running near-tangent to one another, which happens because $\theta\ne0$
turns each successive hexagon a little further than the last. The classical
two-grating account has nothing to say about this configuration; the
index-difference account handles it without modification, because $D$ is a
difference of index fields and nothing requires the two fields to come from
different layers. The dark curves sweeping through that panel are the family's
\emph{envelope}: the locus where consecutive members touch, where the residual
$h_p(\cdot)$ acquires a double root and the local index field of \S\ref{sec:fringe}
tears. That locus is also why the sufficient interval of Theorem~\ref{thm:window}
has to be as wide as it is: near an envelope the nearest index changes fast.

Panel~3 is worth a line of arithmetic. The two spirals share their spacing, so
the radial parts of their indices cancel and $D=(M_A-M_B)\arg(p)/2\pi$ with
$M$ the signed arm counts, a function of the polar angle alone: the fringes
are exactly $M_A-M_B$ rays, here $18-(-12)=30$.
Note what this predicts and the picture confirms: \emph{rotating} either
spiral produces no new fringes, since a rotation only
adds a constant to $D$. That is the zoetrope property, and it falls out of the
theorem rather than needing a separate argument.

\subsection{Catalog plate (Figure~\ref{fig:plate})}

Settings for all twenty-six panels are in the plate script
(Appendix~\ref{app:repro}). Two conventions are worth stating: every panel covers
$500$ world units square at stroke thickness $1.5$ in those units (twice the
extent a screen-sized view would show, because a beat needs several of its periods
in frame before it reads as a beat), and no panel is a crop, scale or resample of
another. The perturbed copy in the lower half of a cell is a second field
evaluation, not a transformed image of the upper half.

\subsection{Contour compositions (Figures~\ref{fig:contourproof} and~\ref{fig:contourfields})}

Every panel is two \textsc{parallel} layers of pitch $s=\contourCarrier$, alike in
everything but the field carried by the second. So the settings are the expression,
the gain $a$, and the extent that maps world units into its $x$ and $y$: three
values, listed below with what the measurement of \S\ref{sec:beyond} found for
each. Every expression here is one of the presets of Table~\ref{tab:exprs}, so a
row can be reproduced by choosing it from the editor and typing two numbers. \emph{Finest} is the narrowest
on-screen contour interval $c/\lvert\nabla f\rvert$ in pixels, \emph{resolved} the
fraction of probes above the limit \eqref{eq:contournyquist}, and the errors are
displacements of the fringe peak from the true contour in pixels, against an
estimator floor of $\contourFloor$ / $\contourFloorP$\,px.

\input{tab-fields}

The dipole row is the one we would point at. Its field is by far the steepest, so
it is the panel where \eqref{eq:contournyquist} binds hardest (its contours
close to $\contourDipoleFinest$\,px apart, and only $\contourDipoleResolved\%$ of
the frame resolves), and it is also the panel that most obviously looks like a
physics figure. Both facts follow from the same gradient.

The remaining panels differ only in framing. Figure~\ref{fig:contourproof} uses
\textsc{saddle} at $a=0.1$ and extent $50$, a much coarser interval so the bands
are wide enough to read on paper and the true curves have room to be drawn over
them.
