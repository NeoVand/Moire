# Game-capture scorer controls

The calibrated MRQ palette and 54 known original-ray parity fixtures pass. The actual contaminated 16-bit game PNG fails its documented gamma-2.2 palette check. A synthetic metadata fixture around that image verifies that transfer failure alone withholds every quantitative score and measured linear field. Missing gamma commands, wrong dump stages, and invalid gamma metadata are rejected. This is utility validation, not an accepted game-render comparison. A corrected three-arm capture is still pending. See [report.json](report.json).
